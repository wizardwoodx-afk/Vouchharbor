# MJ 11.9 — NTH VISUAL SYSTEM + CANVAS INTEGRITY

> One release, three parts: the app is **de-vendored**, painted in the new **NTH** palette, and
> the **canvas** gets the fixes that made wires and ports behave. Bumped `11.9.0` across every
> manifest so it can be shipped as a single upgrade.

## 0. De-vendored

Removed `vendor/hermes-agent` + `vendor/hermes-agent-self-evolution` (~633 files / ~12 MB).
MJ never executed that code — it was only a path constant, an unused env var and comments. The
SKILL.md contract, parsing, and evolution fitness/constraints engine are MJ's own TypeScript.
`NOTICE`/`VENDOR.md` updated; only the two runtime MCP server sets remain vendored (and credited).

## 1. NTH theme (default)

One design system, five rare colours, **zero brown**:

| Token | Colour | Role |
|---|---|---|
| `void` | `#05060b` | obsidian ground |
| `ink` | `#f2f5fb` | primary text |
| `ash` | `#929cb0` | dim / secondary |
| `volt` | `#7c5cff` | electro-violet signal (accent) |
| `plasma` | `#2be6d6` | "alive" pulse (running) |

A single functional red (`#ff3d6e`) is reserved for errors only — never a brand colour. `nth` is
now the default theme, joins every shared selector group, and starts from the same `inscribed`
motion/shape language (percussive, never spring).

> **Motion stays quiet.** An early V12 pass added entrance rises, card stagger, a hover-lift, an
> animated volt tab underline and a toast slide — the owner rejected that as gimmicky. Those were
> cut from `mj.css`; what survives does work, not show: `mj-lamp` breathing (respects
> `prefers-reduced-motion`), a readable keyboard focus ring, crisp volt text selection, and
> scrollbars in the palette.

## 2. Canvas fixes

- **Wires connect to ports.** Port geometry is now measured from **rendered bounding boxes**
  (anchor rect − card rect, divided by zoom) instead of an `offsetTop` walk — which was off by
  ~6px and grew per row, detaching wires from the ports. Wires re-measure after every node
  drag/drop/connect (a `requestAnimationFrame(invalidatePortMetrics)` on graph change), so a moved
  node's wires re-anchor to the port a human can see.
- **Overlapping nodes stay clean.** Each `.node-card` is now an isolated stacking context
  (`isolation: isolate`) with an explicit `z-index`, and the selected/dragged node lifts to the
  top — so an overlapping node never exposes the behind node's ports.
- **Ports sit inside the card edge** (moved off `margin: -6px` to a proper inset) and glow on
  hover/valid-target so the connection target is unmistakable.
- **Node sizing** is one source of truth (`NODE_W`), and the pre-measure fallback now tracks the
  real first-port offset. **Minimap** keeps its own clear corner.

## 3. Verification

- `npm run typecheck`, `npm run build` — clean
- `npm test` — all suites incl. `canvasGeometry` (82/82), `theme`, `versionDrift` pass
- Live browser: wires land on ports within ~1px; overlapping node occludes the one behind.

## 4. To run

```bash
npm ci && npm test && node verify/run.mjs && npm run tauri dev
```

## 5. 11.9.1 — release-packaging + provenance patch

- **Offline pack shipped intact.** The 11.9.0 archive had `verify/suites/` empty, so
  `node verify/run.mjs` reported a vacuous "0 passed / 0 failed" and exited green. 11.9.1
  regenerates all **39 bundles from the 11.9.1 source** and the archive now verifies a real
  `OFFLINE VERIFY SUMMARY: 39 passed, 0 failed` on a clean extraction.
- **`verify/BUILD-INFO.txt`** rewritten for 11.9.1 (was stale at 11.8.5), stating the true
  toolchain and that the pack does not compile Rust or run `tsc`/`vite` (needs dev toolchain).
- **`docs/DEEP-DEBUG-REPORT.md`** title corrected to 11.9.1 and its stale "still open by choice"
  type-hole section reconciled — those three holes are closed via the typed `MjCommands`
  registry (`tauriInvoke<K extends keyof MjCommands>`).
- **README** `vendor/`, `src-tauri/` and history lines corrected (no Hermes agent remains;
  history now runs to 11.9.1).
- Gate: `tsc` 0 · `npm test` 40/40 · `verify/run.mjs` 39/39 · `vite build` 0.

## 6. 11.9.4 — verification toolchain + provenance

- **Offline pack built under Node 22.** 11.9.1's pack was generated with **Node 20**, which
  reached end-of-life on 2026-04-30. 11.9.4 regenerates all **39 bundles from the 11.9.4
  source with Node v22.23.2** — the same supported LTS line CI pins (`node-version: 22`).
- **`verify/BUILD-INFO.txt`** now records the Node 22 toolchain and notes that Node 20 is EOL
  and must not certify a release.
- **`docs/VERIFICATION.md`** tier line updated to MJ 11.9.4.
- Independent SHA-256 recompute against `verify/MANIFEST.json`: **39/39 match**.
- Gate under Node 22: `tsc` 0 · 40/40 live probes (incl. `offlinePack` freshness + `versionDrift`)
  · `verify/run.mjs` 39/39 · `vite build` 0.

## §7 — 11.9.3: the NTH theme actually renders (UI/UX audit catch)

The owner asked for a real UI/UX change; a live screenshot audit showed **none** — the app
was visually identical to the pre-NTH red release even with the NTH chip selected.

Root cause was a CSS cascade tie, not a missing theme. `mj.css` opens with a `:root { ... }`
fallback palette (the inscribed red/black) — specificity **0,1,0**. The `@import "./themes.css"`
is hoisted to the top of `mj.css`, so the `[data-theme="…"]` palette blocks (also **0,1,0**)
came **earlier** in source order. Equal specificity, later source wins → every theme, including
NTH, silently rendered the inscribed fallback. The selected "NTH" chip was a lie.

Fix: pin the fallback to **zero specificity** with `:where(:root)`, so any `[data-theme="…"]`
block always wins the cascade. Verified on a live render: all 12 NTH tokens now resolve
correctly (volt `#7c5cff` accent, obsidian `#05060b` ground, plasma `#2be6d6` pulse), and the
buttons, rail, chips and focus rings all render violet. This is the visible NTH UI the user asked for.

## §8 — 11.9.4: the FOUNDRY redesign (frontend-only)

The owner sent the v3 "Domain Color Atlas" and asked for a real frontend upgrade:
"use any 5 colors and change the icons… don't change the backend." The previous
releases genuinely changed the look (11.9.3 made NTH render at all) but it was still the
cold obsidian/violet identity — read as "similar." So the flagship was **re-cut**, not
recolored:

- **New identity (5 rare tones, no neon):** carbon ground `#0e0f0e`, cream ink `#f3eee3`,
  brass accent `#b8a17b`, verdigris "alive" pulse `#7ba58e`, and a functional oxide
  `#c0563f` reserved for errors. The `:root` boot-fallback was re-based to the same set.
- **A serif display voice:** `--font-serif` (Georgia) drives the `MJ.` wordmark, the hero
  headline, and every panel page title — the atlas's editorial "archive/foundry" tone.
- **Redrawn icon set:** the FOUNDRY cut — heavier 1.9 engraved stroke, miter joins, and a
  small filled *facet chip* at each glyph's junction (replacing the old round LED dot).
  The active rail item gets a brass plate marker + corner facet.
- **Backend untouched.** No graph/engine/ipc logic changed; only `themes.css`, `mj.css`,
  `canvas/icons.tsx`, Settings copy, and the two design probes, which now assert the new
  palette and the FOUNDRY icon grammar.

---

# 11.9.9 — the verified agent factory (2026-09-06)

> Research first: every 2026 orchestrator solves coordination; nobody solves proof. MJ's
> answer is a spine — **your harnesses, your roles → run → adversarial gate → receipt → control**.

## What landed

1. **Adversarial Verification Gate** (`src/mission/verifyGate.ts`). A run is verified only
   when a seat with a DIFFERENT harness than the writer ran and approved. Tiers:
   cross-vendor > cross-seat > self-verification > unverified. STRICT blocks; ADVISORY marks.
   Receipt required in STRICT — proof travels with the verdict.
2. **Receipt & Compliance Center** (`src/pages/ProofPage.tsx` + `src/mission/receiptVault.ts`).
   The old Proof placeholder is now a real center: vault of issued receipts (gate verdict
   attached), self-audit re-verifying every chain, SIEM JSONL export, one-pager mapping the
   evidence layer to EU AI Act Art. 12 — with MJ's non-claims stated equally plainly.
3. **Mission Control** (`src/pages/MissionControlPage.tsx` + `src/mission/fleet.ts`). Fleet
   board, cost ledger by harness and role (measured CLI figures only), stagnation alerts,
   approval inbox. New nav entry (radar icon).
4. **Role Board** (Teams → Role Board, `src/mission/roleBoard.ts`). Users declare their
   harness inventory and assign roles themselves; auto-assign is a starting point, never a
   verdict. Findings flag unowned assignments, llm-writers and self-verification risk.

## Wiring

The Teams Mission Runner now: emits fleet events at start and per-seat finish (with measured
cost), evaluates the gate on measured outcomes, issues the receipt into the vault with the
gate verdict in-chain (`gate.verdict` event, only present for gated runs), and settles the
autonomy engines with the same verdict.

## Gates

Four new probe suites (#49 verifyGate, #50 roleBoard, #51 receiptVault, #52 fleet).
52/52 live suites, 51/51 offline bundles, tsc 0, vite build 0 — certified under Node v22.23.2.
