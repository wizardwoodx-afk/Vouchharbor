# VH 16.9.0 — THE MODERN-CLIENT RELEASE (2026-09-10)

> Both protocol eras externally validated by their official clients:
> legacy 2025-11-25 (SDK v1) and modern 2026-07-28 (SDK v2) — the dual-era
> claim proven from the outside on both sides. Plus the capability genome
> (16.8), MCP conformance (16.6), and the external-validation + benchmark
> work (16.7) inherited along the way.

The 16.9.0 upgrade notes ship in upstream `docs/history/VH-16.9-UPGRADE.md`
and the regenerated `verify/BUILD-INFO.txt`. This file is the discoverable
pointer in-repo.

## Gates

Re-run for 16.9.0: `tsc --noEmit` 0, **91/91 live suites**,
**90/90 offline bundles**, `vite build` 0, `npm audit --omit=dev` 0.
16.7.0 was internal (see VERSION-CHANNELS.md).

## Windows-fix note (this tree)

The shipped zip fails one gate on Windows: `mcpRouter`'s rebuild check
shells out to `node_modules/.bin/esbuild`, which exists but is not
executable on Windows (ENOENT) — fixed here (`esbuild.cmd` + shell flag).
The `loopBusFeed` detached-subscribe crash (every LoopPage mount) is also
fixed here (closure keeps the bus instance). Full record:
`docs/history/VH-16.9.0-WINDOWS-FIX.md`.
