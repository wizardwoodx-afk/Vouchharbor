# MJ 11.12.1 — VARKHA GOVERNANCE EXTRACTS (2026-09-07)

> 11.12.0 was the MOSAIC-Ω alignment extract. 11.12.1 is the Varkha extract —
> a second external framework reference, audited against the current source.
> No new subsystems were added; the review fix is the headline: the execution
> boundary is now fully cryptographic, so the runtime proves the exact signed
> packet says the action is allowed before it ever runs or spends.

The 11.12.1 release notes ship as a single self-contained upgrade in upstream
`UPGRADE.md` / `NEXT-UPGRADES.md`, and the full compatibility audit lives in
`docs/VARKHA-COMPAT.md`. This file is the discoverable pointer in-repo; the
full release narrative is in `CHANGELOG.md` and the regenerated
`verify/BUILD-INFO.txt`. Both ship with the source.

## What landed (the Varkha extracts)

- **Review fix (9.75/10 review): the execution boundary is now fully
  cryptographic.** `TeamExecutor` calls `verifyActionPacket` (digest + Ed25519)
  FIRST, aborts on any mismatch, and only then applies `packetAllowsExecution`.
  The runtime now proves "this exact signed packet says this action is allowed".
  Pinned by `varkhaAlign` §5 (source-order + tamper behaviour).
- **SCAR before PRECEDENT.** `lessonsForBriefing` retrieves matching failure
  lessons first, then the rest — an optimism-bias correction.
- **Write asymmetry.** Agent-inferred beliefs ABOUT THE USER start pending and
  brief only after human approval; user-stated preferences are immediate.
- **Attenuation.** The report carries per-seat envelopes (role scope,
  `write:worktree` / `read:review-snapshot`, `spend:capped-by-ledger`) derived
  from the packet that permitted the run.
- **Step-repetition signal.** Identical consecutive turn signatures per seat
  (MAST's step repetition) surface as a SUGGEST-level Chamber signal, never
  auto-acted, never fed to the strategy experiment.
- **Ablation clarity.** `diffDims` pins that every candidate — mosaic included
  — differs from its parent by exactly one dimension: each experiment is a
  paired toggle against the same parent configuration.

## Gates

Re-run for 11.12.1 under Node v22.23.2: `tsc --noEmit` 0, **62/62 live suites**,
**61/61 offline bundles**, `vite build` 0. New probe suite #62 (`varkhaAlign`)
contributes 17 governance assertions. The full 11.10.5 / 11.11.0 / 11.12.0
release notes (compliance vertical, self-evolution loop, MOSAIC-Ω alignment)
remain accurate and unchanged for 11.12.1.
