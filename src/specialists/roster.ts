/**
 * The generalist roster — the domain specialists that run on the same engine as everything
 * else, with the same two rules the finance pack established: the arithmetic is
 * deterministic, and anything that changes production stops at a human.
 *
 * `status` is honest rather than aspirational: "engine" means a real exported function does
 * the work and a probe drives it; "workflow" means the specialist is a governed sequence
 * whose steps are engine-backed but whose judgement is a human's.
 *
 * `requiresApproval` marks the specialists whose last step touches production, a customer, a
 * credential or money. A read-only audit does not need a gate; a deploy, a rotation, a price
 * change and a customer-facing send do.
 */
import type { Domain } from "./types";

export interface Specialist {
  id: string;
  name: string;
  domain: Domain;
  purpose: string;
  /** Exported function(s) that perform the computation. */
  engine: string;
  inputs: string;
  output: string;
  requiresApproval: boolean;
  /** What the Velvet Hand receipt attests when the specialist completes. */
  receipt: string;
  status: "engine" | "workflow";
}

const A = (a: Specialist): Specialist => a;

export const SPECIALISTS: readonly Specialist[] = Object.freeze([
  /* ── frontend ───────────────────────────────────────────────────────────── */
  A({ id: "fe.contrast-audit", name: "Contrast Audit", domain: "frontend", status: "engine",
      engine: "contrastRatio + wcagVerdict", inputs: "a token set or a list of foreground/background pairs",
      output: "each pair's ratio and the highest WCAG level it meets",
      purpose: "Check every colour pair in a token set against WCAG so accessibility is a build failure rather than a review comment.",
      requiresApproval: false, receipt: "the ratio computed for each pair, the standard applied, and the token set it came from" }),
  A({ id: "fe.token-convert", name: "Token Converter", domain: "frontend", status: "engine",
      engine: "parseColor + toHex", inputs: "colours in hex, rgb() or hsl() from a design file",
      output: "every notation a codebase needs, alpha preserved",
      purpose: "Convert design-file colours into the exact strings a stylesheet, a canvas call and a native theme each require.",
      requiresApproval: false, receipt: "the source value, the conversions produced, and the alpha handling" }),
  A({ id: "fe.type-scale", name: "Type Scale", domain: "frontend", status: "engine",
      engine: "typeScale", inputs: "a base size and a ratio", output: "the scale in px and rem",
      purpose: "Generate the type scale a design system is built on, with the rounding decided once instead of per component.",
      requiresApproval: false, receipt: "the base, the ratio, and every step produced" }),
  A({ id: "fe.grid-drift", name: "Grid Drift", domain: "frontend", status: "engine",
      engine: "snapToGrid", inputs: "spacing values found in a stylesheet", output: "off-grid values and the distance to the grid",
      purpose: "Find the spacing values that have drifted off the 4/8px grid before they become a visual rhythm nobody can name.",
      requiresApproval: false, receipt: "the values tested, the movement each one requires, and the grid base" }),
  A({ id: "fe.a11y-fix", name: "Accessibility Fix Plan", domain: "frontend", status: "workflow",
      engine: "wcagVerdict", inputs: "a contrast audit plus the component inventory",
      output: "a ranked list of token changes that clear the most failures",
      purpose: "Turn a list of failing pairs into the smallest set of token changes that fix the most of them.",
      requiresApproval: true, receipt: "the plan, the failures it clears, and the human's decision before any token changed" }),

  /* ── dev / release engineering ──────────────────────────────────────────── */
  A({ id: "dev.release-guard", name: "Release Guard", domain: "dev", status: "engine",
      engine: "satisfiesRange + compareSemver", inputs: "the manifest, the lockfile and a proposed version",
      output: "ranges a dependency violates, with each range expanded to its real bounds",
      purpose: "Check a release's dependency ranges before the install fails in CI and somebody reaches for --force.",
      requiresApproval: false, receipt: "every range tested, the version tested against it, and the expansion used" }),
  A({ id: "dev.changelog", name: "Changelog Composer", domain: "dev", status: "engine",
      engine: "lintCommit", inputs: "a commit range", output: "a categorised changelog and the commits it could not parse",
      purpose: "Turn a commit range into a readable changelog and name every commit whose message defeated the parser.",
      requiresApproval: false, receipt: "the commits read, what each became in the changelog, and every unparsed one" }),
  A({ id: "dev.retry-shape", name: "Retry Shape", domain: "dev", status: "engine",
      engine: "backoffSchedule", inputs: "a retry policy or the values the client implements",
      output: "the real schedule and the total time before giving up",
      purpose: "Show what a retry ladder actually does over time, so a policy that never finishes is caught in review.",
      requiresApproval: false, receipt: "the policy as implemented, the schedule produced, and the client timeout compared against it" }),
  A({ id: "dev.schedule-audit", name: "Schedule Audit", domain: "dev", status: "engine",
      engine: "parseCron", inputs: "cron expressions from a scheduler", output: "next runs, daylight handling and the day-field rule",
      purpose: "Catch the scheduled job that will not fire, or will fire twice, before anyone notices the data is stale.",
      requiresApproval: false, receipt: "each expression, the runs computed, and the dialect assumed" }),
  A({ id: "dev.migration-plan", name: "Migration Plan", domain: "dev", status: "workflow",
      engine: "deployRisk", inputs: "a schema diff and the deploy plan", output: "the ordered steps and the rollback path",
      purpose: "Sequence a schema change so the deploy can be rolled back without losing the data it wrote.",
      requiresApproval: true, receipt: "the plan, the rollback path, and the human's approval before anything ran against production" }),

  /* ── API ────────────────────────────────────────────────────────────────── */
  A({ id: "api.limit-fit", name: "Rate Limit Fit", domain: "api", status: "engine",
      engine: "tokenBucketPlan", inputs: "the published limit and the client's demand",
      output: "sustainable rate, first rejection and the timeline",
      purpose: "Tell a client team what demand its integration can actually sustain before it meets the limit in production.",
      requiresApproval: false, receipt: "the limit, the demand simulated, and the timeline produced from them" }),
  A({ id: "api.payload-budget", name: "Payload Budget", domain: "api", status: "engine",
      engine: "payloadBudget", inputs: "a response body", output: "size per field and what dominates it",
      purpose: "Find the field that is making a response large while clients pay to download and discard it.",
      requiresApproval: false, receipt: "the payload measured, the per-field breakdown, and the compression question left unanswered" }),
  A({ id: "api.idempotency-review", name: "Idempotency Review", domain: "api", status: "engine",
      engine: "checkIdempotencyKey", inputs: "the key format the client sends and the retention window",
      output: "entropy, shape issues and the retention mismatch",
      purpose: "Check that a write path's idempotency key is actually unguessable and remembered for long enough to matter.",
      requiresApproval: false, receipt: "the key shape measured, the entropy floor, and the retention the server promised" }),
  A({ id: "api.paging-advice", name: "Paging Advice", domain: "api", status: "engine",
      engine: "paginationPlan", inputs: "row counts, page size and the offset pattern in use",
      output: "page arithmetic and whether the offset is deep enough to hurt",
      purpose: "Say when offset paging stops being fine, and what a keyset cursor would cost instead.",
      requiresApproval: false, receipt: "the arithmetic, the depth observed, and the recommendation with its reason" }),
  A({ id: "api.contract-change", name: "Contract Change", domain: "api", status: "workflow",
      engine: "httpSemantics + payloadBudget", inputs: "an API diff and the consumer list",
      output: "breaking changes, affected consumers and a deprecation schedule",
      purpose: "Classify an API change as breaking or not, name which consumers it touches, and propose the schedule.",
      requiresApproval: true, receipt: "the diff classified, the consumers named, and the human's decision before any deprecation notice went out" }),

  /* ── data ───────────────────────────────────────────────────────────────── */
  A({ id: "data.latency-read", name: "Latency Read", domain: "data", status: "engine",
      engine: "percentile", inputs: "a latency series", output: "the percentiles with the sample size stated",
      purpose: "Report latency the way users experience it — at the tail, with the sample size that supports it.",
      requiresApproval: false, receipt: "the series, the interpolation method, and every percentile computed" }),
  A({ id: "data.outlier-triage", name: "Outlier Triage", domain: "data", status: "engine",
      engine: "outliersIqr", inputs: "a data series", output: "fenced values and why the rule may be degenerate here",
      purpose: "Surface the points outside the fences, and say plainly when the sample is too small for the rule to mean anything.",
      requiresApproval: false, receipt: "the fences, the points outside them, and the caveats about the sample" }),
  A({ id: "data.experiment-readout", name: "Experiment Readout", domain: "data", status: "engine",
      engine: "abTest + sampleSize", inputs: "two arms of a test", output: "lift, interval, p-value and the peeking warning",
      purpose: "Read out a test with the uncertainty attached, and refuse to call a result the design could never detect.",
      requiresApproval: false, receipt: "the arms, the test used, the interval, and the honesty notes about stopping rules" }),
  A({ id: "data.sample-plan", name: "Sample Plan", domain: "data", status: "engine",
      engine: "sampleSize", inputs: "a baseline rate and the effect worth detecting",
      output: "trials per arm and the time that implies",
      purpose: "Size a test before it starts, so the decision is made on the power of the design rather than the patience of the team.",
      requiresApproval: false, receipt: "the baseline, the effect, the power and alpha chosen, and the sample derived" }),
  A({ id: "data.metric-definition", name: "Metric Definition", domain: "data", status: "workflow",
      engine: "percentile + stddev", inputs: "a metric name, its proposed definition and the events available",
      output: "a definition with its exclusions and its failure modes",
      purpose: "Write down what a metric means — including the cases it excludes — before it appears on a dashboard.",
      requiresApproval: false, receipt: "the definition, the exclusions, and the events it is actually computed from" }),

  /* ── security ───────────────────────────────────────────────────────────── */
  A({ id: "sec.secret-sweep", name: "Secret Sweep", domain: "security", status: "engine",
      engine: "scanSecrets", inputs: "a diff, a config or a log sample", output: "matched credential patterns, masked, with the false-positive caveat",
      purpose: "Find the credential shapes that have a fixed format — and state what a pattern scan cannot find.",
      requiresApproval: false, receipt: "the patterns tested, the matches found (masked), and the limits of the method" }),
  A({ id: "sec.token-inspect", name: "Token Inspect", domain: "security", status: "engine",
      engine: "jwtInspect", inputs: "a JWT from a support ticket", output: "claims, expiry and the algorithm in use",
      purpose: "Decode what a token says and when it dies, without ever implying the signature was checked.",
      requiresApproval: false, receipt: "the claims decoded, the arithmetic on exp, and the explicit statement that nothing was verified" }),
  A({ id: "sec.header-audit", name: "Header Audit", domain: "security", status: "engine",
      engine: "cspAudit", inputs: "a Content-Security-Policy header", output: "directives that are doing nothing, by severity",
      purpose: "Name the clauses of a security policy that look protective and are not.",
      requiresApproval: false, receipt: "the policy as supplied, every finding, and the checklist it was tested against" }),
  A({ id: "sec.entropy-floor", name: "Entropy Floor", domain: "security", status: "engine",
      engine: "stringEntropyBits", inputs: "a candidate key, password or token", output: "measured bits and what the measure does not prove",
      purpose: "Measure how much a string actually carries, and be clear that entropy measures the string and not the generator.",
      requiresApproval: false, receipt: "the string measured (not stored), the bits, and the interpretation with its limits" }),
  A({ id: "sec.rotation-runbook", name: "Rotation Runbook", domain: "security", status: "workflow",
      engine: "scanSecrets + stringEntropyBits", inputs: "a confirmed exposure and the systems that use the credential",
      output: "the revoke → rotate → verify sequence and its owners",
      purpose: "Sequence a credential rotation so revoking comes before rotating, and verification comes before the all-clear.",
      requiresApproval: true, receipt: "the sequence, the owners, and the human's approval — rotation touches production credentials" }),

  /* ── ops ────────────────────────────────────────────────────────────────── */
  A({ id: "ops.budget-watch", name: "Error Budget Watch", domain: "ops", status: "engine",
      engine: "sloErrorBudget", inputs: "an SLO, a window and consumption so far", output: "remaining budget, burn rate and time to exhaust",
      purpose: "Keep the reliability decision a number rather than an argument held during an incident.",
      requiresApproval: false, receipt: "the SLO, the window, the consumption observed, and the extrapolation with its assumption stated" }),
  A({ id: "ops.capacity-plan", name: "Capacity Plan", domain: "ops", status: "engine",
      engine: "capacityHeadroom", inputs: "current utilisation, growth rate and a ceiling",
      output: "the month the ceiling is crossed and the runway",
      purpose: "Give the launch question a date: when growth meets the ceiling, if nothing changes.",
      requiresApproval: false, receipt: "the inputs, the compound curve, and the breach month computed from them" }),
  A({ id: "ops.incident-class", name: "Incident Classifier", domain: "ops", status: "engine",
      engine: "incidentSeverity", inputs: "impact numbers from triage", output: "an S1–S4 call with the rule that produced it",
      purpose: "Assign severity from impact rather than from who noticed, using a rule that is printed alongside the verdict.",
      requiresApproval: false, receipt: "the impact data, the severity, and the rule applied — printed so it can be argued with" }),
  A({ id: "ops.deploy-gate", name: "Deploy Gate", domain: "ops", status: "engine",
      engine: "deployRisk", inputs: "the change, its tests, its rollback and its timing",
      output: "a risk score, the active factors and a recommendation",
      purpose: "Score a deploy against the six factors that turn releases into incidents, and say which one to fix first.",
      requiresApproval: true, receipt: "the factors assessed, the score, and the human's go-ahead — this gate can stop a release" }),
  A({ id: "ops.postmortem", name: "Postmortem Drafter", domain: "ops", status: "workflow",
      engine: "incidentSeverity + sloErrorBudget", inputs: "the incident timeline and its impact",
      output: "a blameless timeline, the budget cost and the actions with owners",
      purpose: "Draft the postmortem from the timeline, with the impact measured against the budget it consumed.",
      requiresApproval: false, receipt: "the timeline used, the budget arithmetic, and the actions with their owners" }),

  /* ── docs ───────────────────────────────────────────────────────────────── */
  A({ id: "docs.reading-level", name: "Reading Level", domain: "docs", status: "engine",
      engine: "readability", inputs: "prose from a page or a README", output: "grade level, ease score and the cause",
      purpose: "Keep user-facing documentation at the reading level its audience actually has.",
      requiresApproval: false, receipt: "the text measured, the scores, and the syllable-estimation caveat" }),
  A({ id: "docs.len-budget", name: "Length Budget", domain: "docs", status: "engine",
      engine: "readingTime", inputs: "a document", output: "words, minutes and code lines",
      purpose: "Decide whether a page needs a summary by knowing how long it takes to read.",
      requiresApproval: false, receipt: "the word count, the assumed rate, and the code lines counted separately" }),
  A({ id: "docs.structure-check", name: "Structure Check", domain: "docs", status: "engine",
      engine: "headingLint", inputs: "markdown", output: "level jumps, duplicate anchors, title problems",
      purpose: "Keep headings in a shape that a table of contents and a screen reader can both follow.",
      requiresApproval: false, receipt: "the heading tree, every structural issue, and its line number" }),
  A({ id: "docs.terminology-lock", name: "Terminology Lock", domain: "docs", status: "engine",
      engine: "terminologyDrift", inputs: "a document set", output: "terms written more than one way",
      purpose: "Keep one spelling per concept so search, glossary links and translation keep working.",
      requiresApproval: false, receipt: "the variants found with their counts, and the fixed variant list used" }),
  A({ id: "docs.release-notes", name: "Release Notes", domain: "docs", status: "workflow",
      engine: "lintCommit + readability", inputs: "a changelog and the audience it serves",
      output: "notes written for users, checked for reading level",
      purpose: "Turn engineering changes into notes a customer can act on, at a reading level they can finish.",
      requiresApproval: true, receipt: "the source commits, the notes drafted, and the human's approval before anything was published" }),

  /* ── growth ─────────────────────────────────────────────────────────────── */
  A({ id: "growth.economics", name: "Unit Economics", domain: "growth", status: "engine",
      engine: "unitEconomics", inputs: "revenue, margin, acquisition cost and churn",
      output: "lifetime value, the ratio and the payback period",
      purpose: "Answer whether growth pays for itself before more money is spent proving it.",
      requiresApproval: false, receipt: "the inputs, the ratio, the payback, and the assumptions the model rests on" }),
  A({ id: "growth.funnel-read", name: "Funnel Read", domain: "growth", status: "engine",
      engine: "funnel", inputs: "stage counts in order", output: "stage conversions and the largest proportional loss",
      purpose: "Point the next experiment at the step that loses the most, rather than the step with the most traffic.",
      requiresApproval: false, receipt: "the counts, each stage's conversion, and the step identified as the constraint" }),
  A({ id: "growth.backlog-rank", name: "Backlog Ranker", domain: "growth", status: "engine",
      engine: "riceScore", inputs: "candidate work with reach, impact, confidence and effort",
      output: "a ranked table with the inputs left visible",
      purpose: "Rank a backlog so disagreement can be traced to an input somebody chose, not to seniority.",
      requiresApproval: false, receipt: "every candidate scored, the formula used, and the inputs as supplied" }),
  A({ id: "growth.plan-check", name: "Plan Check", domain: "growth", status: "engine",
      engine: "growthModel", inputs: "a growth plan with its churn assumption",
      output: "the trajectory, the exit ARR and the implied rate",
      purpose: "Test whether a revenue plan survives its own churn assumption before it is presented.",
      requiresApproval: false, receipt: "the plan modelled month by month, the assumed rates, and the exit position" }),
  A({ id: "growth.price-change", name: "Price Change", domain: "growth", status: "workflow",
      engine: "unitEconomics + growthModel", inputs: "a proposed price change and the affected base",
      output: "the margin effect, the churn break-even and the affected cohorts",
      purpose: "Model a price change: what it earns, what churn it can absorb, and who it touches.",
      requiresApproval: true, receipt: "the model, the break-even churn, and the human's approval — a price change is customer-facing" }),

  /* ── 19.7.15 [Cartographer]: the pack widens to twenty-four domains ───────── */

  /* ── mobile ───────────────────────────────────────────────────── */
  A({ id: "mobile.touch-audit", name: "Touch-target audit", domain: "mobile", status: "engine",
      engine: "touchTargets", inputs: "a screen's controls and their pixel sizes", output: "every control's verdict against the platform floor",
      purpose: "Measure each control against the platform's minimum touch size before a reviewer finds the one nobody can hit.",
      requiresApproval: false, receipt: "each target measured, the floor applied, and where the spacing exception was used" }),
  A({ id: "mobile.size-budget", name: "Size budget", domain: "mobile", status: "engine",
      engine: "appSizeBudget", inputs: "current download size, budget and growth per release", output: "the release at which the budget is crossed",
      purpose: "Project download size release by release so the budget is a number somebody watches, not a surprise at store review.",
      requiresApproval: false, receipt: "the size today, the growth applied, and the release where the budget is crossed" }),
  A({ id: "mobile.grid-snap", name: "Grid snap review", domain: "mobile", status: "engine",
      engine: "snapToGrid", inputs: "spacing tokens taken from the design file", output: "off-grid values and the distance to the grid",
      purpose: "Keep spacing tokens on one grid so two screens built by two people still look like one product.",
      requiresApproval: false, receipt: "every token checked, off-grid values named, and the direction of each correction" }),
  A({ id: "mobile.startup-percentiles", name: "Startup percentiles", domain: "mobile", status: "engine",
      engine: "percentiles", inputs: "cold-start timings collected from a device fleet", output: "p50, p95 and p99 rather than an average",
      purpose: "Report startup at the percentiles users feel, because an average hides the slower half of a device fleet.",
      requiresApproval: false, receipt: "the sample size, the percentile method used, and the p50/p95/p99 readout" }),
  A({ id: "mobile.ab-readout", name: "UI experiment readout", domain: "mobile", status: "engine",
      engine: "abTest", inputs: "two arms with their conversions", output: "the lift, its interval and whether it is real",
      purpose: "Read a UI experiment honestly: the lift, its uncertainty, and whether the sample supports shipping it.",
      requiresApproval: false, receipt: "both arms, the test applied, and the interval around the difference" }),
  A({ id: "mobile.release", name: "Mobile release", domain: "mobile", status: "workflow",
      engine: "appSizeBudget + touchTargets", inputs: "a build number and the store submission checklist", output: "a release package ready to submit, gated",
      purpose: "Ship a mobile build: the size and touch-target gates cleared, store metadata checked, and a human approving the push.",
      requiresApproval: true, receipt: "the gates cleared, the metadata checked, and the human's approval of the store submission" }),
  A({ id: "mobile.store-listing", name: "Store listing copy", domain: "mobile", status: "engine",
      engine: "metaLint", inputs: "title, subtitle and description for the store", output: "what each field will truncate",
      purpose: "Catch truncation in a store listing before the store does it silently on a customer's phone.",
      requiresApproval: false, receipt: "each field measured, the display limit applied, and the truncation point named" }),
  A({ id: "mobile.locale-coverage", name: "Locale coverage", domain: "mobile", status: "engine",
      engine: "localeCoverage", inputs: "translated keys per app locale", output: "which locales clear the shipping bar",
      purpose: "Decide which app locales can ship from translated keys on disk rather than from intent in a planning document.",
      requiresApproval: false, receipt: "the key count, each locale's coverage, and the bar that was applied" }),
  A({ id: "mobile.payload-budget", name: "Payload budget", domain: "mobile", status: "engine",
      engine: "payloadBudget", inputs: "the API response shape behind one screen", output: "the response size against a mobile data budget",
      purpose: "Hold a screen's API response inside a data budget that a phone on a slow network can actually afford.",
      requiresApproval: false, receipt: "the measured payload, the budget declared, and the fields that dominate it" }),
  A({ id: "mobile.battery-profile", name: "Battery profile", domain: "mobile", status: "engine",
      engine: "powerBudget", inputs: "battery capacity, duty cycle and the active and sleep currents", output: "runtime, and which state owns the charge",
      purpose: "Tell a user how long the app lasts and which state is spending the battery, before a review calls it heavy.",
      requiresApproval: false, receipt: "the duty cycle, the currents, and the state consuming the largest share of the charge" }),
  A({ id: "mobile.push-send", name: "Push campaign", domain: "mobile", status: "workflow",
      engine: "funnel + growthModel", inputs: "a notification campaign and its audience definition", output: "the modelled effect, gated before anything sends",
      purpose: "Model a notification campaign and stop at the gate, because a push is a message delivered to somebody's phone.",
      requiresApproval: true, receipt: "the audience size, the modelled effect, and the human's approval before any message is sent" }),

  /* ── cloud ───────────────────────────────────────────────────── */
  A({ id: "cloud.instance-plan", name: "Instance sizing", domain: "cloud", status: "engine",
      engine: "instanceSizing", inputs: "p95 utilisation, a headroom target and the current fleet size", output: "instances needed and the resulting p95",
      purpose: "Size a fleet from measured p95 and a stated headroom target instead of from memory of the last incident.",
      requiresApproval: false, receipt: "the measured utilisation, the target declared, and the resulting instance count" }),
  A({ id: "cloud.egress-review", name: "Egress review", domain: "cloud", status: "engine",
      engine: "egressCost", inputs: "egress volume, origin price and cache hit rate", output: "monthly cost with and without the cache",
      purpose: "Show what a cache is worth in money, using the tariff the team actually pays rather than a published list price.",
      requiresApproval: false, receipt: "the volumes, the prices, and the saving computed per path" }),
  A({ id: "cloud.token-scope", name: "Token scope audit", domain: "cloud", status: "engine",
      engine: "jwtInspect", inputs: "a service token used by a workload", output: "its scopes, expiry and claims, decoded",
      purpose: "Read what a service token can actually do, because a scope nobody intended is a permission that outlives its reason.",
      requiresApproval: false, receipt: "the decoded claims, the expiry, and the scopes the token carries" }),
  A({ id: "cloud.cost-anomaly", name: "Cost anomaly watch", domain: "cloud", status: "engine",
      engine: "anomalyZ", inputs: "daily spend history for a service", output: "days that sit far from their own window's mean",
      purpose: "Surface the day a bill moved without an incident, while nobody was watching the graph.",
      requiresApproval: false, receipt: "the window examined, the mean and spread, and each flagged day with its z" }),
  A({ id: "cloud.spend-forecast", name: "Spend forecast", domain: "cloud", status: "engine",
      engine: "spendForecast", inputs: "months of infrastructure spend", output: "the trend and a projected month",
      purpose: "Put a trajectory on infrastructure spend so the budget conversation starts from a line rather than a feeling.",
      requiresApproval: false, receipt: "the months observed, the fitted trend, and the projection with its caveat attached" }),
  A({ id: "cloud.capacity-runway", name: "Capacity runway", domain: "cloud", status: "engine",
      engine: "capacityHeadroom", inputs: "current headroom, growth rate and a threshold", output: "how long the headroom lasts before the threshold",
      purpose: "Answer how long current capacity lasts, so a migration can be scheduled instead of demanded.",
      requiresApproval: false, receipt: "the headroom today, the growth rate assumed, and the runway before the threshold is crossed" }),
  A({ id: "cloud.error-budget", name: "Platform error budget", domain: "cloud", status: "engine",
      engine: "sloErrorBudget", inputs: "an SLO target and the measurement window", output: "the budget, what is spent and what remains",
      purpose: "Spend reliability effort against a budget instead of against the loudest recent incident.",
      requiresApproval: false, receipt: "the SLO, the window, and the error minutes allowed and consumed" }),
  A({ id: "cloud.deploy-window", name: "Deployment window", domain: "cloud", status: "engine",
      engine: "deployRisk", inputs: "a change set and the blast radius it touches", output: "a risk read for the window chosen",
      purpose: "Choose a deployment window from the change's own risk shape rather than the team's calendar preference.",
      requiresApproval: false, receipt: "the change size, the blast radius, and the factors behind the risk read" }),
  A({ id: "cloud.retention-audit", name: "Log retention audit", domain: "cloud", status: "engine",
      engine: "retentionClock", inputs: "log classes with creation dates and retention terms", output: "what is already overdue for deletion",
      purpose: "Find log classes past their retention date, which is the finding an auditor asks about first.",
      requiresApproval: false, receipt: "each class, its retention term, and the days past or remaining" }),
  A({ id: "cloud.autoscale-apply", name: "Autoscale policy apply", domain: "cloud", status: "workflow",
      engine: "instanceSizing + sloErrorBudget", inputs: "a proposed scaling policy for a production service", output: "a production scaling change, gated",
      purpose: "Change how production scales: modelled first, applied by a human, because a scaling policy is a production change.",
      requiresApproval: true, receipt: "the model, the policy difference, and the human's approval of the apply" }),
  A({ id: "cloud.rotation-run", name: "Credential rotation", domain: "cloud", status: "workflow",
      engine: "scanSecrets + stringEntropyBits", inputs: "the credentials due for rotation this cycle", output: "the rotation plan and its execution, gated",
      purpose: "Rotate credentials on schedule with the exposure window measured and the change approved before it goes out.",
      requiresApproval: true, receipt: "which credentials rotated, the exposure window, and the human's approval" }),

  /* ── database ───────────────────────────────────────────────────── */
  A({ id: "db.index-selectivity", name: "Index selectivity", domain: "db", status: "engine",
      engine: "indexSelectivity", inputs: "table row counts and how much a predicate matches", output: "whether the index will be used, and why",
      purpose: "Decide whether an index earns its write cost from key density rather than from the shape of the query alone.",
      requiresApproval: false, receipt: "the row and distinct counts, the fraction matched, and the crossover applied" }),
  A({ id: "db.pool-sizing", name: "Connection pool", domain: "db", status: "engine",
      engine: "poolSizing", inputs: "request rate, query latency and instance count", output: "pool size per instance against the server ceiling",
      purpose: "Size connection pools from Little's Law instead of from the number that happened to work last time.",
      requiresApproval: false, receipt: "the concurrency computed, the pool size derived, and the headroom left on the server" }),
  A({ id: "db.slow-query-percentiles", name: "Slow-query percentiles", domain: "db", status: "engine",
      engine: "percentiles", inputs: "query durations lifted from a log", output: "p50, p95 and p99 per query",
      purpose: "Rank slow queries at the percentiles that hurt, because an average query time is not a user experience.",
      requiresApproval: false, receipt: "the sample, the percentile method, and the readout per query" }),
  A({ id: "db.outlier-queries", name: "Outlier queries", domain: "db", status: "engine",
      engine: "outliersIqr", inputs: "query durations from one window", output: "queries beyond the Tukey fence",
      purpose: "Separate the genuinely exceptional queries from the merely slow ones, so tuning effort lands where it pays.",
      requiresApproval: false, receipt: "the quartiles, the fence, and every query outside it" }),
  A({ id: "db.migration-order", name: "Migration ordering", domain: "db", status: "engine",
      engine: "compareSemver", inputs: "migration file names in their numeric order", output: "the order they will actually apply in",
      purpose: "Check that a migration series applies in the order intended, because lexical filenames and numeric order disagree.",
      requiresApproval: false, receipt: "the sorted order, the anomalies found, and the convention applied" }),
  A({ id: "db.backup-retention", name: "Backup retention", domain: "db", status: "engine",
      engine: "retentionClock", inputs: "backup sets with creation dates and retention terms", output: "which backups are past their retention date",
      purpose: "Keep backup retention honest: neither so short that recovery is impossible nor so long that it is a liability.",
      requiresApproval: false, receipt: "each backup set, its retention term, and the days past or remaining" }),
  A({ id: "db.migration-plan", name: "Migration plan", domain: "db", status: "engine",
      engine: "deployRisk", inputs: "a proposed schema change and its blast radius", output: "a risk read before the change is written",
      purpose: "Read a schema change's risk before it is written, when the plan is still cheap to alter.",
      requiresApproval: false, receipt: "the change described, the blast radius, and the factors behind the risk read" }),
  A({ id: "db.migration-apply", name: "Migration apply", domain: "db", status: "workflow",
      engine: "deployRisk + indexSelectivity", inputs: "a migration ready against a production database", output: "the migration applied, gated",
      purpose: "Apply a schema migration: the plan reviewed, the lock window known, and a human approving the change to production.",
      requiresApproval: true, receipt: "the plan reviewed, the lock window estimated, and the human's approval of the apply" }),
  A({ id: "db.replica-lag-watch", name: "Replica lag watch", domain: "db", status: "engine",
      engine: "anomalyZ", inputs: "replication lag samples over time", output: "replicas whose lag is out of family",
      purpose: "Catch a replica falling behind before a read from it returns stale data to a customer.",
      requiresApproval: false, receipt: "the window, the mean and spread, and each replica flagged with its z" }),
  A({ id: "db.capacity-plan", name: "Storage capacity plan", domain: "db", status: "engine",
      engine: "capacityHeadroom", inputs: "current storage use, growth and the warning threshold", output: "when the threshold is reached",
      purpose: "Turn storage growth into a date, so provisioning is a scheduled task rather than a pager event.",
      requiresApproval: false, receipt: "the headroom today, the growth rate, and the date the threshold is reached" }),
  A({ id: "db.partition-plan", name: "Partition plan", domain: "db", status: "workflow",
      engine: "percentiles + indexSelectivity", inputs: "table sizes, access patterns and retention", output: "a partitioning change to production, gated",
      purpose: "Restructure a large table for access and retention, with the change gated because partitioning is a schema change.",
      requiresApproval: true, receipt: "the access pattern evidence, the partition scheme proposed, and the human's approval" }),

  /* ── embedded ───────────────────────────────────────────────────── */
  A({ id: "embedded.power-budget", name: "Power budget", domain: "embedded", status: "engine",
      engine: "powerBudget", inputs: "battery capacity, duty cycle and the active and sleep currents", output: "runtime, and which state owns the charge",
      purpose: "Know the runtime a device will achieve before the hardware is ordered, and which state is spending it.",
      requiresApproval: false, receipt: "the duty cycle, the currents measured, and the state consuming the largest share" }),
  A({ id: "embedded.schedulability", name: "Schedulability", domain: "embedded", status: "engine",
      engine: "timingSlack", inputs: "task periods and worst-case execution times", output: "utilisation against the rate-monotonic bound",
      purpose: "Prove a task set schedulable before a missed deadline proves it was not.",
      requiresApproval: false, receipt: "each task's utilisation, the bound applied, and the slack remaining" }),
  A({ id: "embedded.loop-percentiles", name: "Loop-time percentiles", domain: "embedded", status: "engine",
      engine: "percentiles", inputs: "control-loop execution times from a run", output: "p50, p95 and p99 for the loop",
      purpose: "Watch the tail of a control loop, because the deadline is missed by the worst case, not the average.",
      requiresApproval: false, receipt: "the sample, the percentile method, and the loop's tail behaviour" }),
  A({ id: "embedded.jitter-outliers", name: "Jitter outliers", domain: "embedded", status: "engine",
      engine: "outliersIqr", inputs: "interrupt-to-task latencies", output: "the latencies outside the Tukey fence",
      purpose: "Find the interrupts whose latency is genuinely out of family rather than merely above average.",
      requiresApproval: false, receipt: "the quartiles, the fence, and the offending latencies" }),
  A({ id: "embedded.retry-ladder", name: "Retry ladder", domain: "embedded", status: "engine",
      engine: "backoffSchedule", inputs: "a base delay and a retry count for a bus or link", output: "the retry schedule on the wire",
      purpose: "Fix the retry schedule for a link so a flapping bus does not turn into a burst of traffic.",
      requiresApproval: false, receipt: "the base delay, the ladder produced, and the total window it occupies" }),
  A({ id: "embedded.payload-budget", name: "Frame budget", domain: "embedded", status: "engine",
      engine: "payloadBudget", inputs: "a frame or message shape on the wire", output: "its size against the transport budget",
      purpose: "Keep a message inside the transport's frame budget, where one byte over is not slower, it is absent.",
      requiresApproval: false, receipt: "the frame size measured, the budget, and the fields that dominate it" }),
  A({ id: "embedded.secret-scan", name: "Device key scan", domain: "embedded", status: "engine",
      engine: "scanSecrets", inputs: "firmware sources and configuration files", output: "credential shapes found in them",
      purpose: "Find credentials that were compiled into firmware, where they cannot be rotated by any operation short of a recall.",
      requiresApproval: false, receipt: "each secret shape found, where it appeared, and the file it came from" }),
  A({ id: "embedded.entropy-check", name: "Key entropy", domain: "embedded", status: "engine",
      engine: "stringEntropyBits", inputs: "device keys and identifiers as generated", output: "the entropy their shape implies",
      purpose: "Check that a device key has the entropy its purpose needs before a fleet is provisioned with it.",
      requiresApproval: false, receipt: "the entropy computed, the requirement, and the key's own shape" }),
  A({ id: "embedded.fleet-growth", name: "Fleet growth model", domain: "embedded", status: "engine",
      engine: "growthModel", inputs: "a provisioning plan and its churn", output: "the fleet trajectory and its end state",
      purpose: "Model fleet growth so backend capacity and certificate lifetimes are planned rather than discovered.",
      requiresApproval: false, receipt: "the plan modelled period by period, the rates assumed, and the end state" }),
  A({ id: "embedded.firmware-release", name: "Firmware release", domain: "embedded", status: "workflow",
      engine: "appSizeBudget + timingSlack", inputs: "a firmware build and the target hardware list", output: "a release package ready to flash, gated",
      purpose: "Release firmware with the size and timing gates cleared and a human approving, because it cannot be redeployed.",
      requiresApproval: true, receipt: "the gates cleared, the hardware list, and the human's approval of the release" }),
  A({ id: "embedded.key-provision", name: "Key provisioning", domain: "embedded", status: "workflow",
      engine: "stringEntropyBits + scanSecrets", inputs: "a device identity to be provisioned at manufacture", output: "a provisioning run, gated",
      purpose: "Provision device identity with the entropy verified and the run approved, because a fleet's keys are set once.",
      requiresApproval: true, receipt: "the entropy verified, the keys provisioned, and the human's approval of the run" }),

  /* ── ml & ai ───────────────────────────────────────────────────── */
  A({ id: "ml.eval-interval", name: "Evaluation interval", domain: "ml", status: "engine",
      engine: "evalInterval", inputs: "a sample size and the correct outcomes", output: "the proportion and its confidence interval",
      purpose: "Report an evaluation with the interval around it, because a bare score invites a decision it cannot support.",
      requiresApproval: false, receipt: "the sample, the confidence level, and the interval computed" }),
  A({ id: "ml.split-audit", name: "Split audit", domain: "ml", status: "engine",
      engine: "splitAudit", inputs: "corpus size, split proportions and duplicate rows", output: "whether the split leaks",
      purpose: "Check that a train/validation/test split is honest before the score it produces is quoted anywhere.",
      requiresApproval: false, receipt: "the proportions, the duplicate count, and the grouping rule applied" }),
  A({ id: "ml.sample-size", name: "Evaluation size", domain: "ml", status: "engine",
      engine: "sampleSize", inputs: "an effect worth detecting and a baseline rate", output: "the sample needed to see it",
      purpose: "Plan an evaluation from the effect worth detecting, so a null result is informative rather than merely disappointing.",
      requiresApproval: false, receipt: "the effect, the power, and the sample size derived" }),
  A({ id: "ml.lift-readout", name: "A/B lift readout", domain: "ml", status: "engine",
      engine: "abTest", inputs: "two arms with conversions", output: "the lift and whether the sample supports it",
      purpose: "Read a model or prompt A/B honestly, with the uncertainty attached to the difference.",
      requiresApproval: false, receipt: "both arms, the statistic applied, and the interval around the lift" }),
  A({ id: "ml.prompt-budget", name: "Prompt budget", domain: "ml", status: "engine",
      engine: "payloadBudget", inputs: "system prompt and context as assembled", output: "the token cost of the context",
      purpose: "Hold the assembled context inside a token budget so cost and latency stay predictable as prompts grow.",
      requiresApproval: false, receipt: "the context measured, the budget, and the components that dominate it" }),
  A({ id: "ml.latency-percentiles", name: "Inference latency", domain: "ml", status: "engine",
      engine: "percentiles", inputs: "inference durations from production traffic", output: "p50, p95 and p99 per model",
      purpose: "Watch inference at the tail, because autoscaling reacts to the worst requests, not the average ones.",
      requiresApproval: false, receipt: "the sample, the percentile method, and the tail per model" }),
  A({ id: "ml.cost-per-token", name: "Cost per million tokens", domain: "ml", status: "engine",
      engine: "unitEconomics", inputs: "token volume, provider pricing and the revenue it serves", output: "cost per million tokens and its payback",
      purpose: "Put a unit cost on inference so a model choice is a margin decision rather than a preference.",
      requiresApproval: false, receipt: "the volume, the price used, and the unit cost derived" }),
  A({ id: "ml.drift-watch", name: "Output drift watch", domain: "ml", status: "engine",
      engine: "anomalyZ", inputs: "a daily metric for model outputs", output: "days out of family, and by how far",
      purpose: "Notice an output distribution moving while every individual response still looks plausible.",
      requiresApproval: false, receipt: "the window, the mean and spread, and each flagged day" }),
  A({ id: "ml.grounding-lint", name: "Grounding citations", domain: "ml", status: "engine",
      engine: "citationLint", inputs: "a model answer that cites sources", output: "which numeric claims carry no citation",
      purpose: "Check that a grounded answer actually cites the claims that need a source.",
      requiresApproval: false, receipt: "the citations found, the claims without one, and the pattern rules applied" }),
  A({ id: "ml.model-promote", name: "Model promotion", domain: "ml", status: "workflow",
      engine: "evalInterval + splitAudit", inputs: "a candidate model with its evaluation", output: "a promotion to serve traffic, gated",
      purpose: "Promote a model with the evaluation and its interval in front of a human, because promotion changes what customers see.",
      requiresApproval: true, receipt: "the evaluation, its interval, the split audit, and the human's approval" }),
  A({ id: "ml.training-run", name: "Training run", domain: "ml", status: "workflow",
      engine: "spendForecast + evalInterval", inputs: "a training plan with its compute estimate", output: "a training run, gated on spend",
      purpose: "Start a training run with the cost projected and approved, because compute is money spent before any result exists.",
      requiresApproval: true, receipt: "the projected spend, the evaluation plan, and the human's approval of the run" }),

  /* ── research ───────────────────────────────────────────────────── */
  A({ id: "research.citation-lint", name: "Citation lint", domain: "research", status: "engine",
      engine: "citationLint", inputs: "a draft with numeric claims", output: "the claims that carry no citation",
      purpose: "Find uncited numeric claims while the draft is still cheap to fix, rather than at review.",
      requiresApproval: false, receipt: "the citations counted, the uncited claims listed, and the pattern rules applied" }),
  A({ id: "research.rater-agreement", name: "Rater agreement", domain: "research", status: "engine",
      engine: "raterAgreement", inputs: "two raters' labels for the same items", output: "raw agreement and Cohen's kappa beside it",
      purpose: "Show how much of a coding agreement is simply chance, because raw agreement flatters every rubric.",
      requiresApproval: false, receipt: "the labels, the observed and chance agreement, and kappa" }),
  A({ id: "research.sample-size", name: "Study sample size", domain: "research", status: "engine",
      engine: "sampleSize", inputs: "an effect size worth detecting and a baseline", output: "the sample the study needs",
      purpose: "Size a study from the effect worth detecting so an inconclusive result is a finding rather than a waste.",
      requiresApproval: false, receipt: "the effect, the power chosen, and the sample derived" }),
  A({ id: "research.result-percentiles", name: "Result percentiles", domain: "research", status: "engine",
      engine: "percentiles", inputs: "measurements from an experiment", output: "p50, p95 and p99 for the distribution",
      purpose: "Report measurements as a distribution, because a mean hides the tail an experiment may actually be about.",
      requiresApproval: false, receipt: "the sample, the percentile method, and the readout" }),
  A({ id: "research.outlier-trials", name: "Outlier trials", domain: "research", status: "engine",
      engine: "outliersIqr", inputs: "trial measurements from one condition", output: "trials outside the Tukey fence",
      purpose: "Identify trials that are outliers by the measures used, and separate exclusion decisions from dislike of the result.",
      requiresApproval: false, receipt: "the quartiles, the fence, and every trial outside it" }),
  A({ id: "research.paper-readability", name: "Paper readability", domain: "research", status: "engine",
      engine: "readability", inputs: "a section of the manuscript", output: "its reading grade and the sentences that carry it",
      purpose: "Keep a paper readable to the people who will use it, not only to the reviewers who will judge it.",
      requiresApproval: false, receipt: "the text measured, the grade computed, and the formula applied" }),
  A({ id: "research.reading-time", name: "Reading time", domain: "research", status: "engine",
      engine: "readingTime", inputs: "a manuscript or abstract", output: "reading time and the sections that dominate it",
      purpose: "Know what a reviewer has actually been asked to read before assuming a careful read.",
      requiresApproval: false, receipt: "the word count, the reading speed assumed, and the time computed" }),
  A({ id: "research.section-structure", name: "Section structure", domain: "research", status: "engine",
      engine: "headingLint", inputs: "the manuscript's headings", output: "the heading structure and its faults",
      purpose: "Check that a manuscript's structure follows its own promise, because a missing methods heading is a rejection risk.",
      requiresApproval: false, receipt: "the headings found, the faults listed, and the structure rule applied" }),
  A({ id: "research.terminology-drift", name: "Terminology drift", domain: "research", status: "engine",
      engine: "terminologyDrift", inputs: "a manuscript and the glossary it declares", output: "terms used inconsistently",
      purpose: "Catch a term that changes meaning halfway through a paper, which is where reviewers lose the thread.",
      requiresApproval: false, receipt: "the glossary, the drift found, and the sections it appears in" }),
  A({ id: "research.ethics-submit", name: "Ethics submission", domain: "research", status: "workflow",
      engine: "headingLint + citationLint", inputs: "a protocol ready for an ethics committee", output: "a submission, gated",
      purpose: "Submit a protocol with its structure checked and a human approving, because an ethics submission is a commitment.",
      requiresApproval: true, receipt: "the protocol checked, the attachments verified, and the human's approval of the submission" }),
  A({ id: "research.preprint-post", name: "Preprint release", domain: "research", status: "workflow",
      engine: "citationLint + raterAgreement", inputs: "a manuscript ready for public release", output: "a public posting, gated",
      purpose: "Post a preprint with its citations checked and a human approving, because publication is irreversible in practice.",
      requiresApproval: true, receipt: "the citation check, the agreement figures attached, and the human's approval" }),

  /* ── media ───────────────────────────────────────────────────── */
  A({ id: "media.loudness-gain", name: "Loudness gain", domain: "media", status: "engine",
      engine: "loudnessGain", inputs: "measured integrated loudness and the target", output: "the gain to apply and the peak after it",
      purpose: "Reach a loudness target without discovering at the encoder that the limiter had to do the work.",
      requiresApproval: false, receipt: "the measured loudness, the gain applied, and the peak it produced" }),
  A({ id: "media.bitrate-budget", name: "Bitrate budget", domain: "media", status: "engine",
      engine: "bitrateBudget", inputs: "runtime and a size budget with an encoding ladder", output: "which rung of the ladder fits",
      purpose: "Choose the encoding rung from the size budget and the runtime, instead of from habit or from a competitor's number.",
      requiresApproval: false, receipt: "the ladder evaluated, the rung chosen, and the size it produces" }),
  A({ id: "media.segment-durations", name: "Segment durations", domain: "media", status: "engine",
      engine: "percentiles", inputs: "segment durations from a finished edit", output: "p50, p95 and p99 across the timeline",
      purpose: "See the shape of an edit, where a few long segments carry the pace of the whole piece.",
      requiresApproval: false, receipt: "the segments measured, the percentile method, and the readout" }),
  A({ id: "media.dropout-outliers", name: "Dropout outliers", domain: "media", status: "engine",
      engine: "outliersIqr", inputs: "dropped-frame counts across playback sessions", output: "sessions outside the Tukey fence",
      purpose: "Separate a systematic playback problem from the handful of sessions that were always going to be unlucky.",
      requiresApproval: false, receipt: "the quartiles, the fence, and the sessions outside it" }),
  A({ id: "media.subtitle-contrast", name: "Subtitle contrast", domain: "media", status: "engine",
      engine: "contrastRatio + wcagVerdict", inputs: "subtitle colour and the plate behind it", output: "the contrast ratio and the level it meets",
      purpose: "Check that subtitles are legible against the actual plate colour rather than against an assumption about it.",
      requiresApproval: false, receipt: "both colours, the ratio computed, and the standard applied" }),
  A({ id: "media.caption-type-scale", name: "Caption type scale", domain: "media", status: "engine",
      engine: "typeScale", inputs: "a base caption size and a ratio", output: "the caption scale in px and rem",
      purpose: "Generate the caption scale once so every title card and lower third stops being decided individually.",
      requiresApproval: false, receipt: "the base, the ratio, and every step of the scale produced" }),
  A({ id: "media.episode-metadata", name: "Episode metadata", domain: "media", status: "engine",
      engine: "metaLint", inputs: "an episode title and description", output: "what each catalogue and player will truncate",
      purpose: "Catch the truncation in an episode title where every surface cuts it at a different width.",
      requiresApproval: false, receipt: "each field measured, the limits applied, and the truncation points" }),
  A({ id: "media.subtitle-coverage", name: "Subtitle coverage", domain: "media", status: "engine",
      engine: "localeCoverage", inputs: "subtitle tracks per locale against the base", output: "which locales clear the shipping bar",
      purpose: "Decide which subtitle tracks ship together, because a missing track is a missing audience.",
      requiresApproval: false, receipt: "the base count, each locale's coverage, and the bar applied" }),
  A({ id: "media.subtitle-expansion", name: "Subtitle expansion", domain: "media", status: "engine",
      engine: "stringExpansion", inputs: "a source line and the target language", output: "the line's expected length when translated",
      purpose: "Anticipate the reading speed a translated subtitle line will demand before the track is produced.",
      requiresApproval: false, receipt: "the source line, the expansion range used, and the resulting length" }),
  A({ id: "media.rendition-publish", name: "Rendition publish", domain: "media", status: "workflow",
      engine: "bitrateBudget + loudnessGain", inputs: "a mastered asset with its ladder and loudness targets", output: "a publishing run to the CDN, gated",
      purpose: "Publish renditions with the ladder and loudness verified and a human approving, because publishing is customer-facing.",
      requiresApproval: true, receipt: "the ladder verified, the loudness measured, and the human's approval of the publish" }),
  A({ id: "media.rights-clearance", name: "Rights clearance", domain: "media", status: "workflow",
      engine: "dateTerms + retentionClock", inputs: "a piece of licensed material and its grant", output: "a clearance window, gated for the licence holder",
      purpose: "Clear licensed material with the window computed and approved, because an expired licence is a takedown.",
      requiresApproval: true, receipt: "the grant dates, the cleared window, and the human's approval of the use" }),

  /* ── finops ───────────────────────────────────────────────────── */
  A({ id: "finops.spend-forecast", name: "Spend forecast", domain: "finops", status: "engine",
      engine: "spendForecast", inputs: "months of spend for a service or the whole estate", output: "the trend and a projected month",
      purpose: "Give finance a trajectory instead of last month's number and an assurance.",
      requiresApproval: false, receipt: "the months observed, the fitted trend, and the projection with its caveat" }),
  A({ id: "finops.anomaly-watch", name: "Spend anomaly watch", domain: "finops", status: "engine",
      engine: "anomalyZ", inputs: "daily spend samples", output: "days out of family, with their z",
      purpose: "Find the day spend moved without a corresponding incident, while the cause is still cheap to find.",
      requiresApproval: false, receipt: "the window, the mean and spread, and each flagged day" }),
  A({ id: "finops.unit-cost", name: "Unit cost rollup", domain: "finops", status: "engine",
      engine: "unitEconomics", inputs: "revenue, margin, acquisition cost and churn", output: "cost per unit and the payback period",
      purpose: "Express infrastructure cost per customer or per thousand requests, which is the number engineering can act on.",
      requiresApproval: false, receipt: "the inputs, the cost per unit, and the assumptions the model rests on" }),
  A({ id: "finops.egress-cost", name: "Egress cost", domain: "finops", status: "engine",
      engine: "egressCost", inputs: "egress volume, origin price and cache behaviour", output: "cost per path and what the cache saves",
      purpose: "Show where egress money actually goes, path by path, instead of treating it as one line item.",
      requiresApproval: false, receipt: "the volumes, the prices, and the per-path computation" }),
  A({ id: "finops.cost-percentiles", name: "Cost percentiles", domain: "finops", status: "engine",
      engine: "percentiles", inputs: "per-tenant or per-request cost samples", output: "the distribution, not just the total",
      purpose: "Find the tenants and requests that carry the tail of the bill, which a total never shows.",
      requiresApproval: false, receipt: "the sample, the percentile method, and the distribution readout" }),
  A({ id: "finops.capacity-runway", name: "Capacity runway", domain: "finops", status: "engine",
      engine: "capacityHeadroom", inputs: "current use, growth and the commitment threshold", output: "when the commitment threshold is reached",
      purpose: "Time a reservation decision rather than making it under pressure when the threshold is already crossed.",
      requiresApproval: false, receipt: "the headroom, the growth rate, and the runway before the threshold" }),
  A({ id: "finops.reliability-spend", name: "Reliability spend", domain: "finops", status: "engine",
      engine: "sloErrorBudget", inputs: "an SLO and the window it is measured over", output: "the error budget and what is left of it",
      purpose: "Frame reliability work as budget spent, which is the only version of the conversation that ends in a decision.",
      requiresApproval: false, receipt: "the SLO, the window, and the budget allowed and consumed" }),
  A({ id: "finops.plan-check", name: "Plan check", domain: "finops", status: "engine",
      engine: "growthModel", inputs: "a revenue or usage plan with its churn", output: "the trajectory and the exit position",
      purpose: "Test whether a plan survives its own churn assumption before the cost model is built on top of it.",
      requiresApproval: false, receipt: "the plan modelled period by period, the rates assumed, and the exit position" }),
  A({ id: "finops.retention-audit", name: "Retention audit", domain: "finops", status: "engine",
      engine: "retentionClock", inputs: "data classes with creation dates and retention terms", output: "what may be deleted and what is overdue",
      purpose: "Find the storage that policy already says should be gone, which is the cheapest saving available.",
      requiresApproval: false, receipt: "each class, its retention term, and the days past or remaining" }),
  A({ id: "finops.budget-commit", name: "Commitment purchase", domain: "finops", status: "workflow",
      engine: "spendForecast + egressCost", inputs: "a proposed reservation against forecast usage", output: "a commitment, gated on spend",
      purpose: "Buy a commitment with the usage modelled and a human approving, because a reservation is money spent up front.",
      requiresApproval: true, receipt: "the forecast it rests on, the break-even, and the human's approval of the purchase" }),
  A({ id: "finops.showback-invoice", name: "Chargeback run", domain: "finops", status: "workflow",
      engine: "unitEconomics + percentiles", inputs: "costs allocated across teams or tenants", output: "an internal invoice run, gated before it is issued",
      purpose: "Issue internal charges with the allocation method stated and approved, because a chargeback nobody agreed to is an argument.",
      requiresApproval: true, receipt: "the allocation method, the amounts per team, and the human's approval of the run" }),

  /* ── legal ───────────────────────────────────────────────────── */
  A({ id: "legal.clause-terms", name: "Clause terms", domain: "legal", status: "engine",
      engine: "clauseLint", inputs: "a clause and the terms to count", output: "the density of undefined terms a dispute would turn on",
      purpose: "Point at the words a contract will be argued over later, while redrafting is still a text edit.",
      requiresApproval: false, receipt: "the terms counted, their occurrences, and the density per thousand words" }),
  A({ id: "legal.contract-dates", name: "Contract dates", domain: "legal", status: "engine",
      engine: "dateTerms", inputs: "an effective date, term length and notice period", output: "expiry and the last day to serve notice",
      purpose: "Put the dates a contract turns on in front of the people who have to act on them, not only the people who signed.",
      requiresApproval: false, receipt: "the effective date, the expiry, and the last day to notice" }),
  A({ id: "legal.terminology-drift", name: "Defined-term drift", domain: "legal", status: "engine",
      engine: "terminologyDrift", inputs: "a contract and the terms it defines", output: "defined terms used inconsistently",
      purpose: "Catch a defined term that wanders, which is how a clear clause becomes an ambiguous one.",
      requiresApproval: false, receipt: "the defined terms, the drift found, and where each appears" }),
  A({ id: "legal.clause-structure", name: "Clause structure", domain: "legal", status: "engine",
      engine: "headingLint", inputs: "a contract's clause headings", output: "the structure and the headings that break it",
      purpose: "Check that a contract's structure matches its own table of contents, which is where cross-references break.",
      requiresApproval: false, receipt: "the headings found, the faults listed, and the structure rule applied" }),
  A({ id: "legal.plain-language", name: "Plain language", domain: "legal", status: "engine",
      engine: "readability", inputs: "a clause or a policy", output: "its reading grade and the sentences that carry it",
      purpose: "Give a policy a reading grade, because a term nobody understands is a term nobody can follow.",
      requiresApproval: false, receipt: "the text measured, the grade computed, and the formula applied" }),
  A({ id: "legal.retention-clock", name: "Retention clock", domain: "legal", status: "engine",
      engine: "retentionClock", inputs: "document classes with creation dates and retention terms", output: "which documents may already be destroyed",
      purpose: "Run document retention as a clock rather than as a spring clean somebody schedules when they remember.",
      requiresApproval: false, receipt: "each class, its term, and the days past or remaining" }),
  A({ id: "legal.pii-in-draft", name: "Personal data in a draft", domain: "legal", status: "engine",
      engine: "piiScan", inputs: "a draft, an exhibit or a redaction candidate", output: "personal-data classes present, samples masked",
      purpose: "Find personal data in material about to be filed, and mask the sample so the finding is not itself a disclosure.",
      requiresApproval: false, receipt: "the classes present, the masked samples, and the patterns applied" }),
  A({ id: "legal.redaction-strength", name: "Redaction strength", domain: "legal", status: "engine",
      engine: "stringEntropyBits", inputs: "an identifier as redacted", output: "the entropy the redaction leaves",
      purpose: "Check that a redaction has removed enough that the identifier cannot be recovered, not merely that it looks shorter.",
      requiresApproval: false, receipt: "the redacted form, the entropy remaining, and the requirement applied" }),
  A({ id: "legal.filing-secret-scan", name: "Filing secret scan", domain: "legal", status: "engine",
      engine: "scanSecrets", inputs: "documents about to be filed or published", output: "credential shapes present in them",
      purpose: "Catch the API key in an exhibit before the exhibit is a public record.",
      requiresApproval: false, receipt: "each secret shape found, its location, and the file it came from" }),
  A({ id: "legal.regulatory-filing", name: "Regulatory filing", domain: "legal", status: "workflow",
      engine: "dateTerms + clauseLint", inputs: "a filing due to a regulator by a statutory date", output: "a filing, gated on the human",
      purpose: "File with the statutory date computed and the signatory approved, because a late filing is a penalty, not a delay.",
      requiresApproval: true, receipt: "the due date computed, the documents attached, and the human's approval of the filing" }),
  A({ id: "legal.notice-serve", name: "Serve notice", domain: "legal", status: "workflow",
      engine: "dateTerms", inputs: "a notice to be served inside a contractual window", output: "a notice served, gated",
      purpose: "Serve a notice inside a window that closes on a fixed date, with the founder or counsel approving the wording.",
      requiresApproval: true, receipt: "the window computed, the notice text served, and the human's approval" }),

  /* ── privacy ───────────────────────────────────────────────────── */
  A({ id: "privacy.pii-scan", name: "Personal-data scan", domain: "privacy", status: "engine",
      engine: "piiScan", inputs: "a body of text, a sample or an export", output: "personal-data classes present, every sample masked",
      purpose: "Establish what personal data a text contains before it is copied into a ticket, a prompt or a repo.",
      requiresApproval: false, receipt: "the classes present, the masked samples, and the patterns applied" }),
  A({ id: "privacy.retention-clock", name: "Retention clock", domain: "privacy", status: "engine",
      engine: "retentionClock", inputs: "data classes with creation dates and retention terms", output: "what is overdue for deletion",
      purpose: "Run deletion on the clock the policy promises, rather than on the quarter somebody remembers it.",
      requiresApproval: false, receipt: "each class, its term, and the days past or remaining" }),
  A({ id: "privacy.secret-scan", name: "Secret scan", domain: "privacy", status: "engine",
      engine: "scanSecrets", inputs: "repositories, logs or configuration", output: "credential shapes found in them",
      purpose: "Find credentials where they should not be, since a secret in a log is a secret with an audience.",
      requiresApproval: false, receipt: "each secret shape, its location, and the source scanned" }),
  A({ id: "privacy.identifier-entropy", name: "Identifier entropy", domain: "privacy", status: "engine",
      engine: "stringEntropyBits", inputs: "an identifier used to refer to a person", output: "the entropy its shape implies",
      purpose: "Check whether a pseudonymous identifier can be reversed by guessing, which is the difference between pseudonymous and anonymous.",
      requiresApproval: false, receipt: "the identifier shape, the entropy computed, and the requirement" }),
  A({ id: "privacy.tracker-surface", name: "Tracker surface", domain: "privacy", status: "engine",
      engine: "cspAudit", inputs: "a content security policy from a web surface", output: "what the policy permits third parties to do",
      purpose: "See what a page actually permits, because a tag manager can widen a policy nobody reopened.",
      requiresApproval: false, receipt: "the directives read, what each permits, and the findings" }),
  A({ id: "privacy.notice-structure", name: "Notice structure", domain: "privacy", status: "engine",
      engine: "headingLint", inputs: "a privacy notice's headings", output: "whether the structure carries the required sections",
      purpose: "Check that a notice is organised so a reader can find the section that applies to them.",
      requiresApproval: false, receipt: "the headings found, the sections missing, and the structure rule" }),
  A({ id: "privacy.notice-readability", name: "Notice readability", domain: "privacy", status: "engine",
      engine: "readability", inputs: "a privacy notice", output: "its reading grade",
      purpose: "Give a notice a reading grade, because informed consent that requires a law degree is neither.",
      requiresApproval: false, receipt: "the text measured, the grade computed, and the formula applied" }),
  A({ id: "privacy.access-outliers", name: "Access outliers", domain: "privacy", status: "engine",
      engine: "outliersIqr", inputs: "per-actor access counts over a window", output: "actors outside the Tukey fence",
      purpose: "Separate an actor whose access pattern is genuinely exceptional from ordinary variation in a busy system.",
      requiresApproval: false, receipt: "the quartiles, the fence, and the actors outside it" }),
  A({ id: "privacy.access-percentiles", name: "Access percentiles", domain: "privacy", status: "engine",
      engine: "percentiles", inputs: "record-access volumes by system", output: "the distribution across systems",
      purpose: "Rank systems by the tail of their access volume, where over-collection shows up first.",
      requiresApproval: false, receipt: "the sample, the percentile method, and the readout" }),
  A({ id: "privacy.dsar-respond", name: "Data-subject request", domain: "privacy", status: "workflow",
      engine: "piiScan + retentionClock", inputs: "a request from a data subject with a statutory clock", output: "a response assembled, gated",
      purpose: "Answer a data-subject request inside its statutory clock with a human approving the disclosure.",
      requiresApproval: true, receipt: "the request logged, the clock applied, the records identified, and the human's approval" }),
  A({ id: "privacy.consent-change", name: "Consent change", domain: "privacy", status: "workflow",
      engine: "readability + localeCoverage", inputs: "a change to consent wording and the locales it ships in", output: "a consent change, gated",
      purpose: "Change consent wording with the reading grade and locale coverage known and a human approving the change.",
      requiresApproval: true, receipt: "the wording diff, the locales covered, and the human's approval" }),

  /* ── people ───────────────────────────────────────────────────── */
  A({ id: "people.headcount-model", name: "Headcount model", domain: "people", status: "engine",
      engine: "headcountModel", inputs: "current headcount, hire rate and attrition", output: "the trajectory and its end state",
      purpose: "Show what a hiring plan does once attrition compounds against it, before the plan is promised to anyone.",
      requiresApproval: false, receipt: "the plan modelled month by month, the rates assumed, and the resulting headcount" }),
  A({ id: "people.band-position", name: "Band position", domain: "people", status: "engine",
      engine: "compBand", inputs: "a salary band and a proposed offer", output: "position in band and compa-ratio",
      purpose: "Place an offer in its band as both a position and a compa-ratio, which are different questions asked with one word.",
      requiresApproval: false, receipt: "the band, the offer, the position computed, and the ratio against the midpoint" }),
  A({ id: "people.hiring-funnel", name: "Hiring funnel", domain: "people", status: "engine",
      engine: "funnel", inputs: "stage counts from sourcing to offer", output: "stage conversions and the step losing the most",
      purpose: "Point recruiting effort at the stage that loses the most people, not the stage with the most candidates.",
      requiresApproval: false, receipt: "the counts, each stage's conversion, and the step identified as the constraint" }),
  A({ id: "people.jd-readability", name: "Job-description reading", domain: "people", status: "engine",
      engine: "readability", inputs: "a job description", output: "its reading grade",
      purpose: "Write a job description people can read, because a posting nobody finishes is a posting nobody answers.",
      requiresApproval: false, receipt: "the text measured, the grade computed, and the formula applied" }),
  A({ id: "people.policy-reading-time", name: "Policy reading time", domain: "people", status: "engine",
      engine: "readingTime", inputs: "a policy or handbook section", output: "how long it takes to read",
      purpose: "Know what a policy actually asks of the people who must follow it before announcing it is mandatory.",
      requiresApproval: false, receipt: "the word count, the reading speed assumed, and the time computed" }),
  A({ id: "people.criteria-drift", name: "Review-criteria drift", domain: "people", status: "engine",
      engine: "terminologyDrift", inputs: "a review rubric and the terms it defines", output: "criteria used inconsistently across sections",
      purpose: "Catch a review criterion that means something different in two places, which is how ratings stop being comparable.",
      requiresApproval: false, receipt: "the defined criteria, the drift found, and where each appears" }),
  A({ id: "people.survey-size", name: "Survey sample size", domain: "people", status: "engine",
      engine: "sampleSize", inputs: "an effect worth detecting and a baseline rate", output: "the responses a survey needs",
      purpose: "Size an engagement survey from the difference worth acting on, so a weak result is not read as a mandate.",
      requiresApproval: false, receipt: "the effect, the power chosen, and the responses derived" }),
  A({ id: "people.scorecard-agreement", name: "Scorecard agreement", domain: "people", status: "engine",
      engine: "raterAgreement", inputs: "two interviewers' scores for the same candidates", output: "agreement and kappa beside it",
      purpose: "Show how much interviewer agreement is chance, which is the number that decides whether a scorecard is a measure.",
      requiresApproval: false, receipt: "the scores compared, the chance agreement, and kappa" }),
  A({ id: "people.handbook-structure", name: "Handbook structure", domain: "people", status: "engine",
      engine: "headingLint", inputs: "a handbook's headings", output: "its structure and the sections that break it",
      purpose: "Check a handbook's structure, because a policy that cannot be found has not been communicated.",
      requiresApproval: false, receipt: "the headings found, the faults listed, and the structure rule applied" }),
  A({ id: "people.offer-send", name: "Extend an offer", domain: "people", status: "workflow",
      engine: "compBand + headcountModel", inputs: "an approved role with a band and its headcount plan", output: "an offer extended, gated",
      purpose: "Extend an offer with the band position computed and a human approving, because an offer is a commitment to a person.",
      requiresApproval: true, receipt: "the band position, the plan it fits, and the human's approval of the offer" }),
  A({ id: "people.comp-change", name: "Compensation change", domain: "people", status: "workflow",
      engine: "compBand + unitEconomics", inputs: "a proposed change for an individual or a group", output: "a pay change, gated",
      purpose: "Change compensation with the band effect and the affordability modelled and a human approving the change.",
      requiresApproval: true, receipt: "the band effect, the cost modelled, and the human's approval" }),

  /* ── revenue ───────────────────────────────────────────────────── */
  A({ id: "revenue.pipeline-coverage", name: "Pipeline coverage", domain: "revenue", status: "engine",
      engine: "pipelineCoverage", inputs: "a quota and stage-weighted pipeline", output: "weighted coverage against the quota",
      purpose: "Answer whether the quarter is covered by weighted pipeline rather than by the size of the list.",
      requiresApproval: false, receipt: "each stage, its win rate, the weighted total, and the gap" }),
  A({ id: "revenue.sla-clock", name: "SLA clock", domain: "revenue", status: "engine",
      engine: "slaClock", inputs: "tickets with priorities and response times", output: "first response against each priority target",
      purpose: "Show attainment ticket by ticket, because an average response time hides every customer who waited all day.",
      requiresApproval: false, receipt: "the tickets measured, the targets applied, and the breaches named" }),
  A({ id: "revenue.unit-economics", name: "Unit economics", domain: "revenue", status: "engine",
      engine: "unitEconomics", inputs: "revenue, margin, acquisition cost and churn", output: "lifetime value, the ratio and payback",
      purpose: "Test whether growth pays for itself before more money is spent proving the question again.",
      requiresApproval: false, receipt: "the inputs, the ratio, the payback, and the assumptions behind them" }),
  A({ id: "revenue.revenue-funnel", name: "Revenue funnel", domain: "revenue", status: "engine",
      engine: "funnel", inputs: "stage counts from lead to closed", output: "stage conversions and the largest proportional loss",
      purpose: "Find the step where revenue leaks instead of the step where marketing is loudest.",
      requiresApproval: false, receipt: "the counts, each stage's conversion, and the constraint identified" }),
  A({ id: "revenue.prioritise", name: "Deal prioritisation", domain: "revenue", status: "engine",
      engine: "riceScore", inputs: "candidate deals or plays with reach, impact, confidence and effort", output: "a ranked list with the inputs visible",
      purpose: "Rank competing plays so disagreement traces to an input somebody chose rather than to seniority.",
      requiresApproval: false, receipt: "every candidate scored, the formula used, and the inputs as supplied" }),
  A({ id: "revenue.plan-check", name: "Plan check", domain: "revenue", status: "engine",
      engine: "growthModel", inputs: "a revenue plan with its churn and expansion assumptions", output: "the trajectory and the exit position",
      purpose: "Test whether a revenue plan survives its own churn assumption before it becomes a hiring plan.",
      requiresApproval: false, receipt: "the plan modelled period by period, the rates assumed, and the exit position" }),
  A({ id: "revenue.deal-size-percentiles", name: "Deal-size percentiles", domain: "revenue", status: "engine",
      engine: "percentiles", inputs: "closed deal values over a period", output: "the distribution, not just the average deal",
      purpose: "Show a deal-size distribution, because an average deal is a customer that does not quite exist.",
      requiresApproval: false, receipt: "the sample, the percentile method, and the distribution readout" }),
  A({ id: "revenue.discount-maths", name: "Discount maths", domain: "revenue", status: "engine",
      engine: "unitEconomics", inputs: "a proposed discount and the deal economics", output: "what the discount costs over the contract",
      purpose: "Price a discount over the life of the contract rather than in the quarter it is granted.",
      requiresApproval: false, receipt: "the discount, its cost over the term, and the margin it reduces" }),
  A({ id: "revenue.pipeline-anomaly", name: "Pipeline anomaly", domain: "revenue", status: "engine",
      engine: "anomalyZ", inputs: "daily pipeline creation or stage movement", output: "days out of family",
      purpose: "Catch a pipeline that moved for a reason nobody recorded, while the reason is still findable.",
      requiresApproval: false, receipt: "the window, the mean and spread, and each flagged day" }),
  A({ id: "revenue.quote-issue", name: "Issue a quote", domain: "revenue", status: "workflow",
      engine: "unitEconomics + compBand", inputs: "a deal with its terms and approved pricing", output: "a quote issued to a customer, gated",
      purpose: "Issue a quote with the economics and the approval chain checked, because a quote is a commitment made in writing.",
      requiresApproval: true, receipt: "the economics computed, the approvals recorded, and the human's sign-off" }),
  A({ id: "revenue.discount-approve", name: "Approve a discount", domain: "revenue", status: "workflow",
      engine: "unitEconomics + pipelineCoverage", inputs: "a discount request beyond the standard band", output: "an exception approved, gated",
      purpose: "Approve a discount beyond policy with its cost modelled and the decision recorded against a name.",
      requiresApproval: true, receipt: "the discount modelled over the contract, the coverage effect, and the human's approval" }),

  /* ── marketing ───────────────────────────────────────────────────── */
  A({ id: "marketing.meta-lint", name: "Title & meta", domain: "marketing", status: "engine",
      engine: "metaLint", inputs: "a page title, description and slug", output: "what each search surface truncates",
      purpose: "Catch the truncation in a search result before a customer reads half a sentence.",
      requiresApproval: false, receipt: "each field measured, the display limit applied, and the truncation point named" }),
  A({ id: "marketing.crawl-budget", name: "Crawl budget", domain: "marketing", status: "engine",
      engine: "crawlBudget", inputs: "page count, response latency and the allowed rate", output: "crawl time and the binding constraint",
      purpose: "Say how long a crawl takes and which limit binds, before an indexing delay is mistaken for a penalty.",
      requiresApproval: false, receipt: "the page count, the rate applied, and the limiting factor identified" }),
  A({ id: "marketing.acquisition-funnel", name: "Acquisition funnel", domain: "marketing", status: "engine",
      engine: "funnel", inputs: "visitor counts from impression to conversion", output: "stage conversions and the largest loss",
      purpose: "Find the stage where acquisition actually leaks instead of the stage with the most traffic.",
      requiresApproval: false, receipt: "the counts, each stage's conversion, and the constraint identified" }),
  A({ id: "marketing.campaign-ab", name: "Campaign A/B", domain: "marketing", status: "engine",
      engine: "abTest", inputs: "two creative variants with conversions", output: "the lift and whether the sample supports it",
      purpose: "Read a creative test with its uncertainty, so a winner is chosen on evidence rather than on the highest number.",
      requiresApproval: false, receipt: "both variants, the test applied, and the interval around the difference" }),
  A({ id: "marketing.spend-forecast", name: "Paid spend forecast", domain: "marketing", status: "engine",
      engine: "spendForecast", inputs: "months of paid-media spend", output: "the trend and a projected month",
      purpose: "Give a channel a trajectory so budget conversations start from a line rather than a request.",
      requiresApproval: false, receipt: "the months observed, the fitted trend, and the projection" }),
  A({ id: "marketing.channel-anomaly", name: "Channel anomaly", domain: "marketing", status: "engine",
      engine: "anomalyZ", inputs: "daily sessions, spend or conversions", output: "days out of family",
      purpose: "Notice a channel moving while nobody changed a campaign, which is the day to look rather than next week.",
      requiresApproval: false, receipt: "the window, the mean and spread, and each flagged day" }),
  A({ id: "marketing.unit-economics", name: "CAC & payback", domain: "marketing", status: "engine",
      engine: "unitEconomics", inputs: "revenue, margin, acquisition cost and churn", output: "lifetime value, the ratio and payback",
      purpose: "Bound what a customer may cost to acquire, which is the ceiling every campaign has to live under.",
      requiresApproval: false, receipt: "the inputs, the ratio, the payback, and the assumptions behind them" }),
  A({ id: "marketing.landing-readability", name: "Landing-page reading", domain: "marketing", status: "engine",
      engine: "readability", inputs: "landing-page copy", output: "its reading grade",
      purpose: "Match the reading grade of a page to the audience it is actually written for.",
      requiresApproval: false, receipt: "the text measured, the grade computed, and the formula applied" }),
  A({ id: "marketing.locale-coverage", name: "Locale coverage", domain: "marketing", status: "engine",
      engine: "localeCoverage", inputs: "translated campaign assets per locale", output: "which locales can run together",
      purpose: "Decide which locales launch as one campaign, because a missing translation is a missing market.",
      requiresApproval: false, receipt: "the base count, each locale's coverage, and the bar applied" }),
  A({ id: "marketing.campaign-launch", name: "Launch a campaign", domain: "marketing", status: "workflow",
      engine: "unitEconomics + abTest", inputs: "a campaign ready to spend against an audience", output: "a launch, gated",
      purpose: "Launch a campaign with the economics bounded and a human approving, because a launch spends money in public.",
      requiresApproval: true, receipt: "the budget, the expected payback, and the human's approval of the launch" }),
  A({ id: "marketing.spend-commit", name: "Commit ad spend", domain: "marketing", status: "workflow",
      engine: "spendForecast + anomalyZ", inputs: "a committed spend against forecast performance", output: "a spend commitment, gated",
      purpose: "Commit spend with the forecast and its spread in front of a human, because committed media is money already spent.",
      requiresApproval: true, receipt: "the forecast it rests on, the downside modelled, and the human's approval" }),

  /* ── localisation ───────────────────────────────────────────────────── */
  A({ id: "locale.coverage", name: "Translation coverage", domain: "locale", status: "engine",
      engine: "localeCoverage", inputs: "translated keys per locale", output: "which locales clear the shipping bar",
      purpose: "Decide which locales ship from what is on disk, before a rollout discovers the gap in production.",
      requiresApproval: false, receipt: "the key count, each locale's coverage, and the bar applied" }),
  A({ id: "locale.expansion", name: "String expansion", domain: "locale", status: "engine",
      engine: "stringExpansion", inputs: "a source string and the target language", output: "the length it will take when translated",
      purpose: "Design to the length a string becomes rather than the length it was written at.",
      requiresApproval: false, receipt: "the source string, the expansion range used, and the resulting length" }),
  A({ id: "locale.heading-structure", name: "Localised structure", domain: "locale", status: "engine",
      engine: "headingLint", inputs: "headings across translated pages", output: "structure that survives translation",
      purpose: "Keep hierarchy readable after translation, where titles expand and headings wrap into the body.",
      requiresApproval: false, receipt: "the headings found, the faults listed, and the structure rule applied" }),
  A({ id: "locale.terminology-drift", name: "Glossary drift", domain: "locale", status: "engine",
      engine: "terminologyDrift", inputs: "translated content and the glossary it declares", output: "terms translated inconsistently",
      purpose: "Keep a product's vocabulary consistent across locales, which is what a glossary is for.",
      requiresApproval: false, receipt: "the glossary, the drift found, and where each appears" }),
  A({ id: "locale.translated-readability", name: "Translated readability", domain: "locale", status: "engine",
      engine: "readability", inputs: "a translated page", output: "its reading grade in the target language",
      purpose: "Check that a translation is readable in its own language rather than merely faithful to the source.",
      requiresApproval: false, receipt: "the text measured, the grade computed, and the formula applied" }),
  A({ id: "locale.script-contrast", name: "Script contrast", domain: "locale", status: "engine",
      engine: "contrastRatio + wcagVerdict", inputs: "text and background colours used with a script", output: "the ratio and the level it meets",
      purpose: "Check contrast for the script actually being rendered, where stroke weight and glyph size change legibility.",
      requiresApproval: false, receipt: "both colours, the ratio computed, and the standard applied" }),
  A({ id: "locale.script-type-scale", name: "Script type scale", domain: "locale", status: "engine",
      engine: "typeScale", inputs: "a base size and a ratio for a script", output: "the scale that script needs",
      purpose: "Give each script a scale its glyphs can carry, instead of scaling Latin metrics by eye.",
      requiresApproval: false, receipt: "the base, the ratio, and every step of the scale produced" }),
  A({ id: "locale.grid-snap", name: "Script grid", domain: "locale", status: "engine",
      engine: "snapToGrid", inputs: "spacing values used with a script", output: "values off the grid and their distance from it",
      purpose: "Keep spacing sane where line heights differ per script and layouts drift a few pixels at a time.",
      requiresApproval: false, receipt: "each value checked, off-grid values named, and the direction of correction" }),
  A({ id: "locale.review-agreement", name: "Review agreement", domain: "locale", status: "engine",
      engine: "raterAgreement", inputs: "two reviewers' verdicts on the same strings", output: "agreement and kappa beside it",
      purpose: "Show whether translation reviewers actually agree, which decides whether review is a process or an opinion.",
      requiresApproval: false, receipt: "the verdicts compared, the chance agreement, and kappa" }),
  A({ id: "locale.locale-release", name: "Locale release", domain: "locale", status: "workflow",
      engine: "localeCoverage + stringExpansion", inputs: "a locale ready to be enabled for users", output: "a locale enabled, gated",
      purpose: "Enable a locale with coverage and layout risk known and a human approving, because a locale is a promise to users.",
      requiresApproval: true, receipt: "the coverage, the expansion risks found, and the human's approval" }),
  A({ id: "locale.glossary-commit", name: "Publish a glossary", domain: "locale", status: "workflow",
      engine: "terminologyDrift + headingLint", inputs: "a glossary change that binds future translations", output: "a glossary published, gated",
      purpose: "Change the vocabulary every future translation must follow, with the diff reviewed and a human approving it.",
      requiresApproval: true, receipt: "the terms changed, the affected locales, and the human's approval of the change" }),

  /* ── supply chain ───────────────────────────────────────────────────── */
  A({ id: "supply.eoq", name: "Order quantity", domain: "supply", status: "engine",
      engine: "eoq", inputs: "annual demand, order cost and holding cost", output: "the order size that minimises both costs",
      purpose: "Set order sizes from the trade-off between ordering and holding, which is the argument that never ends otherwise.",
      requiresApproval: false, receipt: "the inputs, the quantity derived, and the cost either side of it" }),
  A({ id: "supply.safety-stock", name: "Safety stock", domain: "supply", status: "engine",
      engine: "safetyStock", inputs: "demand, its spread, lead time and a service level", output: "the buffer and the reorder point",
      purpose: "Put a price on a service level, because the last few points of availability cost more than the rest combined.",
      requiresApproval: false, receipt: "the inputs, the service level applied, and the buffer it requires" }),
  A({ id: "supply.lead-percentiles", name: "Lead-time percentiles", domain: "supply", status: "engine",
      engine: "percentiles", inputs: "supplier lead times recorded per order", output: "p50, p95 and p99 lead times",
      purpose: "Plan against the lead time that happens, not the one on the purchase order.",
      requiresApproval: false, receipt: "the sample, the percentile method, and the lead-time readout" }),
  A({ id: "supply.demand-outliers", name: "Demand outliers", domain: "supply", status: "engine",
      engine: "outliersIqr", inputs: "daily or weekly demand figures", output: "periods outside the Tukey fence",
      purpose: "Separate a genuine demand spike from the ordinary variation that would otherwise set the buffer.",
      requiresApproval: false, receipt: "the quartiles, the fence, and every period outside it" }),
  A({ id: "supply.demand-forecast", name: "Demand forecast", domain: "supply", status: "engine",
      engine: "spendForecast", inputs: "period demand for one item", output: "the trend and a projected period",
      purpose: "Give replenishment a trajectory so the order is placed before the stockout rather than after it.",
      requiresApproval: false, receipt: "the periods observed, the fitted trend, and the projection" }),
  A({ id: "supply.supplier-sla", name: "Supplier SLA clock", domain: "supply", status: "engine",
      engine: "slaClock", inputs: "orders with promised and actual dates", output: "delivery performance against the promise",
      purpose: "Measure a supplier against the promise, order by order, rather than against a quarterly impression.",
      requiresApproval: false, receipt: "the orders, the promise dates, and the performance computed" }),
  A({ id: "supply.shelf-life", name: "Shelf-life clock", domain: "supply", status: "engine",
      engine: "retentionClock", inputs: "batches with receipt dates and shelf lives", output: "what expires next and what is already past",
      purpose: "Rotate stock on the clock a batch actually carries, before expiry turns inventory into waste.",
      requiresApproval: false, receipt: "each batch, its shelf life, and the days remaining" }),
  A({ id: "supply.warehouse-headroom", name: "Warehouse headroom", domain: "supply", status: "engine",
      engine: "capacityHeadroom", inputs: "current occupancy, growth and the practical ceiling", output: "when the ceiling is reached",
      purpose: "Turn warehouse growth into a date so the next lease is negotiated before the space runs out.",
      requiresApproval: false, receipt: "the occupancy, the growth rate, and the runway to the ceiling" }),
  A({ id: "supply.network-growth", name: "Network growth check", domain: "supply", status: "engine",
      engine: "growthModel", inputs: "a supply-network plan with its churn", output: "the trajectory and its end state",
      purpose: "Test a network plan against its own attrition before it becomes a capital commitment.",
      requiresApproval: false, receipt: "the plan modelled period by period, the rates assumed, and the exit position" }),
  A({ id: "supply.po-issue", name: "Raise a purchase order", domain: "supply", status: "workflow",
      engine: "eoq + safetyStock", inputs: "a replenishment need with quantity and timing", output: "a purchase order, gated",
      purpose: "Raise a purchase order with the quantity and timing computed and a human approving, because a PO is money committed.",
      requiresApproval: true, receipt: "the quantity, the timing, the cost, and the human's approval" }),
  A({ id: "supply.supplier-commit", name: "Supplier commitment", domain: "supply", status: "workflow",
      engine: "safetyStock + slaClock", inputs: "a volume commitment to a supplier", output: "a commitment, gated",
      purpose: "Commit volume with the service-level effect and the supplier's record modelled, and a human approving.",
      requiresApproval: true, receipt: "the volume, the service-level effect, the supplier's record, and the human's approval" }),

  /* ── web3 ───────────────────────────────────────────────────── */
  A({ id: "web3.gas-plan", name: "Gas plan", domain: "web3", status: "engine",
      engine: "gasPlan", inputs: "gas units, the fee market and the price of the asset", output: "cost per operation and for the batch",
      purpose: "Put a cost on an on-chain operation before the batch is written, when the design can still change.",
      requiresApproval: false, receipt: "the units, the fee components, and the cost computed per operation" }),
  A({ id: "web3.token-decimals", name: "Token decimals", domain: "web3", status: "engine",
      engine: "tokenDecimals", inputs: "a raw base-unit amount and the token's decimals", output: "the human amount, and back again",
      purpose: "Convert base units in integer arithmetic, which is where a decimal point silently becomes a decimal error.",
      requiresApproval: false, receipt: "the raw amount, the decimals, and the round trip verified" }),
  A({ id: "web3.seed-entropy", name: "Seed entropy", domain: "web3", status: "engine",
      engine: "stringEntropyBits", inputs: "a seed phrase or key as generated", output: "the entropy its structure implies",
      purpose: "Check that a seed has the entropy it needs before it holds anything worth taking.",
      requiresApproval: false, receipt: "the entropy computed, the requirement, and the key's structure" }),
  A({ id: "web3.key-material-scan", name: "Key material scan", domain: "web3", status: "engine",
      engine: "scanSecrets", inputs: "repositories, configs and deployment scripts", output: "key material found in them",
      purpose: "Find a private key that reached a repository, because that is not a rotation, it is a compromise.",
      requiresApproval: false, receipt: "each key shape found, where it appeared, and the source scanned" }),
  A({ id: "web3.session-token", name: "Session token audit", domain: "web3", status: "engine",
      engine: "jwtInspect", inputs: "a session token issued by the app", output: "its claims, scopes and expiry, decoded",
      purpose: "Read what a session token grants, because a widened scope outlives the reason it was granted.",
      requiresApproval: false, receipt: "the decoded claims, the scopes present, and the expiry" }),
  A({ id: "web3.dapp-headers", name: "dApp header audit", domain: "web3", status: "engine",
      engine: "cspAudit", inputs: "the content security policy a dApp serves", output: "what the policy permits",
      purpose: "See what a front end allows third parties to run, because a wallet session deserves the strictest policy on the estate.",
      requiresApproval: false, receipt: "the directives read, what each permits, and the findings" }),
  A({ id: "web3.rpc-rate-plan", name: "RPC rate plan", domain: "web3", status: "engine",
      engine: "tokenBucketPlan", inputs: "read volume and the provider's rate limit", output: "a token-bucket plan that holds under it",
      purpose: "Keep RPC calls inside a provider's limit before the provider does it for you, at the worst moment.",
      requiresApproval: false, receipt: "the rate, the bucket plan, and the burst permitted" }),
  A({ id: "web3.call-payload", name: "Call payload", domain: "web3", status: "engine",
      engine: "payloadBudget", inputs: "a contract call's calldata shape", output: "its size against block and cost budgets",
      purpose: "Keep calldata inside its budget, where every byte costs gas on every single call.",
      requiresApproval: false, receipt: "the payload measured, the budget, and the fields that dominate it" }),
  A({ id: "web3.treasury-runway", name: "Treasury runway", domain: "web3", status: "engine",
      engine: "spendForecast", inputs: "months of treasury outflows", output: "the trend and a projected month",
      purpose: "Give a treasury a runway in months, which is the number that decides when a raise starts.",
      requiresApproval: false, receipt: "the months observed, the fitted trend, and the projection" }),
  A({ id: "web3.tx-sign", name: "Sign a transaction", domain: "web3", status: "workflow",
      engine: "gasPlan + tokenDecimals", inputs: "a prepared transaction with its cost", output: "a signed transaction, gated",
      purpose: "Sign a transaction with the cost and the amounts verified in integer arithmetic and a human approving the signature.",
      requiresApproval: true, receipt: "the transaction, its gas cost, the amounts verified, and the human's approval" }),
  A({ id: "web3.key-ceremony", name: "Key ceremony", domain: "web3", status: "workflow",
      engine: "stringEntropyBits + scanSecrets", inputs: "a key generation and custody procedure", output: "a ceremony executed, gated",
      purpose: "Run a key ceremony with entropy verified, custody recorded, and a human approving each step in the room.",
      requiresApproval: true, receipt: "each step witnessed, the entropy verified, and the human's approval recorded" }),

  /* ── frontend (deepened) ───────────────────────────────────────────────────── */
  A({ id: "fe.focus-targets", name: "Focus target sizes", domain: "frontend", status: "engine",
      engine: "touchTargets", inputs: "interactive elements and their rendered sizes", output: "which controls are too small to target",
      purpose: "Treat pointer and keyboard targets with the same minimums as touch, because a trackpad is not a mouse.",
      requiresApproval: false, receipt: "each element measured, the floor applied, and the spacing exception noted" }),
  A({ id: "fe.label-expansion", name: "Label expansion", domain: "frontend", status: "engine",
      engine: "stringExpansion", inputs: "a UI label and the locales it ships in", output: "the length that label becomes",
      purpose: "Size a button for its longest translation rather than for the English it was designed in.",
      requiresApproval: false, receipt: "the label, the expansion range used, and the resulting length" }),
  A({ id: "fe.release", name: "Frontend release", domain: "frontend", status: "workflow",
      engine: "snapToGrid + localeCoverage", inputs: "a front-end build with its token set and locales", output: "a release published, gated",
      purpose: "Publish a front-end release with design tokens and locale coverage checked and a human approving the publish.",
      requiresApproval: true, receipt: "the token check, the locale coverage, and the human's approval of the release" }),

  /* ── engineering (deepened) ───────────────────────────────────────────────────── */
  A({ id: "dev.retention-audit", name: "Branch retention", domain: "dev", status: "engine",
      engine: "retentionClock", inputs: "branches with last-commit dates and a retention term", output: "branches past their retention date",
      purpose: "Retire stale branches on a clock, so review surfaces do not slowly fill with abandoned work.",
      requiresApproval: false, receipt: "each branch, its age, and the days past its retention term" }),
  A({ id: "dev.dependency-window", name: "Dependency window", domain: "dev", status: "engine",
      engine: "dateTerms", inputs: "a support window and the notice a deprecation needs", output: "the last date to migrate off a dependency",
      purpose: "Plan a dependency migration against the support window rather than against the announcement.",
      requiresApproval: false, receipt: "the window dates, the migration deadline, and the days remaining" }),
  A({ id: "dev.release-tag", name: "Tag a release", domain: "dev", status: "workflow",
      engine: "nextVersion + satisfiesRange", inputs: "a merged change set and the current version", output: "a release tag, gated",
      purpose: "Tag a release with the version derived from the changes and a human approving, because a tag is a promise to consumers.",
      requiresApproval: true, receipt: "the version derived, the range check, and the human's approval of the tag" }),

  /* ── api (deepened) ───────────────────────────────────────────────────── */
  A({ id: "api.response-budget", name: "Response budget", domain: "api", status: "engine",
      engine: "payloadBudget", inputs: "an endpoint's response shape", output: "its size against the declared budget",
      purpose: "Hold an endpoint's response inside a budget so one new field does not double a mobile payload.",
      requiresApproval: false, receipt: "the shape measured, the budget, and the fields that dominate it" }),
  A({ id: "api.sla-attainment", name: "API SLA attainment", domain: "api", status: "engine",
      engine: "slaClock", inputs: "requests with priority classes and response times", output: "attainment against each class target",
      purpose: "Report API performance per priority class, because one target for every caller is a target for none.",
      requiresApproval: false, receipt: "the requests, the class targets, and the breaches named" }),
  A({ id: "api.version-retire", name: "Retire an API version", domain: "api", status: "workflow",
      engine: "satisfiesRange + dateTerms", inputs: "a version marked for retirement and its consumers", output: "a retirement, gated",
      purpose: "Retire an API version with the deprecation window computed and a human approving, because consumers are still calling it.",
      requiresApproval: true, receipt: "the consumers listed, the notice window, and the human's approval of the retirement" }),

  /* ── data (deepened) ───────────────────────────────────────────────────── */
  A({ id: "data.split-audit", name: "Dataset split audit", domain: "data", status: "engine",
      engine: "splitAudit", inputs: "a dataset's split proportions and duplicates", output: "whether the split leaks",
      purpose: "Check that a published dataset's split does not leak, because a leaked benchmark is a benchmark nobody trusts.",
      requiresApproval: false, receipt: "the proportions, the duplicates found, and the grouping rule" }),
  A({ id: "data.retention-clock", name: "Dataset retention", domain: "data", status: "engine",
      engine: "retentionClock", inputs: "datasets with creation dates and retention terms", output: "datasets past their retention date",
      purpose: "Run dataset retention as a clock so a training set does not outlive the consent it was collected under.",
      requiresApproval: false, receipt: "each dataset, its term, and the days past or remaining" }),
  A({ id: "data.publish-dataset", name: "Publish a dataset", domain: "data", status: "workflow",
      engine: "outliersIqr + retentionClock", inputs: "a dataset ready for publication", output: "a publication, gated",
      purpose: "Publish a dataset with outliers reviewed and retention checked, and a human approving what becomes public.",
      requiresApproval: true, receipt: "the outlier review, the retention check, and the human's approval of the publication" }),

  /* ── security (deepened) ───────────────────────────────────────────────────── */
  A({ id: "security.key-entropy", name: "Key entropy", domain: "security", status: "engine",
      engine: "stringEntropyBits", inputs: "keys as generated by a service", output: "the entropy their shape implies",
      purpose: "Verify that keys have the entropy their purpose requires, which is cheaper than discovering it during an incident.",
      requiresApproval: false, receipt: "the key shape, the entropy computed, and the requirement applied" }),
  A({ id: "security.access-anomaly", name: "Access anomaly", domain: "security", status: "engine",
      engine: "anomalyZ", inputs: "per-identity access counts", output: "identities whose volume is out of family",
      purpose: "Find the identity that changed behaviour this week, while the change is still explainable.",
      requiresApproval: false, receipt: "the window, the mean and spread, and each identity flagged with its z" }),
  A({ id: "security.key-ceremony", name: "Key ceremony", domain: "security", status: "workflow",
      engine: "stringEntropyBits + scanSecrets", inputs: "a key generation and custody procedure", output: "a ceremony executed, gated",
      purpose: "Run a key ceremony with each step witnessed and a human approving, because custody is only as good as its record.",
      requiresApproval: true, receipt: "each step witnessed, the entropy verified, and the human's approval recorded" }),

  /* ── reliability (deepened) ───────────────────────────────────────────────────── */
  A({ id: "ops.capacity-runway", name: "Capacity runway", domain: "ops", status: "engine",
      engine: "capacityHeadroom", inputs: "headroom, growth and the threshold", output: "how long until the threshold is reached",
      purpose: "Schedule capacity work from a date rather than from the pager that announces it.",
      requiresApproval: false, receipt: "the headroom, the growth rate, and the runway before the threshold" }),
  A({ id: "ops.incident-percentiles", name: "Incident percentiles", domain: "ops", status: "engine",
      engine: "percentiles", inputs: "incident durations by severity", output: "the distribution of duration, not the mean",
      purpose: "See how long incidents actually last at the tail, which is what an on-call rotation experiences.",
      requiresApproval: false, receipt: "the sample, the percentile method, and the duration readout" }),
  A({ id: "ops.postmortem-publish", name: "Publish a postmortem", domain: "ops", status: "workflow",
      engine: "readability + headingLint", inputs: "a postmortem ready for the wider team", output: "a publication, gated",
      purpose: "Publish a postmortem with its structure and readability checked and a human approving what goes on the record.",
      requiresApproval: true, receipt: "the structure check, the readability measure, and the human's approval" }),

  /* ── docs (deepened) ───────────────────────────────────────────────────── */
  A({ id: "docs.translation-expansion", name: "Doc expansion", domain: "docs", status: "engine",
      engine: "stringExpansion", inputs: "a documentation string and its locales", output: "the length it takes when translated",
      purpose: "Anticipate the layout damage a translation does to documentation built in English.",
      requiresApproval: false, receipt: "the string, the expansion range used, and the resulting length" }),
  A({ id: "docs.review-agreement", name: "Doc review agreement", domain: "docs", status: "engine",
      engine: "raterAgreement", inputs: "two reviewers' verdicts on the same pages", output: "agreement and kappa beside it",
      purpose: "Check whether documentation review is a process or two people with different opinions.",
      requiresApproval: false, receipt: "the verdicts compared, the chance agreement, and kappa" }),
  A({ id: "docs.publish-runbook", name: "Publish a runbook", domain: "docs", status: "workflow",
      engine: "headingLint + citationLint", inputs: "a runbook ready for the on-call rotation", output: "a publication, gated",
      purpose: "Publish a runbook with its structure and citations checked and a human approving, because somebody will follow it at 3am.",
      requiresApproval: true, receipt: "the structure check, the citations verified, and the human's approval" }),

  /* ── growth (deepened) ───────────────────────────────────────────────────── */
  A({ id: "growth.experiment-size", name: "Experiment size", domain: "growth", status: "engine",
      engine: "sampleSize", inputs: "an effect worth detecting and a baseline rate", output: "the sample the experiment needs",
      purpose: "Size an experiment from the effect worth acting on, so a null result is a finding rather than a shrug.",
      requiresApproval: false, receipt: "the effect, the power chosen, and the sample derived" }),
  A({ id: "growth.metric-anomaly", name: "Metric anomaly", domain: "growth", status: "engine",
      engine: "anomalyZ", inputs: "a daily growth metric", output: "days out of family",
      purpose: "Catch a metric that moved without a release or a campaign to explain it.",
      requiresApproval: false, receipt: "the window, the mean and spread, and each flagged day" }),
  A({ id: "growth.pricing-page", name: "Publish a pricing page", domain: "growth", status: "workflow",
      engine: "unitEconomics + metaLint", inputs: "a price change and the page presenting it", output: "a publication, gated",
      purpose: "Publish a pricing page with the economics modelled and a human approving, because a price is a promise to customers.",
      requiresApproval: true, receipt: "the economics modelled, the page checked, and the human's approval of the publication" }),
]);

export function specialistsByDomain(domain: Domain): Specialist[] {
  return SPECIALISTS.filter((s) => s.domain === domain);
}

export function findSpecialist(id: string): Specialist | undefined {
  return SPECIALISTS.find((s) => s.id === id);
}

export function specialistStatus(): {
  total: number; engine: number; workflow: number; requiringApproval: number; domains: number;
} {
  return {
    total: SPECIALISTS.length,
    engine: SPECIALISTS.filter((s) => s.status === "engine").length,
    workflow: SPECIALISTS.filter((s) => s.status === "workflow").length,
    requiringApproval: SPECIALISTS.filter((s) => s.requiresApproval).length,
    domains: new Set(SPECIALISTS.map((s) => s.domain)).size,
  };
}
