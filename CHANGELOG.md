# Changelog

Notable changes per release. Full notes in [docs/history/](docs/history/).

| Version | What changed |
|---|---|
| **18.7.0** | **"Keystone" — the A2A base gets its audit trail, and the release contract stops lying.** Goals tell the truth: `done` now requires every step executed; terminal-but-unexecuted goals settle honestly and report DONE / PARTIAL / PLANNED / BLOCKED / IN-PROGRESS with separate executed% and settled% numbers (18.6.0 review fix). New A2A handoff ledger: every peer delegation attempt — including honest refusals — is receipted with peer, reason, and the peer's receipt digest when a bridge executed; shown in the collaboration panel. VERIFY.sh no longer hardcodes version or suite counts (stale 18.4.0 header removed; count derives from the tree). Offline verifier parallelized in a bounded pool with deterministic per-suite attribution — ~2× faster, reviewer execution windows can now finish it. Protocol unchanged v0.10.7; suites 113/112; all gates green. |
| **18.6.0** | **"Polaris" — the identity release: the door becomes a receipt ledger.** The VH-19 conversation is rebuilt as a numbered log — Nº-stamped entries, timestamps, hairline dividers, outcomes as rubber stamps — instead of the chat-bubble idiom every AI tool copies. New motion grammar, all reduced-motion safe: the horizon arc draws itself on every view change, the tide bar breathes while the engine works and goes amber at a paused gate, ledger entries enter in a 40ms cascade, outcome stamps pop with a settle. Matured chrome: 30px display titles at -0.03em/650, three-stop viewport depth gradient, metadata receded to the aged tier. Clean tree: generated icon masters out of public/. Protocol unchanged v0.10.7; suites unchanged 113/112; all gates green. |
| **18.5.0** | **Apex — governed goal mode and standing gate rules: the always-on-agent category's mechanics, rebuilt on VH's floor with the cloud-credential trust model refused.** (1) **ASSIGNMENTS (goal mode)** (`src/vh19/goals.ts`, door panel, `probe/goals` 19 checks): a goal decomposes via the product's own router into ≤5 specialist steps; each step settles ONLY through the real pipeline (executed with receipt digest / honest plan without a provider / paused at the human gate / refused); checkpointed in the local store, resumable after restart; settled steps cannot be re-settled. (2) **SESSION AUTO-REVIEW RULES** (`src/vh19/gateRules.ts`): "allow this category for this session" at a risky gate ask — module-memory only (restart forgets), answers only when every category in the ask is ruled, NEVER critical, every answer logged and shown with one-click revoke; permanent autonomy still exam-earned. (3) **GEMINI STABLE v1** native endpoint default per the review note (OpenAI-compat path stays v1beta/openai/), pinned. (4) VERSION IDENTITY 18.4.0 → 18.5.0; 113 live suites / 112 offline bundles; MCP + host bundles regenerated. Unchanged: protocol v0.10.7, the 147-specialist bench, signed-collaboration envelope, bounded self-evolution, cloud-sync honesty. Gates on node v20.20.2: tsc 0, protocol 171/171, unit 20/20, theme 10/10, collabInvite 29/29, goals 19/19, selfEvolve 18/18, teamEvolve 35/35, vh19 79/79, vh19Door 28/28, fleet green, offline green, VERIFY.sh green. |
| **18.4.0** | **Zenith — hardening and depth, no new layers, exactly as the pre-seed review ordered.** (1) **IDENTITY SELF-PROOF**: `ensureIdentity` now requires the public components inside the decrypted private JWK to equal the stored public JWK — tampered metadata refuses even with the right passphrase (`probe/collabInvite` 29 checks). (2) **STRUCTURAL A2A BINDING**: the A2A client records card-verified peer keys (`a2aIdentityBridge`) after JWS verification; `requireBoundKey` resolves them, so connected peers bind without trust-on-first-use, attacker keys against A2A-bound members still refuse, manual bindings win; vh19 reads the bridge store by well-known key (self-contained pin holds). (3) **VERIFY.sh**: one-line zero-dependency verification at the archive root (the 111-suite offline pack; the script honestly states the protocol selftest is not zero-dep) — the 18.3.0 reviewer could not reproduce gates without node_modules; now a bare, offline machine can. (4) **BENCH 102 → 147**: forty-five individually specified specialists (wasm/desktop-shell/embedded/SQL/API-contract/legacy; threat-intel/mobile-appsec/enterprise-identity/malware-triage/applied-crypto/privacy; fuzzing/visual/contract/perf-regression/chaos; pipeline/ML/IaC/k8s review; streaming/warehouse/quality/vector/time-series; gitops/release/observability/cost/DR/edge; standards/lit-review/OSS-due-diligence/market-sizing; runbooks/api-docs/localization/proposals; forecasting/RCA/risk-modeling/experiment-design; motion/tokens/a11y-audit/service-design; compliance-ops/vendor-risk/support-escalation/capacity-plan) — the catalog computes 147 and refuses to pad. (5) VERSION IDENTITY 18.3.0 → 18.4.0 across every manifest and current-facing doc. Unchanged: protocol v0.10.7, MCP surface, cloud-sync honesty. Gates on node v20.20.2: tsc 0, protocol 171/171, unit 20/20, theme 10/10, collabInvite 29/29, selfEvolve 18/18, teamEvolve 35/35, vh19 79/79, vh19Door 26/26, fleet green, offline green, VERIFY.sh green. |
| **18.3.0** | **Meridian — the hardening-and-polish release the external review ordered.** (1) **ATTACKER-KEY HOLE CLOSED** (`probe/collabInvite` 23 checks): `verifyApproval` now resolves the approver's key from a BOUND identity registry (`src/vh19/collabRegistry.ts`) — an unbound member refuses, a presented key not deep-equal to the bound one refuses, the signature verifies against the bound key; accepting an invitation binds the issuer in the same human click; unbind is one click. Bonus hole caught while pinning: signed approvals over an OLD proposal digest used to verify — `approveTeamEvolution` now refuses stale digests. (2) **PRIVATE KEYS OUT OF LOCALSTORAGE** (`src/vh19/secureKeys.ts`): AES-256-GCM at rest under a PBKDF2-SHA-256 150k passphrase (never stored), decrypted session-only in memory, locked identities cannot sign, wrong passphrases refuse in words, legacy 18.2.0 plaintext blobs purged as breached. Not an OS keychain and says so; the Tauri-IPC upgrade path is documented (no unverifiable Rust shipped). (3) **MOTION** — Horizon motion system: 140–260 ms quint-ease entrances for views/messages/toasts, 100 ms button micro-press, seal-dot breathing, streaming shimmer; `prefers-reduced-motion` collapses all of it. (4) **NEW MARK** — thin sage arc over a platinum ring on near-black, regenerated across every desktop size, favicon and sidebar seal; the old icon retired. (5) **PERFORMANCE** — the CODE-SPLITTING roadmap ships: every dock except the VH-19 door is `React.lazy` (six on-demand chunks), react stays its own vendor chunk; main chunk 552 → 528 kB with the engine synchronous by decision. (6) **BROWSER CSP** injected in non-Tauri builds (desktop Tauri CSP untouched). (7) VERSION IDENTITY 18.2.0 → 18.3.0 across every manifest and current-facing doc; 112 live suites / 111 offline bundles; MCP + host bundles regenerated and re-pinned. Unchanged: protocol v0.10.7, the 20-tool MCP surface, cloud-sync honesty, the 102-specialist bench. Gates on node v20.20.2: tsc 0, protocol 171/171, unit 20/20, theme 10/10, collabInvite 23/23, selfEvolve 18/18, teamEvolve 35/35, vh19 79/79, vh19Door 26/26, full fleet green, offline pack green, vite build 9 chunks. |
| **18.2.0** | **Horizon — premium minimal UI, signed collaboration invitations, bounded recursive self-evolution.** (1) **HORIZON UI**: new token sheet from the product owner's three palettes (near-black Thamar/Lead inks, platinum mist, Echo Park sage #748785 accent), SF-first type, hairline cards, quiet motion; old patina green retired and `probe/theme` (10 checks) pins the new identity. New app icon — minimal horizon glyph, no V, no gimmick — regenerated across every desktop size incl. `.ico`/`.icns`; favicon + sidebar seal match. **Settings ships as a real page** (gear in the sidebar): live state only — memory/sync truth with a "force sync" refusal as the proof, the self-evolution floor, applied self-changes with reverts, real bench count, single-source version. (2) **SIGNED COLLABORATION INVITATIONS** (`src/vh19/collabInvite.ts`, `probe/collabInvite` 14 checks — the reviewers' biggest gap, closed): per-member ECDSA P-256 VH identities (TOFU, honestly labeled); User 1 mints an invite naming scope/risk-ceiling/duration/capabilities, signed over canonical bytes; tampering, foreign keys and garbage refuse in words; approvals are signatures by the approver's own key and `approveTeamEvolution` verifies every signed approval (member match + consistency) before adoption — Harshen's key cannot sign Qwen's consent. Door wires both directions. (3) **BOUNDED RECURSIVE SELF-EVOLUTION** (`src/vh19/selfEvolve.ts`, `probe/selfEvolve` 18 checks): tighten-only proposals minted from YOUR ledger (3 rejections of a specialist → tier-tighten; rejection clusters at a loose bar → raise routing bar; reject three proposals in a category → that category's proposals are suppressed — it evolves its own proposing), every one human-applied, receipt-linked, revert-exact; the floor (receipt protocol, human gate + 90% exam, honesty contract, NO loosenings) is unrepresentable in the store and audited by `floorIntact()`. Door gets a Self-evolution panel. (4) **THE TEAM SELF-EVOLVES AFTER CONNECTION**: `autoProposeIfReady` mints the team-evolution proposal automatically once the connected team clears the real bar (≥3 runs, ≥1 verified, ≥2 proven specialists) — visible, never silent, never twice; adoption still needs every member, now optionally SIGNED (`probe/teamEvolve` 35 checks). (5) **REVIEWER FEEDBACK**: R1's invitation lifecycle ships with zero new frameworks (SubtleCrypto only); R2's bundle concern gets a published roadmap (`docs/CODE-SPLITTING.md`) — route-level splits + vendor chunk, engine stays synchronous by decision; the 102 bench members stay framed as specialist roles/capability modules. (6) **VERSION IDENTITY** 18.1.0 → 18.2.0 across every manifest and current-facing doc; suites 110 → 112 live (mission-layer self-evolution spine keeps its own `selfEvolveMission` suite, 52 checks); offline pack, MCP + host bundles regenerated and re-pinned. Unchanged: protocol v0.10.7, the 20-tool MCP surface, cloud-sync honesty. Gates on node v20.20.2: tsc 0, protocol 171/171, unit 20/20, theme 10/10, collabInvite 14/14, selfEvolve 18/18, teamEvolve 35/35, vh19 79/79, vh19Door 26/26, full live fleet green, offline pack byte-identical. |
| **18.1.0** | **TeamEvolve — the differentiator ships: the TEAM itself learns, across users, with every member's consent.** (1) **CROSS-USER TEAM-EVOLVE** (`src/vh19/teamEvolve.ts`, `probe/teamEvolve` 31 checks): joint runs are recorded with real outcomes (`verified`/`failed`/`refused`) and the specialists that ran them; with a real bar (≥3 runs, ≥1 verified, ≥2 distinct proven specialists) VH proposes an evolved team composition — recommended specialists with rationale, source-run provenance and a computed digest. **Adoption requires EVERY member's explicit approval**: partial, duplicated, outsider and declined approvals all refuse in words — there is no code path that adopts on one signature (User 2's consent is structural, not ceremonial). The adopted config is a digest-stamped, versioned artifact that LEANS on future routing with a visible `team-evolved preference (config vN)` reason — it never injects a specialist absent from the decision — and revocation is a one-call human act. The door wires it: peer-delegated runs land in the team ledger through `askVH19({ team })`, and the Team-Evolve panel proposes, shows pending proposals, collects the local member's approval, and states plainly that peer approvals arrive via the A2A host runtime. (2) **CATEGORY-SCOPED AUTONOMY.** The 90% exam can now be scoped to one category; a pass grants autonomy for THAT category only; the human gate consults the scoped grant (`autonomyCovers`); category revocation leaves other scopes untouched; the overall grant still covers everything. Six new engine checks (`probe/vh19` now 79). (3) **THE BENCH GREW AGAIN — 62 → 102 real specialists** (frontend-state, web-perf, legacy modernization, event architecture, ML pipelines, API auth, mobile security, audit trails, adversarial testing, a11y/chaos/mutation testing, security-diff/test-quality/migration review, streaming/warehouse/governance, secrets-ops/FinOps/release/edge, user research/standards/benchmarking/pricing, runbooks/RFCs/microcopy/incident-comms, cohort/capacity/risk/funnel, inclusive/design-systems/IA/conversational/trust-UX) — each individually specified; the catalog reports its own count (102) and refuses to pad. (4) **THE STALE-COMMENT CLASS IS CLOSED.** The 18.0.1 review caught `nav.ts` saying "five docks" while six shipped; the comment is fixed AND pinned (`probe/vh19Door` now fails on any five-docks drift; 22 checks). (5) **VERSION IDENTITY** 18.0.1 → 18.1.0 ("TeamEvolve") across every manifest and current-facing doc; offline pack rebuilt to **109 bundles / 110 live suites**; MCP + host engine bundles regenerated and re-pinned. Unchanged: protocol v0.10.7, the 20-tool MCP surface, cloud-sync honesty, the Mastra-merge deferral (audit pipeline first). Gates on node v20.20.2: tsc 0, protocol 171/171, unit 20/20, vh19 79/79, teamEvolve 31/31, vh19Door 22/22, full live fleet green, offline pack byte-identical. |
| **18.0.1** | **Generalist, patch 1 — the VH-19 engine becomes the product's actual front door, closing the integration gap the 18.0.0 external reviews named ("the brain is ready, the face is not").** (1) **THE DOOR.** New `src/views/Vh19.tsx`: a full Generalist chat UI wired to the REAL `askVH19` — every response shows its routing decision (specialists, scores, reasons, routed-by, fallback notes), an honest outcome badge (`answered · planned · refused · gated-out · error · peer-delegated`), a provenance digest, and Accept/Reject buttons whose reasons land in the learning ledger. The human gate is a real modal that BLOCKS the run until decided. The 90% exam runs end to end in the UI (propose → grade with correction boxes → score → autonomy state, revoke in one click). Session provider config is memory-only and labeled as such, with the durable env contract stated on the panel. (2) **THE APP OPENS ON IT.** `App.tsx` registers the sixth dock and the default view is now `vh19`; `nav.ts`/`Sidebar` surface it; `probe/navAlign` updated with the product (six docks, Generalist first) — the pin moved WITH the product, not around it. (3) **THE BENCH GREW — 30 → 62 real specialists** across the same 10 categories (python, go, mobile, build-tooling, git-workflow, shell, parsing, appsec, supply-chain, privacy, config-hardening, load/contract/visual testing, architecture review, visualization, data quality, incident, cloud-infra, networking, market/OSS/API research, API docs, stakeholder comms, localization, forensics, estimation, experiments, data modeling, threat modeling, onboarding) — each individually specified, none padded. (4) **SPECIALIST MANAGEMENT.** Enable/disable per specialist (`setSpecialistEnabled`); the router fields only the enabled bench and `considered` counts the enabled bench; pinned by six new `probe/vh19` checks (now 73). (5) **THE REVIEWER'S GREP, ENFORCED.** New `probe/vh19Door.test.tsx` (18 checks): the shell opens on VH-19, the door imports `askVH19` from the engine, the door RENDERS via react-dom/server with the bench/exam/provider/gate/learning surfaces and the honesty copy on screen. (6) **DOCS REFRESHED.** FEATURES.md rewritten for the Generalist era (the Patina-era sheet is retired); README front-door section describes what actually ships; release-verification record carries the 18.0.1 gates. (7) **VERSION IDENTITY.** 18.0.0 → 18.0.1 across every manifest and current-facing doc; offline pack rebuilt to **108 bundles / 109 live suites** with byte pins; MCP + host engine bundles regenerated and re-pinned. Still deferred WITH REASONS: Mastra/AI-SDK source merge (audit pipeline first), operational cloud sync, MCP tool expansion (20-tool count is fleet-pinned), cross-user Team-Evolve automation (the A2A wire and receipts exist; the automated loop is the next deliberate release). Gates on node v20.20.2: tsc 0, protocol 171/171, unit 20/20, vh19 73/73, vh19Door 18/18, full live fleet green, offline pack byte-identical. |
| **18.0.0** | **Generalist — the VH-19 front door: one agent the user talks to, a Mixture-of-Experts specialist bench behind it, and the 90% autonomy exam.** New engine layer `src/vh19/` on the existing governed pipeline (GuardRail, human gate, egress guard): (1) the **specialist bench** — 30 individually specified seed specialists across 10 categories with honest risk tiers, real system prompts, computed catalog digest; the registry scales to hundreds and reports its own count, never a padded one. (2) The **MoE-style router** — deterministic keyword/capability scoring with named reasons as the probe-pinned spine; an optional LLM re-rank may REORDER candidates but never invent them (outside-set ids, bad JSON or a missing answer fall back to deterministic with the reason recorded); off-bench requests honestly match nobody. (3) The **provider seam** — OpenAI-compatible / Anthropic / Gemini wires behind one `complete()`: documented base URLs, overridable for gateways/local, env-only keys with enforced redaction, every URL through the SSRF egress guard, real timeouts, and a missing key is a typed refusal — the seam never fakes a completion. (4) **Accept/reject learning** — local-first per-user ledger of accepts/rejects/corrections with reasons; pattern briefings computed from real records; cloud vector sync opt-in and HONESTLY non-operational (`requestCloudSync` refuses in words; nothing leaves the device). (5) The **90% autonomy exam** — generated only from the user's real scenarios (empty history refused in words), agent proposes AND explains each answer, user grades, ≥90% grants autonomy with the monitor+override floor structural (cannot be stored off; revocation is a one-call human act), <90% returns to the learning loop with wrong answers flowing back as corrections; an exam is graded exactly once. (6) The **Generalist front door** — GuardRail scan → memory → route → human gate → real execution or honest non-execution → provenance-digested response; no key ⇒ planned/not-executed, no gate ⇒ risky refused, no A2A bridge ⇒ nothing sent, provider failure ⇒ reported in words. (7) **`probe/vh19` — 67 checks** pinning all of the above including the anti-cheat paths; live fleet 107 → **108 suites**, offline pack rebuilt to **107 bundles** with byte pins. (8) Deferred WITH REASONS, not silently: the Mastra/AI-SDK source merge (security-first: a vendored framework needs its own audit pipeline; the seam is shaped for the drop-in), 300+ specialists (architecture yes, padding no), operational cloud sync, MCP tool additions (the 20-tool count is fleet-pinned), and the VH-19 UI door. Version identity 17.10.7 → 18.0.0 ("Generalist") across every manifest and current-facing doc; protocol unchanged at v0.10.7. Gates on node v20.20.2: tsc 0, protocol 171/171, unit 20/20, vh19 67/67, full live fleet green, offline pack byte-identical. |
| **17.10.7** | **WarrantTeams, patch 7 — RULE 6 lands in this edition: protocol 0.10.6 → 0.10.7, and the two bounds this edition's own campaign reported as FINDINGS are now closed here.** 17.10.5 shipped the 17.10.4 Warrant core, so WarrantTeams carried RULES 3–5 but not RULE 6 while the Warrant trunk had already closed it. `protocol/` is self-contained (no app module imports it), so the back-port is a bounded protocol-only diff and `protocol/` is now byte-identical to the Warrant trunk. (1) **ROTATION MUST PROVE POSSESSION OF THE INCOMING KEY** (`protocol/src/core/vh-crypto.js`): the proof now carries a SECOND signature, by the incoming private key, over `VH-ROTATE-POP-v1 | oldFp | newFp | ts`, verified against the incoming bundle and bound to the caller's authenticated fingerprint; missing `pop`, a wrong key or a mismatched `oldFp` all fail closed as `invalid-rotation-proof`. Closes the measured offline-fingerprint squat — a joined member could bind an OFFLINE identity's public bundle as its successor and lock the real keyholder out (`fp-already-joined`); it could not sign as the victim, so this was impersonation + denial of identity, not escalation. (2) **AUTHORITY IS WITHDRAWN BY AUTHORITY** (`protocol/src/core/vh-trust.js` `_revokedActions` + `authorityFps`, `protocol/src/server/harbor.js`): a revocation against a DESIGNATED identity is honoured only from an authorised writer (the harbour root, or a fingerprint in `VH_AUTHORITIES`) — refused at submission (`policy:revocation-requires-authority`, new metrics `revocationRejected` / `rotationRejected`) and ignored at consumption, so records already in a ledger are inert. Closes the measured designated-authority denial of service, where any joined stranger could strip the operator's declared capability — one token or `*`, stickily, with re-declaring unable to restore it. Revocations of ORDINARY identities are unchanged, pinned so the rule cannot over-reach. (3) **GATE.** `protocol/test/selftest.js` **171/171** (was 152) — the nineteen RULE 6 checks; `wcarena/warrant-compromise-campaign.mjs` promotes both findings from INFO rows to SCORED attacks and reports **22/22 refused, control intact** (was 16/16 + 3 findings); `adversarial-campaign` 14/14, `v104-authority-matrix` 10/10 and `governance-attacks` 0-of-4 false-accepts are unchanged. (4) **ONE APP-SOURCE LINE, CAUGHT BY THE PROJECT'S OWN PROBE.** `src/mission/securityReview.ts` labelled the grant-authority surface "protocol v0.10.6"; `probe/docIdentity`'s protocol-version label scan failed the first re-run and named the exact line. The label now reads v0.10.7, the posture names RULE 6, and the evidence cites 171 checks and the 22/22 campaign — pinned by that suite. (5) **VERSION IDENTITY.** `VH_VERSION` 17.10.5 → 17.10.7 across every manifest (`package.json`, both lockfile roots, `Cargo.toml`, `Cargo.lock`, `tauri.conf.json`), the six operational-doc titles and bodies, `verify/BUILD-INFO.txt`, the one-pager and deck-outline identity lines; the one-pager and deck outline also carried stale suite counts (102/101 — the Warrant trunk's, never this edition's) and now read 105/104. The MCP engine bundle and the offline pack were both regenerated, so their byte pins hold. **No wire change beyond the two RULE 6 points; there is no compat mode for RULE 6 — a rotation without possession cannot be verified.** (6) **THE A2A LIVE BRIDGE — an inbound delegation now EXECUTES and is sealed, instead of returning a completion string.** Cross-harbor delegation had the whole ladder real (strict A2A v1.0 discovery, JWS card verification, sender routing, GuardRail, both human gates, tamper-evident digests, replay guards) and then ended in a template literal: `` const artifact = `${name} completed: "${task}" — executed under … governance.` `` — a claim of execution with no execution behind it, in the one product whose premise is that a claim without evidence is not a claim. `probe/harborTeams` and `probe/a2aV10` could not see it: they pinned the ladder, and the ladder was genuinely real; only the last step lied. New module `src/mission/a2aBridge.ts` runs the delegated work through the REAL `executeTeam` (real CLI spawn through injected deps, real git, and the repository's OWN test command as the verdict) and seals it as `vh-proof-receipt/2`, re-verifying its own receipt before it claims anything. `DelegationRecord` gains `execution` (measured: harness, run status, seats run/verified, measured USD, wall clock, `notRun` reasons) and `receipt`, both null whenever nothing ran. The chain is now `A2A → Warrant → GuardRail → receiver gate → REAL TeamExecutor → real git/CLI result → vh-proof-receipt/2`. (7) **THE HONESTY RULE, WHICH IS THE POINT.** The bridge has no fallback. No deps, no harness configured, no `repoRoot`, or a harness whose binary is absent → `ok: false`, `outcome: "refused"`, `artifact: null`, reason in words. A run whose own verification did not pass is `executed-failed`, never `completed`. Demos must ask explicitly (`allowUnexecuted`) and get a record whose note reads `NOT EXECUTED` with no execution and no receipt. (8) **THE GATE FORCED THE BRIDGE TO BE HONEST — twice.** A single-seat run tiers `self-verification` and the adversarial gate BLOCKS it ("an author grading its own work is not a review"), so the bridge runs a writer plus a read-only reviewer on a **different harness** and earns a real `cross-vendor` PASS. The first draft tried one seat and was correctly blocked by the product's own gate. (9) **NEW PROBE `probe/a2aBridge` (34 checks)** — real git repo, real CLI boundary, the repo's own test as verdict: the four refusal paths, a real execution whose receipt VERIFIES, a tampered event breaking the chain, the **anti-cheat** (a repo whose tests fail comes back `executed-failed` and never `completed`), the delegation record binding execution + receipt end to end, and a **regression pin** so no path can silently fabricate a completion string again. (10) **THE DOCUMENTATION LAYER, SYNCHRONISED.** An external review of the first 17.10.7 package found current-facing leftovers the enforced probes did not cover: `docs/INTEROP.md` and `docs/INFORMATION-ARCHITECTURE.md` titles still read 17.10.5, `README.md` still described the protocol as "v0.10.2 … 122/122", and `protocol/package.json`'s description still said v0.10.4 while the code declared 0.10.7. All four corrected; `probe/a2aBridge`'s regression pin and the existing `docIdentity` protocol-label scan now hold the line. The one-pager and deck outline also carried stale suite counts (102/101 — the Warrant trunk's, never this edition's) and now read 107/106. (11) **THE A2A RUNTIME IS MOUNTED — rev 3, after the second external review.** That review scored the LiveBridge 9.9 and then found the deeper gap: *"the bridge is implemented and tested, but it still isn't automatically mounted by the normal application runtime … I found no production call that actually starts the A2A server."* The grep agreed — `createA2AServer` appeared in `src/` once, at its own definition, plus two comments; every other caller was a probe. A passing end-to-end harness proves the architecture works **when a test wires it**; it does not prove the shipped application exposes it. New `src/mission/a2aRuntime.ts` → `startA2ARuntime()` is the ONE bootstrap (harbor identity → team → JWS-signed v1.0 card → delegation handler → receiver risk policy → LiveBridge → listen), `tools/vh-host.entry.ts` is the process around it, `npm run host:build` bundles + byte-pins `tools/vh-host-engine.mjs` the way the MCP engine is pinned, and `npm run host` verifies that pin before it will listen. Two supporting fixes had to exist for the mount to be honest: `nodeRunnerDeps()` (real `spawn`, real git, the repository's own test as the verdict — the app's `hostRunnerDeps` simulates in a browser) and `A2AServerOptions.port`, because the card is SIGNED for an interface URL and a listener on a different port serves a card that lies about its own interface (the runtime now throws if bound ≠ advertised). (12) **THE RECEIVER GRADES THE REQUEST ITSELF.** `DelegationPacketV10.tier` is written by the SENDING harbor and the receiver believed it, so a remote harbor could label a `git push --force` as `"safe"` and walk past the receiver's human gate — contradicting §10 in this codebase's own words ("the classification is derived from the action itself — not asserted by the agent that wants to perform it"). `receiverRiskVerdict()` re-classifies the inbound task with the receiver's own table and takes the worse of the two; it can only upgrade; every settled record carries the verdict as `receiverPolicy`, so a sender sees when its label was overruled and why. (13) **SECURE BY DEFAULT ON THE NEW DOOR.** A mounted harbor always enforces a bearer token — omit `--token` and one is minted and reported, because the card advertises `harborIdentity` and a card claiming a scheme the listener does not enforce refuses everything. Risky work is DENIED at a headless gate; `--allow-risky` exists for a supervised host that wires a real `HumanGate`. No execution deps / no harness binary / no bound repository → the harbor still mounts (it has to, to refuse politely) and every delegation is refused in words. (14) **NEW PROBE `probe/a2aRuntime` (48 checks) — the launch-time end-to-end the review asked for.** §3 runs TWO independently running VH processes: the receiver mounts and publishes its JWK; a stranger discovers the card over HTTP and verifies its signature; an unauthorized caller is refused; the sender process delegates; the receiver process runs a real TeamExecutor mission (2 seats, `cross-vendor` gate PASS, real worktree, the repo's own test as the verdict) and returns a sealed `vh-proof-receipt/2` that verifies in the sender's process **and in a third process** via `tools/verify-receipt.mjs` (exit 0 with the issuer key pinned out of band; exit 3 — integrity valid, issuer UNVERIFIED — without it). Also pinned: a doctored engine exits 2 instead of listening; the shipped bundle is byte-identical to a rebuild; a mounted harbor that cannot execute refuses in words; a repo whose tests fail is `executed-failed`, never completed; a sender's `"safe"` label on a CRITICAL task is upgraded and denied, while a sender that declares `"risky"` is never talked down. **Still not wired:** `npm run host` does not spawn `protocol/src/server/harbor.js` alongside the listener, so the receiver gate does not yet consult that harbour's grant ledger — authority on this door is JWS identity + bearer token + receiver risk policy + human gate. Codename WarrantTeams. Counts: **107 live probe suites / 106 offline bundles / 171 protocol checks / 53 A2A v1.0 checks / 34 A2A-bridge checks / 48 A2A-runtime checks**. Gates on node v20.20.2: tsc 0, 107/107 live, 106/106 offline, protocol 171/171, wcarena 5/5 batteries (22/22 · 14/14 · 10/10 · 0-of-4 · exploit refused), benchmark 6/6, unit 20/20, bridge 17/17, drill digest unchanged, vite clean (478.33 kB). |
| **17.10.5** | **WarrantTeams — the Warrant security core and the Teams execution layer become one product, and A2A stops being "v1-style": it is now the released Linux-Foundation A2A 1.0.0 wire.** An external review scored the Teams branch 9.4/10 (teams execution 9.7, governance 9.7, evolution 9.6) and concluded: *"take this Teams implementation and put it directly on top of 17.10.4's Warrant core, then upgrade the A2A card to the actual current v1.0 schema and add a genuine A2A transport."* This release does exactly that. (1) **THE MERGE.** The tree is re-based on the verified 17.10.4 Warrant release — protocol v0.10.6 RULES 3–5 (scope-bounded delegation, designated-only unbounded authority, "a capability claim is not a licence"), the honest assurance scorer, the wcarena adversarial battery (14/14 attack classes refused, authority matrix 10/10, governance attacks refused, compromise campaign 16/16), protocol selftest 152/152 — with the FINALFIX security layers re-applied on top untouched: the hardened expression sandbox (no computed member access, frozen facades), the GuardRail content gate, memory hygiene + secure approvals + workspace caps, and the SSRF egress guard. Harbor Teams is intact: A2A card signing, human-gated dual-digest cross-harbor delegation. (2) **STRICT A2A v1.0.0** (`src/mission/a2aV10.ts`, 53-check probe). The reviewer's classification was correct: the 17.6.2-era card was VH's internal v1-style contract, not the released schema. The new layer implements §4.4 exactly — NO top-level `url` or `protocolVersion` (both live in `supportedInterfaces`: url + protocolBinding + protocolVersion, first entry preferred), `securitySchemes` a MAP of string → discriminated-union scheme (exactly one of apiKey/httpAuth/oauth2/openIdConnect/mtls), JWS-format `signatures` (base64url protected header + base64url signature over canonical key-sorted card bytes, so any post-signing mutation of any field breaks verification), the §3.3.2 error codes (−32001…−32006), and the full Task state machine. `harborCardForTeamV10` emits the strict card from a live team. (3) **A GENUINE TRANSPORT** (`src/mission/a2aServer.ts` / `a2aClient.ts`). The piece the review said was missing: `GET /.well-known/agent-card.json` (RFC 8615, Cache-Control + ETag) serving the signed card; JSON-RPC `message/send`, `tasks/get`, `tasks/cancel`, `tasks/pushNotificationConfig/set·get`, and SSE `message/stream` with status/artifact/final events; push webhooks with the configured Authorization header. Governance layered on the transport itself: inbound text is GuardRail-scanned BEFORE any task state exists, replayed request fingerprints are refused inside a 30 s window, bodies capped at 1 MiB, content-type enforced, loopback bind by default, declared securitySchemes enforced through an authorize hook (no hook + declared schemes = everything refused), and every decision lands in a capped audit trail. The client egress-guards every target URL and REFUSES legacy-shape cards at discovery. (4) **THE SAME GOVERNANCE LADDER, NOW OVER THE WIRE.** `delegateViaA2A` + `makeDelegationHandler` carry cross-harbor delegation across real HTTP: strict discovery + JWS verification (unproven identities carry nothing), sender routing + GuardRail hard-refusal before transmission, sender human gate, tamper-evident packet digest, receiver re-scan on arrival, receiver routing, receiver human gate, execution, receiver digest re-verified by the sender, TTL 10 min, each side's own replay registry settling a delegation exactly once. Every crossing publishes to the inter-agent bus. Safe tier stays autonomous-both-sides-but-audited; risky tier still requires BOTH humans. (5) **DOCUMENTED AS A SECURITY SURFACE.** securityReview gains two entries: the GuardRail content gate and the A2A v1.0 transport boundary, with their evidence. Counts: **105 live probe suites / 104 offline bundles / 152 protocol checks / 53 A2A v1.0 checks**. Gates on node v20.20.2: tsc 0, 105/105 live, 104/104 offline, benchmark green, protocol selftest 152/152, wcarena 3/3 batteries, MCP live 20 tools, vite clean. Codename WarrantTeams. |
| **17.10.4** | **Warrant, patch 4 — RULE 5: a capability claim is not a licence. The last self-attestation hole, closed at the root.** An external review of 17.10.3 scored it 9.4/10 and declined to call Warrant solved: RULE 4 had fixed the *wildcard* case, but for **named** tokens "holding" was still established by a **self-signed capability declaration** — so any participant could join, `declareCapability(["write:payroll"])` (no `*`, no forgery, no key compromise, no collusion) and mint a real payroll grant to an accomplice. Reproduced from the trust code alone: `heldTokens()` → `canDelegate()` → `{ ok: true, reason: "attested-granter:holds-action" }`. The release that closed *unbounded* self-authority had left the *named* case standing. (1) **THE FIX — PROTOCOL v0.10.6, RULE 5.** Delegable authority now has exactly **two** sources: a **live grant** naming the fingerprint as subject (revocation- and expiry-aware), or a declaration by an **operator-authorized identity** — the harbour root key, or a fingerprint named in `VH_AUTHORITIES` (the v0.10.5 name `VH_WILDCARD_AUTHORITIES` is still read). Everything else is a **claim**: descriptive — reputation, capability reporting, audit, the UI — and never a licence. `canDelegate()` derives from the delegable set and names the rule that refused (`capability-claim-is-not-authority`, or `grantor-holds-nothing`; `*`-class tokens keep RULE 4's `wildcard-authority-not-designated`). No new subsystem, no new wire type, no loosened gate. (2) **DESIGNATION FOLLOWS THE IDENTITY, NOT THE KEY.** Rotation is the routine hygiene action; if it silently voided designation, operators would stop rotating. The harbour records a rotation only after verifying a proof signed by the **outgoing** key, and `rotate` is not a member-submittable kind (`VOUCH_KINDS`) — so the lineage in the ledger cannot be forged from a message — and a designated identity's successor inherits the designation, transitively. **Grants and revocations stay keyed to the exact fingerprint**, deliberately: if grants followed lineage, a revoked key could rotate once and walk out of its own revocation. Both halves are pinned by tests. The SDK gains an identity-injection seam (`join(name, group, { identity })`) so an operator can designate a fingerprint it generated out-of-band; possession of the private key is the identity, and designation still grants nothing by itself. (3) **THE REVIEWER'S FIVE CASES, PINNED.** self-`write:payroll` ⇒ **REFUSED** · self-`admin:finance` ⇒ **REFUSED** · root-authorized `write:payroll` ⇒ **ACCEPTED** · valid parent ⇒ narrower child **ACCEPTED** · valid parent ⇒ wider child **REFUSED** (`grantor-out-of-scope`). (4) **THE SHIPPED HARNESSES WERE RE-BASED ON GIVEN AUTHORITY — because RULE 5 changed what "legitimate" means.** Attaching to a harbour you started cannot work any more: an operator must be designated at BOOT, and without an operator the legitimate control is unprovable. So `wcarena/adversarial-campaign.mjs` grew from 11 to **14 attack classes** (the two new ones are the reviewer's named cases: self-declared `write:payroll` and `admin:finance`) and now starts its own harbour, designates an operator, hands every honest actor real grants and reports **14/14 refused with the control intact**; `wcarena/v104-authority-matrix.mjs` (**10/10**) and `wcarena/governance-attacks.mjs` (control permitted, 4/4 refused) were re-based the same way through a shared `wcarena/_harbor.mjs`. Running them surfaced a second-order trap worth naming: an identity the operator had **designated** was still refused as `grantor-unattested`, because RULE 1 asked for a self-declared capability and designation was not in that list — authority the operator believes it delegated but cannot use. Fixed at the root: **designation is attestation** (the operator's own statement about who an identity is), pinned in the suite. (5) **GATE.** `protocol/test/selftest.js` **152/152** (was 140) — twelve new checks, including the named-claim refusals, the positive "the same token GIVEN is delegable" path, the lineage clause (a proven rotation carries designation; an unrecorded successor carries nothing; a rotation carries **no grants**), and the pin that lineage records are never member-submittable. The suite now reports the failing response on a red check instead of a bare ❌. (6) **UPGRADE IMPACT, STATED.** An identity that used to self-declare a token and grant it now needs designation or a grant. Bootstrap for a real organisation: name the organisation's key in `VH_AUTHORITIES`, let it declare its scope tokens and hand grants to members, who sub-delegate inside them. A fresh harbour with no designation hands out nothing it was not told to. `VH_GRANT_POLICY=compat` restores the old semantics for migration only. (7) **THE ATTACKER-GRADE COMPROMISE CAMPAIGN — the pass the reviewer asked for next, now shipped with the release it tests.** `wcarena/warrant-compromise-campaign.mjs` assumes the attacker is already *somebody* — a joined member, a key thief, someone who has captured traffic, an identity with **real authority in another harbour** — and attacks the six fronts RULE 5 left standing: designated-authority compromise, key rotation, delegation, revocation race, replay, cross-harbour propagation. Two harbours, real grants, real ledger: **16/16 attacks refused, control intact.** Rotation cannot manufacture designation (rotate your own key, then try to delegate a claim ⇒ `capability-claim-is-not-authority`); **grants are keyed to the exact key**, so a rotated identity inherits nothing (⇒ `no-parent-grant`); a chain cannot name a root it holds nothing from (⇒ `no-parent-grant`) and the depth cap terminates it (depth 3 ⇒ `delegation-depth-exceeded`); designation is a role, not a blanket (undeclared token ⇒ `grantor-out-of-scope`); withdrawing the issuer's grant kills sub-grants minted **before** it — the sub's own grant is untouched and unexpired and still refuses at consumption (⇒ `no-authorization`); captured traffic is refused verbatim (`replayed-envelope`), stale (`stale-envelope`) and edited (`bad-signature`); and **nothing travels between harbours** — real authority in A is a claim in B, A's operator is an ordinary member in B, and A's sealed statements fail B's verification. The positive cases are pinned too: designation **follows** a proven rotation (`grantor-holds-nothing` before the successor re-declares its own scope, then a real grant after) — rotation carries a *role*, never operating scope. **Three measured bounds are reported rather than hidden**, and are the candidate work for the next rule, not defects swept under: (a) **rotation proofs never prove possession of the NEW key** — the harbour verifies continuity against the caller's current key and then adopts the bundle the caller names, so a member can bind an **offline** identity's public bundle as its successor (the duplicate-fingerprint check only covers identities currently connected): the squatter shows up as the designated fingerprint and the real keyholder is locked out (`fp-already-joined`), but it cannot sign as it (`bad-signature`) and therefore **gains no authority** — impersonation + denial of identity, not escalation; (b) **any member's revocation record strips a designated identity's declared capability** — one token, or `"*"` for all of them — and **re-declaring does not restore it**, because the revocation lives in the ledger and is keyed to the fingerprint; no authority is gained, but it is a denial-of-authority vector against the operator; (c) the shape of a chain: hops only deepen from the root (a sub-delegation must name a root that granted the granter directly), which is conservative by construction. Candidate RULE 6, **not implemented**: require proof-of-possession of the new key in `key:rotate` (the joining handshake already has exactly this challenge pattern), and treat revocations against a **designated** fingerprint as operator-only. Reported to the reviewer; no wire change made in this release. Codename Warrant; identity consolidated at 17.10.4. Counts: **102 live probe suites / 101 offline bundles / 152 protocol checks**. |
| **17.10.3** | **Warrant, patch 3 — the adversarial campaign, and the amplification it found one level up.** The 17.10.1 reviewer's closing recommendation was to stop adding security subsystems and try to break Warrant. This release does exactly that, in public, with a harness that ships: **`protocol/wcarena/adversarial-campaign.mjs` (NEW)** — 11 attack classes against a live harbour plus a legitimate control, and the source of the release's headline finding. (1) **THE FINDING — SELF-DECLARED WILDCARD AUTHORITY (protocol v0.10.5, RULE 4).** RULE 3 (v0.10.4) enforced *a granter may only hand out what it HOLDS*. But "holding" was still established by a **self-signed capability declaration** — so any participant could join, `declareCapability(["*"])` (or `["delegate:*"]`), and mint unbounded authority to an accomplice **in two messages**. Reproduced against the running server before the fix: the harbour's own ledger shows the stranger's `authorization` **accepted** (seq 9) and the accomplice's `agent_action` **committed** (seq 10) — the exact amplification class 17.10 claimed closed, left standing one level up. **RULE 4:** a `*`-class token (`*`, `delegate:*`, `admin:*`) counts **only when the harbour DESIGNATED its holder** — the harbour root key automatically, plus any operator-named fingerprints (`VH_WILDCARD_AUTHORITIES=<fp>,<fp>`) — refused otherwise as `policy:wildcard-authority-not-designated` with the claim recorded in the audit line's `held` array. Unbounded authority is **never transitive** (a `*` grant cannot be passed on by a non-designated holder), and the blanket parent-`*` shortcut on the delegated path is now gated the same way. Named capabilities are untouched, so the ordinary "I hold `write:offers`, I may delegate `write:offers`" path is unchanged. `VH_GRANT_POLICY=compat` restores the old semantics for migration only. Enforced at **issuance and consumption** (coverage re-check threads the designation list, as do `assessActionRisk` / `authorization:find`). (2) **THE CAMPAIGN RESULT — 11/11 REFUSED, CONTROL INTACT.** Case by case: self-declared `*` ⇒ refused · self-declared `delegate:*` ⇒ refused · **revocation race** (grant outlives its issuer's capability) ⇒ refused at consumption · **expiry** ⇒ refused · action-name smuggling (prefix `write:offers_v2`, case `WRITE:OFFERS`, whitespace) ⇒ refused · scope smuggling (`procurement-evil`, `procurement:evil`) ⇒ refused · action substitution under a real grant ⇒ refused — while the legitimate grant → commit control still lands. (3) **GATE.** `protocol/test/selftest.js` **140/140** (was 134) with six new RULE 4 checks: the live default-deny path twice, and — so the rule is not merely "wildcards never work" — the unit-level proof that **designation makes the same claim usable**, that unbounded authority is not transitive, and that named capabilities are unaffected. (4) **CARRIED IN FROM THE SECOND REVIEW (the fixes earmarked for a 17.10.2 build; no separate 17.10.2 was cut).** The security artifact's protocol label, the **red B3 benchmark** (it pinned the selftest's summary string, so it reported FAILED wherever protocol deps were installed — it now reads the count from the selftest itself), `protocol/README.md`'s title and check count, the product README's, the governance-attacks tally (`0 of 4`, not `0 of 3`), the new **protocol-version-label gate** in `probe/docIdentity`, and **`protocol/README-TEST.md`** (the archive could not run its own protocol gate; now it is a documented four-command procedure). Codename Warrant; identity consolidated at 17.10.3. Counts: **102 live probe suites / 101 offline bundles / 140 protocol checks**. |
| **17.10.1** | **Warrant, patch 1 — the second source of truth. An external review of 17.10.0 found the release notes were wrong about their own headline fix, and it was right.** 17.10.0 fixed `harborRerate()` (which returned `60 + seats * 3`) and shipped a real evidence-based scorer — but a **SECOND fabricated formula survived in `src/app/harbor.tsx`**: `assuranceScore: Math.min(96, 60 + s.receipts.length * 2 + s.skills.length * 3)`, rendered by TWO live surfaces (Harbor "Safe harbor", Register "Assurance score"). The authoritative scorer was fixed; a parallel presentation path still invented a number, and the new module's tests could never have seen it because the defect was never in that module. (1) **ONE METRIC, ONE SOURCE** — `HarborTotals.assuranceScore: number` is gone, replaced by `assurance: RerateReport` computed once from `harborRerate()`; a new `assuranceKpi(report)` in the bridge is the ONLY way to turn a report into something a surface can render, re-exported through `app/harbor` so views keep the house rule (the view never reaches the engine). Both KPI cards now render `—` with the scorer's own note when the fleet is `unevaluated`, instead of a confident number. (2) **THE MISS MADE MECHANICAL** — new suite `probe/scoreSourceOfTruth` (11 checks) pins the CONCEPT, not the function: a numeric assurance may be produced in exactly ONE place; no surface may synthesise one from literals (three independent detection shapes); `scoreAssurance` and `assuranceKpi` each have exactly one definition; every live-state KPI renders through the shared formatter; pure panels that receive an `AssuranceScore` as a prop are a sanctioned shape and are checked as such. **Proven by regression:** reintroducing the exact 17.10.0 defect makes the suite fail on three shapes; restoring the fix makes it pass. The lesson is in the file header — *when you fix a concept, grep the whole live runtime for every other implementation of that concept; that grep belongs in the gate, not in a reviewer's memory.* (3) **REVIEW ITEMS CLOSED** — `protocol/THREAT-MODEL.md` header corrected to v0.10.4 (the body already carried the Rule 3 material under a 0.10.3 header). The upgrade doc's own claim that "the native installers in `verify/` have not been regenerated" was **inaccurate**: this tree ships no installer binaries at all — `verify/` holds the offline pack (BUILD-INFO, MANIFEST, run.mjs, suites/); installers are `tauri:build` products and none was produced or verified here. Corrected rather than left standing. (4) **SECOND REVIEW (9.85/10) — THE LABEL DRIFT, AND THE BENCHMARK THAT WAS RED.** An external review of 17.10.1 found `src/mission/securityReview.ts` still labelling the grant-authority surface **protocol v0.10.3** while the implementation and the threat model were both v0.10.4. The sweep that followed found the same class in four more places — one of them a **live failure**: `benchmark/run.mjs` pinned the selftest summary string `ALL 129 UNIFIED-SENTINEL CHECKS PASSED`, so B3 (the grant-authority gate) reported **FAILED** on any machine with protocol dependencies installed — `node benchmark/run.mjs` printed `❌ BENCHMARK: 5 passed, 1 failed` in the shipped tree. Fixed at the root: B3 now READS the count from the selftest's own summary and never restates it, and reports the real 134/134. `protocol/README.md` opened as v0.10.2 with "122 checks"; the product README said 129 checks. The security artifact's grant-authority posture now states the v0.10.4 coverage rule itself (an attested granter may only delegate authority it HOLDS; `*` requires `*`-class authority; sub-delegation may narrow, never widen; coverage is re-checked at consumption; replayed grants are refused) and points at `protocol/wcarena/v104-authority-matrix.mjs` (9/9) and `wcarena/governance-attacks.mjs`. **AND THE CLASS IS MECHANICAL:** `probe/docIdentity` gains a protocol-version-label gate — `protocol/package.json` and `vh-crypto.js` must agree on the version, every current-facing `protocol vX.Y.Z` label (README, `protocol/README.md`, `THREAT-MODEL.md`, all of `src/`) must name that same version, and the scan self-checks that its pattern still matches something. Historical records (`CHANGELOG.md`, `*-UPGRADE.md`, `docs/history/`) are out of scope by design. **REVIEWER'S SECOND ITEM CLOSED:** new `protocol/README-TEST.md` gives the exact from-a-bare-archive procedure — `cd protocol && npm ci`, then `npm test` (134/134), the zero-install bridge gate (17), and the three governance harnesses against a running harbor with expected outputs and refusal reasons — so the missing `socket.io-client` is answered as a dependency step, never as a protocol failure. `protocol/wcarena/governance-attacks.mjs` now derives its tally from the results it actually has (the line read `0 of 3 tested bypasses` while four attacks ran; it now reads `0 of 4`). Codename Warrant; identity consolidated at 17.10.1. Counts: **102 live probe suites / 101 offline bundles**. |
| **17.10.0** | **Warrant — the authority release. A real privilege-amplification defect in the protocol's grant gate, found by running the WeClawArena governance benchmark against VH, fixed at the root and pinned.** (1) **PROTOCOL v0.10.4 — RULE 3, SCOPE-BOUNDED DELEGATION (security fix).** Since 17.6.1 `verifyGrantAuthority()` answered *may this identity delegate?* (attested granter) but never *may this identity delegate **this**?* Attestation was therefore read as authority — but a capability declaration is a **self-signed claim**, not a grant. Consequence: **any identity on a harbor could mint authority for any action**, including actions it had no capability to perform. Reproduced against a live harbor with a `read:public`-only identity: it granted a victim `write:purchase_orders` (ledger seq 17), the victim committed under it (seq 18), and it granted `"*"` (seq 19) — no forgery, no key compromise, no collusion, single actor. A granter must now HOLD what it grants: the exact action (or `*`) via capability or live grant, or a scope token `delegate:<scope>` / `admin:<scope>` (or their `*` forms); `"*"` requires `*`-class authority and a narrow capability can never be widened; sub-delegation may **narrow, never widen**. Deny by default — `policy:grantor-out-of-scope`. **DEFENCE IN DEPTH:** `findAuthorization()` re-checks issuer coverage at **consumption**, so pre-upgrade grants whose issuer has since narrowed or been revoked stop working. Posture `VH_GRANT_POLICY` = `strict` (default) / `compat` (v0.10.3 semantics, migration only — reopens the defect) / `off` (fixtures only); refusals counted in `metrics.grantAuthorityRejected` and audited with action, scope and the tokens actually held. Endorsement supplies STANDING, capability supplies SCOPE — both now required. `protocol/THREAT-MODEL.md` gains Decision 3 and its "colluding endorsers" mitigation row is corrected (no collusion was ever required). (2) **THE FABRICATED ASSURANCE NUMBER.** `harborRerate()` returned `60 + seats * 3` — assurance rose every time you added an agent, whether or not any work was ever verified — while its docstring claimed to walk "sealed receipts + learned skills", both hardcoded to `0`. It bypassed `scoreAssurance()` entirely. It now gathers real evidence from the mission-loop ledger (sealed receipts, cross-vendor verification, arena verdicts, budget adherence, human ratings) and delegates to the real scorer; **with no measured runs the score is `unevaluated` and the number is `null`**. Breaking change: `assurance` is `number | null` and the report carries `status`/`band`/`factors`/`measured`/`note`. `src/app/harbor.tsx` reports the unevaluated case in words. (3) **PROOF, NOT ASSERTION** — `protocol/wcarena/v104-authority-matrix.mjs` (NEW): **9/9** — three legitimate delegation paths accepted end-to-end, six amplification routes refused, including "no authority leaks from a refused grant". `protocol/test/selftest.js` **134/134** (was 129) with a dedicated regression for the amplification chain. Probes **101/101**, offline pack **100/100**. (4) **TESTS THAT ENCODED THE DEFECTS, CORRECTED** — `selftest.js` asserted "declare an unrelated capability, then grant anything" (the vulnerability, written as expected behaviour); `probe/buttonActions` asserted that mustering a hand RAISES assurance. Both now assert the honest contract. `probe/versionDrift` no longer hardcodes the codename "Patina" (it derives from `VH_CODENAME`) — the drift trap it exists to catch, aimed at itself. (5) **TOOLING** — `tools/run-one-probe.mjs` (NEW): run a single probe suite with the same bundling as the full runner. Codename **Warrant**; identity consolidated at 17.10.0 across every manifest, BUILD-INFO, MANIFEST, docs, one-pager, deck, mcp-engine (re-pinned) and interop CLI. Counts: **101 live probe suites / 100 offline bundles**. |
| **17.6.2** | **Patina, patch 2 — the external-validation release. The 17.6.1 verdict's directive, shipped verbatim: interop, external agents, security review, benchmark — no giant features.** (1) **TWO-MACHINE INTEROP PROVEN FOR REAL** — `probe/interop.test.ts`: machine A (TypeScript runtime) seals + anchors a real receipt and exports three files; machine B is a SPAWNED `node tools/vh-interop.mjs` process (separate process, separate rulebook copy, zero shared state) that verifies receipt + envelope, attributes the proof to A's signer, and refuses tampering and replay — the cross-implementation pin being the point. (2) **THE EXTERNAL-AGENT BOUNDARY** — `tools/vh-interop.mjs` (identity / anchor / verify-receipt / verify-envelope / transport-pack): zero npm dependencies, same rulebook as the live product, refusals in words + exit 1, transport packs carry proof and identity only — never content. Contract: `docs/INTEROP.md`. (3) **REPLAY PROTECTION** — anchor envelopes are one-time evidence: `verifyAnchorEnvelope` gains a `ReplayLedger` seam (`memoryReplayLedger`, bounded 4096); the CLI gains `--seen <ledger.json>`; second presentations refused as `replayed-envelope` (probe `crossHarbor` now 9 checks). (4) **SECURITY REVIEW ENLARGED** — `vh-security-review/1` inventories cross-harbor anchoring, grant-authority delegation and the external-agent boundary with posture + evidence, and states the new limits honestly (identity-at-rest, per-harbor replay scope, process-isolated transport). (5) **REPRODUCIBLE BENCHMARK PACK** — `benchmark/run.mjs`: zero-install, fixed-seed pinned inputs (BASELINE.json digest), receipt-verify throughput 500×, ECDSA round-trips 200×, and the protocol 129-check gate as dependency-backed B3 — HONESTLY SKIPPED without `protocol/node_modules`; 6/6 with deps. (6) **THE DOC FIX THE REVIEW NAMED** — release docs now keep the two zero-install notions strictly separate (bridge/CLI/benchmark/pack = genuinely zero-install; protocol cryptographic self-test = dependency-backed, lock-pinned); README carries the table. Identity consolidated at 17.6.2; mcp-engine re-pinned. Counts: **101 live probe suites / 100 offline bundles**. |
| **17.6.1** | **Patina, patch 1 — the integration is now real: receipt proves → protocol vouches, inside the live product.** (1) **NEW `src/vouch/engine/crossHarbor.ts`** — the Patina ⇄ Protocol runtime join: sealed `vh-proof-receipt/2` receipts verify under the ONE rulebook (`verifyProofReceipt`), then anchor into the vouch chain as signer-bound `anchor_receipt` facts whose evidence binds the verified chain head + issuer fingerprint; envelopes are wire-compatible with protocol `sealSecure` (`{p,n,ts,sig}` over `${p}|${n}|${ts}`, ECDSA P-256); identity rides the KV-seam doctrine with sign-then-verify self-test and refusals in words. Register view: every receipt row gains an **Anchor** button. Probe `crossHarbor` 7/7. (2) **Protocol v0.10.3 — the grant-authority rule**: a granter must be attested (unrevoked capability declaration or live vouch) else `policy:grantor-unattested`; delegated grants carry `authority:{root,depth}` with attested root + live parent grant, depth ≤ `maxDelegationDepth` (default 2, env `VH_MAX_DELEGATION_DEPTH`, range 0–8); capability revocations strip attestation live. Full rationale in `protocol/THREAT-MODEL.md`. Gate **129/129** (+8 authority checks). (3) **The bridge gate is honestly zero-install** — `vh-trust.js` no longer top-level-imports `sealSecure` (lazy dynamic import); bridge selftest **17/17 with zero protocol dependencies**, verified on a bare checkout. (4) **Hygiene** — `.github/workflows` restored to the pack; `protocol/package-lock.json` regenerated and shipped; `tools/mcp-engine.mjs` version literals at 17.6.1 with sha re-pinned; identity consolidated at 17.6.1 across manifests, all offline bundles, BUILD-INFO, MANIFEST, docs, one-pager and deck; `web-build/` regenerated. Counts: **100 live probe suites / 99 offline bundles**. |
| **17.6.0** | **Patina — the communication release. The fixed Vouch Harbor Protocol v0.10.2 ships in-tree at `protocol/`** — device-to-device trust substrate: signed vouching, policy + BindingValidator, authorization/reputation/revocation for humans and agents, root-key checkpoints, VH-HYBRID-PQ3 true hybrid post-quantum content transfer. v0.10.2 is the verified-fix release of v0.10.1: nine shipped defects found by executing the suite fixed and re-pinned (incl. the `"constructor" in obj` prototype-chain guard that silently disabled the trust layer, and the ArrayBuffer returns that made the hybrid combiner throw on every call) — gate 122/122. **NEW `protocol/bridge/vouch-receipt-bridge.mjs`**: verifies `vh-proof-receipt/2` receipts byte-identically to `tools/verify-receipt.mjs` and anchors verified chain heads into the vouch chain as policy-governed, signer-bound, revocable `anchor_receipt` facts — the cross-org capability channel's trust anchor; zero-dep gate 17/17. Release identity consolidated at 17.6.0 across every manifest, bundle and operational doc. |
| **17.1.4** | **Patina — hardening polish: dispatch validation, auditable retries, real brain-repair hook, honest `withSchema` semantics.** Tiny follow-up to 17.1.3 closing the reviewer's four residual gaps. (1) **`dispatch_mission` IS NOW ZOD-VALIDATED** — the earlier `if (!isDispatch) { … }` skip in both `sendVouchMessage()` and `runVouchToolCall()` is gone; a new shared `validateWithRetry()` throat runs every tool AND `dispatch_mission` through the same Zod+retry path. `DispatchMissionSchema` (non-empty objective) is enforced. (2) **`VouchBrain.repairArgs` IS A REAL OPTIONAL HOOK** — added to the exported interface with a precise contract: called once on first validation failure with `(tool, rawArgs, errors)`, returns a single repaired candidate or null; returning null/throwing falls back to deterministic repair; the simulated brain does not fabricate cognition. (3) **`withSchema()` NOW ACTUALLY RETRIES** when given a `{ repair }` callback (validate → repair → re-validate → either execute or return `{ok:false, retried:true}`). Without the callback it's a plain validated wrapper, honest naming. (4) **AUDITABLE SCHEMA EVENTS** — `tool.schema_retry` and `tool.schema_refused` receipt events now carry `rawArgs`, `repairedArgs`, `errors`, `attempts`, `repairSource` (`"brain"|"deterministic"`), and `repairReason` so an auditor can see exactly what the model proposed, what was repaired, why, and whether execution happened. AG-UI remains emit-only; receipts remain source of truth. Vendor tree 705 files. Gates on node v20.20.2 and v22.23.2. |
| **17.1.3** | **Patina — hardening wired into the production execution throat.** (1) **POLICY GATEWAY ON THE REAL PATH** — both `sendVouchMessage()` and `runVouchToolCall()` (the MCP capability router) now route every action through `PolicyGateway.propose()`: `deny` emits AG-UI INTERRUPT + a sealed receipt and never runs the tool; `steer` falls through to simulate+human-gate; `allow` runs fast-path. The exported `RISKY_TOOLS` is now a reference to the gateway's canonical set (`workspace_write`, `dispatch_mission`, **`shell_exec`**), fixing the earlier steer-vs-runtime inconsistency. Custom rules registered with `registerPolicyRule()` are evaluated in order and can `deny`/`steer`/`allow` for future CEL/policy-replay work. `budgetRule`/`workspaceRootRule` seats are reserved hooks (no-ops today, not advertised as active). (2) **ZOD SCHEMA VALIDATION WITH ONE STRUCTURED RETRY** — every tool/dispatch is validated before `tool.run()`. Deterministic repair (trim, shell `$ ` stripping, path-traversal basename rewrite, 1 MB content cap, number→string coercion) runs FIRST as safe normalization; if validation still fails, the engine records a `tool.schema_retry` receipt event, attempts a second structured repair from the exact error, and validates once more. Only if the retry ALSO fails does it refuse with a `tool.schema_refused` event and never calls `tool.run()`. Model-backed brains can hook `VouchBrain.repairArgs` to feed the validation error back to the LLM; the simulated brain uses conservative deterministic fixups (no fake cognition). A new integration probe pins this: fixable whitespace args run exactly once after trim, unfixable args are refused without executing. (3) **AG-UI EVENT EMITTER LIVE** — every Vouch turn emits RUN_STARTED/STATE_SNAPSHOT/TEXT_MESSAGE_START·CONTENT·END/TOOL_CALL_START·ARGS·RESULT/INTERRUPT/RUN_FINISHED·ERROR onto an EventTarget; AG-UI is emit-only and never alters governance state (receipts remain source of truth). (4) **PACKAGING REGRESSION RESOLVED** — `vendor/` tree 705 files present; `src-tauri/tauri.conf.json` `bundle.resources` ships it; counts **103 live / 98 offline** probes/suites synchronized everywhere; `tools/drill-benchmark.mjs` and `tools/external-model-validation.mjs` now `packages:"bundle"` so their temp-dir runs resolve zod without NODE_PATH. Counts: **99 live probe files (hardening1713 now 11 assertions incl. retry pin) / 98 offline bundles.** Vendor tree present (705 files). Gates on node v20.20.2 and v22.23.2. |
| **17.1.2** | **Patina hardening — the three named review defects closed, all cert strings re-aligned to 17.1.** (1) **PROVIDER USAGE LEDGER ROUTED THROUGH THE KV ADAPTER** — the same `asKV()` seam that fixes the registry now fronts `recordUsage` / `listUsage` / `clearUsage`; the Monitor panel's usage counts no longer depend on a `Map`-cast against `localStorage` and read/write through the same guarded adapter on web, desktop and probes. (2) **RELEASE STRINGS CONSOLIDATED AT 17.1.2** — every current-facing document (ONEPAGER, DECK-OUTLINE, INFORMATION-ARCHITECTURE, README, BUILD-NATIVE, DESKTOP-NATIVE, INSTALL-ON-LAPTOP, DEPLOY-VERCEL, PLATFORM-LIMITS, VH-17.1-UPGRADE, FEATURES, package.json, Cargo.toml, tauri.conf.json, Cargo.lock, package-lock.json, release.yml/ci.yml) agrees on one version line; the offline verification pack is regenerated from source so the doc-identity probe ships no baked-in 16.10.2 literals. (3) **LEGACY PROBES REWRITTEN FOR PATINA IA** — navAlign, palette, theme, preflight, meridian, and merge now assert the 5-dock Patina shell (Harbor / Ship / Chart / Register / Harbor Master) with the Helm as the single command surface, instead of the superseded 6-door Teammate route; CommandPalette composes through `fuzzyScore` with strict label-first ranking; atelier.css verdigris `#3E7C71` / ink-950 / parchment palette pinned; no `as any` remains in Harbor view. (4) **MCP ENGINE BUNDLE REBUILT** (`tools/mcp-engine.mjs` 480,822 B, byte-pinned with sha256 manifest). Counts: **98 live suites / 97 offline bundles** (after Patina IA rewrites + buttonActions added), gates on node v20.20.2 and v22.23.2. |
| **16.10.2** | **The deliberately-boring hygiene patch (the 16.10.1 review's final list).** (1) **THE PROVIDER REGISTRY'S DEFAULT PATH FIXED** — the review caught the last survivor of the Storage-shape bug family: `defaultStore()` handed the UI `localStorage`, which the registry then read/wrote as a `Map` (`.get/.set`) — `listProviders()` threw, was caught, and silently showed an EMPTY provider list in any real browser/WebView while `addProvider()` could throw outright. One KV adapter now fronts real `Storage` (getItem/setItem), injected Maps (probes), and ephemeral hosts — and every write is guarded: a storage refusal comes back IN WORDS (`provider store refused the write: …`), never thrown, never silently dropped. (2) **STALE RELEASE STRINGS ELIMINATED, AND THE NET WIDENED** — `Vouch Harbor_16.0.0_x64-setup.exe` (16.0-era) survived in three operational docs because the drift scanner only matched `MJ <version>` tokens; the scanner now catches every product-named version token (MJ / VH / Vouch Harbor prefixes and bare `<version>_x64` artifact names), which immediately surfaced and killed the stragglers in DESKTOP-NATIVE and INSTALL-ON-LAPTOP. (3) **TRIGGER SEMANTICS TITLED HONESTLY** — the card now leads with "Always-on while Vouch Harbor is open": in-app triggers tick while the app runs and survive a reload; an OS daemon that survives window-close remains the native-build seat, named and not claimed. (4) The production-stack probe grew the review's exact scenario as a behavioral pin: add/list/remove through the DEFAULT store against a real getItem/setItem shape. **96 live suites / 95 offline bundles.** |
| **16.10.1** | **The integration/hardening release — every 16.10.0 engine wired into the runtime it named, per the external review's verdict that "implementation has outrun the integration." No new features; the six become ONE product.** (1) **REAL PROVIDER ADAPTERS** — groq had fallen through to the OpenAI URL, `base_url` was ignored for every non-ollama kind, and Anthropic got its system prompt stuffed into `messages`: now each kind has its true endpoint (Groq `api.groq.com/openai/v1`), `base_url` is honored for EVERY cloud kind (BYOK gateways), and Anthropic speaks its actual Messages contract (top-level `system`, required `max_tokens`). (2) **DURABLE MISSIONS ARE THE RUNTIME** — the 16.10.0 storage adapter cast `localStorage` to a `Map` (structurally broken on the default path); now a uniform KV adapter wraps real `Storage`, injected Maps, or degrades to ephemeral honestly — and `MissionRuntime.run()` resumes from the digest-verified snapshot, saves after EVERY step, and the Done ledger marks only VERIFIED completions (never twice). (3) **TRIGGERS ARE ALWAYS-ON FOR REAL** — the "mission queued…" stub is dead: triggers ride `dispatchMission` (the SAME governed merge-seam path as chat: intent, risk, gate, receipt), saved triggers persist across reloads and tick on a real interval while the window is open; the daemon seat stays honestly named for the native build. (4) **SKILL IMPORTS MUTATE THE GENOME** — an import now registers a real, persisted `CapabilityGenome` (UNDER_EVALUATION, governorReopen semantics, provenance stamped) instead of returning a label; the UI supplies the issuer public key out-of-band. (5) **THE RECEIPTED BROWSER IS BILATERAL** — desktop click/type/extract ride the existing `browser_act` boundary instead of refusing; every result still lands in the hash chain. (6) **THE SECURITY STORY MATCHES THE WORDS** — the web edition no longer stores cloud keys AT ALL (a browser-readable secret with zero use was pure risk): desktop = OS keychain, web = Ollama/local only, refused in words. (7) **THE SANDBOX IS SYMLINK-PROOF** — filesystem containment canonicalizes the REAL path (deepest existing ancestor + re-joined tail) on both sides; and `shell_exec` gains a capability boundary (allowlisted agent/dev CLIs, registered custom harnesses, or executables inside registered roots) instead of arbitrary programs with only a cwd check. Suite: `probe/productionStack` grows the integration-hardening subtest (7 subtests, behavioral + source pins). **96 live suites / 95 offline bundles**. |
| **16.10.0** | **The production release — the six features that make the teammate a production agent.** (1) **UNIVERSAL MODEL PROVIDERS** (the founder's redesign: not local-only): ChatGPT (OpenAI), Claude (Anthropic), Gemini, Groq, OpenRouter, Ollama and any OpenAI-compatible endpoint — BYOK via the local secret store (keys never enter the registry or code), tier-per-step routing (cheap for routine, big for hard), and the three verbs built in: WORK (tiered labeled calls through the governed `ipc.llmChat` boundary; desktop runs every provider, the web edition runs Ollama for real and refuses cloud in words — a browser must not hold cloud keys), MANAGE (registry + ping health checks + per-tier routes), MONITOR (a usage ledger with ok-rate, avg/p95 latency, per-provider counts and ESTIMATED cost — labeled estimates only, priced models only). `wrapModelBrain` wraps the harness seam wraps the labeled core — all OFF by default. (2) **DURABLE MISSIONS** — the runtime's own `persist()/restore()` (written in 12.x, called by nobody until now) wired to a digest-stamped `vh-durable-mission/1` envelope: save each step, verify-then-resume, refuse tampered snapshots in words; the Done ledger marks an action complete ONLY after verified evidence — a half-finished step cannot fire twice. (3) **THE SKILL STORE** — signed `vh-skill-catalog/1` packages with real provenance (mission + verified seats); imports verify signature AND digest, then land UNDER_EVALUATION — an imported skill re-proves itself on this machine, never imported to ACTIVE; tampered digests and unsigned catalogs are refused in words. (4) **THE NEVER-GIVE-UP LOOP** — a standing constraint ledger re-read after every step (violations refuse the step), "ask the human" as a real action (silence is not an answer), and proof-before-done: no completion claim without verification evidence. (5) **ALWAYS-ON TRIGGERS** — interval/time-window triggers dispatch REAL governed missions through the SAME human gate, with mechanical quiet hours, daily caps and one-concurrent-run; the daemon seat is named, not claimed. (6) **THE RECEIPTED BROWSER** — goto/extract run for real (accessibility-first fetch mode in any edition; native Chromium on desktop), interactive actions refuse in words where the seat is absent, and EVERY action — executed or refused — lands in a hash-chained action receipt you can export. (7) **THE NATIVE-BOUNDARY FIX (the 16.9.7 external review's material find)** — the seam sent the TS-resolved absolute path (`/usr/local/bin/claude`) as the IPC provider id while the Rust gate allowlists BARE names (`claude`), so the native production path refused an installed harness the DI probe could never see; fixed per the reviewer's preferred design: `bareProviderId(bin)` rides the wire, the allowlist stays the single authority, Rust resolves via `which_bin` (fast paths + login-shell paths), and the boundary is PROCESS-proven — a new `probe/brainSeam` native-boundary subtest pins the wire contract in the Rust source, proves every walk bin gate-safe, and mirrors the resolution with a REAL PATH-resolved executable (absolute-path decoy untouched) through resolve → invoke → parse → REAL label. Faces: System gains the Production Ops surface (providers manage/monitor, triggers, skill import, constraints, durable spec); the Browser door becomes the receipted browser. New suite: `probe/productionStack` (6 subtests) — **96 live suites / 95 offline bundles**. |
| **16.9.7** | **The production-seam patch — the 16.9.6 external review's biggest find, fixed at the boundary.** The review proved the real-model planning seam could not actually run in any shipped bundle: `vite.config.ts` aliases `node:child_process` to the honest web stub, so the seam's `execFileSync` threw there and was caught as "exited 1" — a fabricated exit code for a CLI that never ran (exactly what this product exists to prevent), and a synchronous spawn would have blocked the chat's event loop regardless. Fixed: (1) **HOST-AWARE** — the production invoke routes Tauri → `ipc.cliInvoke` (the existing governed IPC boundary into the Rust bin allowlist, the same route the mission loop's hostDeps uses), refuses the web edition in words via a typed `BrainHostError` BEFORE any spawn (the stub is never touched — the module no longer imports `node:child_process` at all), and probes inject their own invoke; (2) **ASYNC** — `VouchBrain.decide` now admits `VouchPlan | Promise<VouchPlan>`, the governed pipeline awaits it, and nothing downstream changed (same simulation, gate, receipts, learning); (3) **stale source narrative** — the vouch.ts header's "no host app and no mission engine yet" and "honestly refused until the merge" are gone (merged since 16.0), with the absence pinned in the probe; (4) **THE TRUE END-TO-END (scope: the injected probe harness)** — `probe/brainSeam` now resolves a REAL binary (node itself, harmless and deterministic), invokes it as a real child process, parses its output into the plan, and pins the REAL label, the hybrid `simulated+real-plan` identity, the refusal taxonomy (host-cannot-spawn is never reported as an exit), and the routing pins in source — the Tauri IPC → Rust gate itself is contract-pinned in source, not process-driven. Counts unchanged: **95 live suites / 94 offline bundles**. |
| **16.9.6** | **The honesty patch — the 16.9.5 external review's two findings, closed and pinned.** (1) **Truthful brain identity** — the real-model seam wrapper's `id`/`label` are live getters: with the seam enabled the product reports `simulated+real-plan` (never "Brain: Simulated" in one surface while a thought says "REAL model — Claude"); disabled, the base identity stands. (2) **Zero stale product strings** — the welcome copy no longer promises "a provider connected through the brain seam (next release)" (it shipped), the fallback no longer says dispatch is "honestly refused until the mission engine is merged in (Step 3)" (merged since 16.0 — the copy now says so), and runtime info reports the LIVE brain id instead of a hardcoded "simulated (offline)". (3) **Language precision** — the seam names itself a real-model PLANNING seam (route/recall/act/gate/learn remain the governed runtime's), never a "fully real-model brain", and names the next milestone in words. All three are pinned in `probe/brainSeam` (identity, stale-string absence, precision) — this class of drift now fails the release. Counts unchanged: **95 live suites / 94 offline bundles**. |
| **16.9.5** | **The focused release — the three asks from the 16.9.1 external review, shipped as features.** (1) **Exhaustive doc-identity scanning** — the 16.9.1 review found current-facing docs (one-pager, deck outline, platform limits) still naming an old release; `probe/docIdentity` now scans EVERY current Markdown document (root + docs/, history excluded by design), reads every `16.x.y`/`v16.x.y` mention, allows historical references only (named context patterns), and FAILS on anything that is not the single `VH_VERSION`; the stale docs were fixed at the same turn (identity + count claims). (2) **The real-model brain seam** — the roadmap's oldest honest label ("brain: simulated") gains the drill's proven pattern: `src/vouch/engine/brainSeam.ts` wraps the labeled simulated brain so the PLAN step can ride a REAL agent CLI from the 25-harness registry (the drill's own install detection — no drift), spawn-for-real or REFUSED IN WORDS (unknown/not installed/failed/timeout/empty — never a fake run, no silent fallback), every run labeled in the plan's thoughts with harness + duration + the unchanged human gate, the browser edition and CLI-less hosts refuse honestly, and the preference is a human decision, OFF by default (`vh.brain.pref`). Probe `probe/brainSeam` drives success, refusal, failure, timeout, and the real host's honest absence via dependency injection — no real model needed to prove the honesty. (3) **The security-review artifact** — the deliverable 16.6.0 named as planned, now shipped: `vh-security-review/1` (`src/mission/securityReview.ts`) inventories every security surface with its posture AND its machine-checkable evidence pointer, states its findings in words (no external pen test; single-host threat model; drill seats are machinery not model intelligence), refuses its overclaims explicitly, and is SHA-256 digest-stamped + Ed25519-signed by the receipt issuer keychain when a key exists — the unsigned path is stated, never faked. Faces: Audit door gains the build/verify/export card. Plus: the release gate now scans docs; `authors` metadata cleaned to the founder (the 16.9.1 review's branding note). New suites: `docIdentity`, `brainSeam`, `securityReview` — **95 live suites / 94 offline bundles**. |
| **16.9.1** | **The executive-surface release — the chat-first face, the three unifying planes, and the provenance repair.** The product now OPENS as a chat (the ChatGPT/Claude posture: centered conversation column, Doto hero, starter cards, QUICK/DEEP cycle control) with the machinery demoted to icon-only rail items and a Panel drawer — the engine paths byte-identical. The three unifying planes ship on real engines: **Trust Center** (Proof door: vault + issuer + EU AI Act / ISO 42001 / SOC 2 crosswalk + one-click Trust Pack), **Assurance Scorecard** (the 0–100 measured-only score), **Cost per Outcome** (measured chargeback; `unmeasured` stays unmeasured) — web edition badged as labeled demo, per the house contract. The INK six palettes retire for **the QUIET FOUR** (obsidian/graphite/porcelain/bone — 2 dark + 2 light, designed dark-first, deliberate migration aliases; theme probe re-pinned 65/65) plus a reduced-motion-honouring motion layer. Provenance: the 16.9.0 review's findings closed — version identity synchronized across every manifest, the stale 16.8.0 `BUILD-INFO.txt` identity regenerated, the version-drift hole CLOSED (it now pins BUILD-INFO's opening identity), the composition layer probe-pinned (`executivePanels`), the offline-honesty semantics kept (esbuild tools refuse in words; the runner SKIPS honestly), and rendering the crosswalk exposed the last heritage name at the engine root (`whatVhProvides`). **92 live suites / 91 offline bundles.** |
| **16.9.0** | **The modern-client release — official MCP SDK v2 conformance (the 16.7.0 review's biggest remaining MCP gap, closed).** The external-client validation covered the legacy path (2025-11-25 via the official v1 SDK, probe #90) but not the modern one; 16.9 adds `probe/mcpSdkClientV2` (92nd suite, 10 tests) driven by **`@modelcontextprotocol/client` v2.0.0** (exact-pinned devDependency — the stable v2 line of the MCP TypeScript SDK, which implements the 2026-07-28 spec) over real stdio with the era **pinned to 2026-07-28**: pinned negotiation lands on the modern revision; `server/discover` reaches the client with both eras advertised (dual-era, externally); the 20-tool surface deterministically ordered; safe calls return real output; **the human gate the modern way (MRTR)** — `input_required` + opaque `requestState`, answered through the client's registered elicitation handler and retried with `inputResponses` inside `callTool()` — approve → executed → receipt `VALID` → write present in the workspace store, decline → reported in words and the store is checked; honest refusals in words (unknown tool, missing drill harness — never a fake run); the SAME v2 SDK in default (legacy) posture still negotiates 2025-11-25 and sees the same 20-tool surface; devDependency hygiene pinned. No new tools, no wire change for existing clients; one new devDependency (`>=20` engine — both certified runtimes). Counts: **91 live suites / 90 offline bundles**. Gates: tsc 0, 91/91 live, 90/90 offline, vite clean — under BOTH node v22.23.2 (CI runtime) and v20.20.2. Also: a stale one-pager count line (offline bundles frozen at 88 for two releases) corrected to the real 90 — honesty doc, pinned by the version-drift suite going forward. |
| **16.8.1** | **The certification patch — the 16.7.0 external review's diligence findings, closed.** No behavior change; two reviewer-found inconsistencies fixed: (1) **the runtime story was split** — release notes certified under Node 20.20.2 while CI installs Node 22 (and `puppeteer-core` already requires ≥22.12); now the full four-gate certification (tsc, 90/90 live, 89/89 offline, vite) is recorded under **both** Node v22.23.2 (the CI runtime; Node 20 reached EOL 2026-04-30) and v20.20.2. (2) **CI platform claims were stale** — README/one-pager said CI repeats verification on Linux, macOS and Windows, but the workflow is deliberately Windows-only (the pre-seed platform); the docs now say exactly that and name the portable offline pack (Node alone, any OS, zero installs) as the platform-wide path. Provenance correction: at the 16.8.0 turn boundary the workspace restore rolled `verify/` back to the 16.7-era pack — recovered byte-for-byte from the durable 16.8.0 zip, re-verified, and this release is certified on the recovered tree. Counts: **90 live / 89 offline** (unchanged). |
| **16.8.0** | **The capability-genome release — VH-COLOR Phase 1.** The roadmap resumes with the VH-COLOR open standard (competitive capability learning, evolution, exchange); its central invariant — "evolution may change capability, may not control its own safety authority" (§32–33) — was already VH's M6 tighten-only loop, and 16.8 ships the protocol's core unit around it: **the capability genome** (`src/vouch/engine/genome.ts`, `probe/genome`, 56 assertions): the §11 entity in full (objective, trigger, procedure, declared failure modes, resource limits, provenance, append-only evolution memory); the §31 state machine pinned table-exact (OBSERVED → CANDIDATE → UNDER_EVALUATION → SHADOW → CANARY → ACTIVE + the five negative states, all terminal except quarantine); promotion as a LINE through four hard gates — **G1 safety / G2 provenance (no orphan capabilities) / G3 dependencies / G4 zero-regression** — where failure QUARANTINES with the stated reason and only a named governor re-evaluation lifts it (the promotion path itself refuses — pinned). The invariant is mechanical: the governor's grant is a SEPARATE argument; a grant embedded in the genome's own fields is invisible to the gate (the probe runs the forgery attack and it quarantines). Trust grades **C0–C5** derive from the provenance chain only (receipt → replay → ≥2 seats → ACTIVE lineage parent); the **signed capability package** (`vh-capability-package/1`: canonical digest + Ed25519 from the receipt keychain) is portable, tamper-evident, and re-earns trust on import (lands UNDER_EVALUATION, never ACTIVE); production regressions **auto-rollback** with fallback to the last ACTIVE version (roll back, don't fail); M4 skills convert to genomes with receipts and seats intact. NO new MCP surface — the 20-tool conformance pin, dual-era wire, and SDK-client suite hold untouched; no new dependencies. Phases parked with reasons: P2 surface, P3 cross-agent observation over A2A, P4 archive; marketplace (§68) and real-model learner-beats-teacher (no agent CLIs here). Counts: **90 live suites / 89 offline bundles**. Gates on node v20.20.2: tsc 0, 90/90 live, 89/89 offline, vite clean. |
| **16.7.0** | **The external-validation release — official SDK client, real-model seam, benchmark integrity.** Hardening sequence item 2 ("real external-model validation"): (1) **Official client conformance** — `probe/mcpSdkClient` (90th, 8 tests) drives the real server with the official `@modelcontextprotocol/sdk` v1.30.0 (pinned devDependency; its latest protocol 2025-11-25 is one of our advertised legacy revisions — connect success IS the version-negotiation proof): 20-tool surface, safe calls, the full human-gate flow to a verified receipt, denial executes nothing, honest refusals. (2) **Real-model seam** — `run_drill` gains an optional `harness`: the seats dispatch to that harness's REAL CLI (loop spawns the real bin; report + receipt label the seat REAL); unknown/missing harness refused in words, never a silent fallback (pinned in-process, over the wire, in the tool description); `tools/external-model-validation.mjs` inventories the product's own 25-harness registry on PATH and, when a real CLI is present, runs the full drill catalog on it (stable `overallDigest`) — on a CLI-less host the report honestly says so. (3) **Benchmark integrity** — the impossible scenario's SEAL was a hardcoded constant the seat could read (leaked answer); now a per-run random 128-bit SEAL, the test file never enters a seat's worktree (verifier re-injects the canonical copy per verification run), and a probe-pinned canary classifies absent/ok/tampered. Deterministic verdicts and attestations unchanged. Counts: **89 live suites / 88 offline bundles** (drill suite 15 tests). Gates on node v20.20.2: tsc 0, 89/89 live, 88/88 offline, vite clean. |
| **16.6.0** | **The hardening release — conformance-pinned MCP, one throat, honest docs, reproducible benchmark.** Per the 16.5.0 review ("stop adding broad features — disproportionate hardening + external proof"), no new surface, four pinned fixes: (1) **MCP conformance suite** — the official 2026-07-28 / 2025-06-18 / ext-tasks JSON Schemas committed under `tools/mcp-conformance/fixtures/` with provenance and sha256 pins; new `probe/mcpConformance` (89th, 12 tests) validates every message — both eras, both directions, our requests and the server's responses — against the official definitions, and writes `report.json` evidence per run. First run caught a real defect: the `-32021` error carried `requiredCapabilities` as an array where the spec defines a ClientCapabilities object — fixed, re-pinned. (2) **One throat** — the 15.x Teammate prototype (`src/mission/teammate.ts`, `pages/TeammatePage.tsx`, `probe/teammate.test.ts`) deleted; `probe/vouch.test.ts` now pins the complete whole-tree set of files touching the mission loop engine (engine module, `bridge.ts`, `LoopPage`). (3) **Honest docs** — the one-pager's "independent audit" claim replaced with what the tree proves (hardening recorded in `PRODUCT-PUSH-NOTES.md`; signed report + reproducible external artifact named as planned deliverables, not claimed); version drift closed; the drill's deterministic-seat distinction (machinery, not model intelligence) explicit in customer-facing materials. (4) **Reproducible drill benchmark** — `tools/drill-benchmark.mjs` runs the full catalog through the real Mission Loop and emits a machine-readable report with one stable `overallDigest`; probe-pinned including cross-process attestation parity. Counts: **88 live suites / 87 offline bundles** (−teammate +mcpConformance; vouch 39 tests, drill 10, mcpRouter 22, vhClean 8). Gates on node v20.20.2: tsc 0, 88/88 live, 87/87 offline, vite clean. |
| **16.5.0** | **Spec-current MCP (2026-07-28) + the completed clean identity.** (1) **Dual-era MCP router** — the router now speaks the 2026-07-28 specification's stateless core alongside the legacy revisions: per-request capabilities/identity in `_meta`, the required `server/discover` (supported versions + capabilities + `ttlMs`/`cacheScope`), `resultType` on every modern result, deterministic (sorted) tool lists with the spec-required cache hints, and the two new error codes (`-32021` missing client capability, `-32022` unsupported version with the supported list). (2) **MRTR** — a gated modern call returns `resultType: "input_required"` with the human decision as an `elicitation/create` request plus an opaque `requestState`; the client re-sends the call with `inputResponses` and the decision resolves the same gate. (3) **Tasks extension** (`io.modelcontextprotocol/tasks`) — clients that declare it get durable task handles over gated work: `tasks/get` (poll), `tasks/update` (answer), `tasks/cancel` (cooperative, honest about work already in flight); the extension is advertised in `server/discover` and never served to a client that didn't declare it. (4) **Legacy clients unchanged** — the 2025-x wire is byte-stable (no `resultType` on old results, initialize handshake intact), so every existing client keeps working; both eras route through the SAME governed executor. Probe-pinned: `probe/mcpRouter` grows from 14 to 22 tests (both eras over real stdio, including the full Tasks lifecycle to a verified receipt). (5) **Clean identity completed on the visible surface** — legacy heritage names removed from the whole visible product surface (shell, pages, canvas watermark, onboarding, IPC bridge, browser stubs, domain catalogs, agent identity prompts, provenance demo), `VOUCH_ACP_BIN` replaces the ACP override env (legacy name still honored), IPC workflow contract moves to `VH` on both sides of the bridge, and a new `vhClean` scan pins the entire visible surface — engine-internal historical comments are the one labeled exception, deferred to a dedicated identity-sweep release. (6) **Docs: one product, one version, one narrative** — the one-pager, deck outline, information-architecture map and platform-limits doc rewritten to the current release with the current capability inventory. Gates on node v20.20.2: tsc 0, **88 live suites / 87 offline bundles** (mcpRouter 22 tests, vhClean 8), vite clean. |
| **16.4.1** | **The provenance patch.** Reviewer-found defects in exported artifacts, fixed and probe-pinned: (1) `buildMissionRecord` no longer hardcodes the obsolete execution version — mission records stamp the single `VH_VERSION` constant, and `probe/missionRecord` now asserts a freshly sealed record reports the current release. (2) Clean-identity residue in exported text removed — the learned-skill manifest author is "Vouch Harbor" (was a legacy name), and the issuer public-key document names the current `vh-proof-receipt/2` wire first with the legacy wire as back-compat, in the clean identity. (3) The drill's MCP tool description is version-agnostic. No behavior change beyond the version stamp: **88 live suites / 87 offline bundles**, all green; tsc 0, vite clean. |
| **16.4.0** | **The drill — the product proving itself on real missions.** The reviews' remaining external-validation gap becomes a product capability: standard real missions run on demand, with the verdict vouched. (1) **Scenarios** — `guard` (fix the disabled-admin authorization bug), `maths` (implement the missing `clamp` so the failing test passes), `impossible` (the honest-failure scenario: the test demands a secret outside the seat's playbook — the loop must report FAILED, never a fake pass). Each scenario is a fresh REAL git repository (real `git init`/commit, real worktrees per seat) whose OWN test command decides the outcome. (2) **The real loop** — every run dispatches through the real mission loop: real in-process governance arena, budget ledger, one signed cycle receipt, a unified mission-ledger entry, and the drill's own vouch receipt (origin "drill", the real test-output tail in the `vouch.drill` event) that verifies offline. (3) **Honesty model, probe-pinned** — real: git, verification, loop, receipts; labeled: the deterministic built-in drill seat (the swappable-brain seam — on a host with real agent CLIs, real crews run real missions); failed scenarios keep their forensic git repo (path in the report) and are vouched too. (4) **One throat** — `run_drill` is a governed, RISKY tool: chat, MCP and the UI all route it through runVouchToolCall (human gate, receipts, no special path); MCP surface is now 20 tools; the System door gains **Drill** (scenario catalog + persisted report ledger with attestation digests). (5) **Attestation digest** — sha256 over the stable fields (scenario, status, verified seats, seats, cycle, version): reproducible per scenario, not wall-clock. (6) New **`probe/drill` (88th)** — 9 tests: PASSED runs with real test output, the honest-failure run (real FAIL output + kept real git repo + vouched), unknown-scenario refusals, digest stability, the single-throat gate, and the full flow over real stdio (gate → approve → real mission → PASSED → receipt verifies over the wire → ledger). Counts: **88 live suites / 87 offline bundles**. Gates on node v20.20.2: tsc 0, 88/88 live, 87/87 offline, vite clean. |
| **16.3.0** | **The gated, reversible meta-loop (M6) — the last major roadmap piece.** The product can now propose changes to its OWN control plane, and every self-change is proposed in words, simulated, human-gated, receipt-vouched and reversible — a self-improving governance layer that is a feature, not a loop. (1) **Targets** — `risk.tier` (tighten a tool's gate: safe → risky) and `preference.set` (add a standing preference applied by RECALL); the meta loop may NEVER loosen — demotions are refused in words and the refusal is vouched; loosening happens only via a revert. The receipt protocol, the gate primitive, the brain and the code are explicitly NOT self-modifiable at runtime. (2) **The five properties, probe-pinned** — proposed in words (mandatory reason), simulated (dry-run prediction in the gate + receipt), human-gated (the SAME gate primitive — and the revert is gated too), vouched (applied / denied / refused / reverted all mint receipts with the full diff, origin "meta", verifying offline), reversible (exact state restored; reverts are terminal and carry their own receipt, bound to the original change). (3) **Behavior-pinned** — after a tightening is applied, a call to that tool through the governed executor ACTUALLY pauses at the human gate; after the revert it runs freely again; the same proof runs over the wire (clock free → wire-gated → wire-reverted → free). (4) **Faces** — MCP gains `meta_propose` / `meta_status` / `meta_revert` (the governed surface is now 19 tools); System gains the **Meta loop** door (current gated surface, proposal form, self-change ledger with diffs/reasons/decisions/receipts, gated Revert) — decisions are made on the one gate (Vouch face) or over MCP. (5) `src/vouch/engine/meta.ts` + the new **`probe/metaLoop` (87th)** — 9 tests: engine-level pins (all five properties, the tightening-only refusal book, preference round-trip, denial path, full-ledger receipt verification) and the real server over real stdio. Counts: **87 live suites / 86 offline bundles**. Gates on node v20.20.2: tsc 0, 87/87 live, 86/86 offline, vite clean. |
| **16.2.0** | **The MCP capability router (M3) — Vouch Harbor becomes callable.** Any MCP client (Claude Desktop, an agent harness, your own tooling) can list and call the product's capabilities over stdio, and every call routes through the SAME governed pipeline as the chat face: risk classification → human gate on risky calls → signed receipt per completed call. (1) **Single governed executor** — `runVouchToolCall` is the one route from any face to the capability tools; there is no second door and no bypass. (2) **16 governed tools** over JSON-RPC 2.0: the safe surface (`calculator`, `clock`, `search`, `web_search`, `memory_save`/`memory_recall`, `preference_save`, `workspace_list`, `system_info`), the risky surface (`workspace_write`, `dispatch_mission` — human-gated, non-blocking: `pending` + `approvalId`/`callId`, resolved from the UI or the server's own `approve_action`/`deny_action`, polled with `call_status`), and the audit surface (`mission_status`, `verify_receipt`). (3) **Receipts never optional** — success, denial, refusal and error all mint a receipt carrying the calling face's `origin` (`"mcp"`); `verify_receipt` re-verifies offline (chain + seal + issuer). (4) **Real mission loop over the wire** — approved `dispatch_mission` mints a mission ID, runs the real loop on the host crew, lands in the unified mission ledger, and mints its receipt at completion — chat parity. (5) **2026-07-28 spec direction** — stateless self-describing requests, explicit handles threaded by the client (the spec's own guidance), poll-based long-running work (the Tasks direction); wire stays `2025-06-18` so every current client works. (6) Committed engine bundle `tools/mcp-engine.mjs` byte-pinned by the new **`probe/mcpRouter` (86th)** — in-process engine pins + the real server driven over real stdio (initialize, tools/list, safe call + receipt verify, unknown-tool refusal, full denial flow, full approval flow, a real mission dispatched over the wire, JSON-RPC errors) + bundle freshness. Counts: **86 live suites / 85 offline bundles**. Gates on node v20.20.2: tsc 0, 86/86 live, 85/85 offline, vite clean. |
| **16.1.0** | **The clean identity + the learning bridge.** (1) **Clean identity** — the legacy heritage names are gone from the product surface: UI, bot identity, provenance, persistence keys (`vouch.*`), IPC channels (`vh://*`), styles (`vouch.css`, `vh-*` keyframes), generated files, exports and the native manifest (`com.vouchharbor.harbor`). One product version line (`VH_VERSION`) stamps the execution core, control plane, proof protocol and native shell; dispatch provenance reads "Vouch Harbor execution core <ver>" / "Vouch Harbor control plane <ver>". Pinned permanently by the new `vhClean` probe (85th). (2) **Proof wire** — new receipts emit `vh-proof-receipt/2`; each wire generation is sealed with the published constant of that generation (the legacy constant is preserved, never renamed); every verifier — in-process (both planes) and the three offline tools — resolves the seal per format. Back-compat is fixture-pinned: a signed pre-16.1 receipt verifies in-process and via the CLI; a one-hex-char chain tamper fails. Legacy exported documents (`mj-dossier/1` and the other five formats) still verify through their readers. (3) **M4 — the learning bridge**: a mission that runs to a verified done distills into a `dispatch_mission` skill carrying real mission provenance (mission ID, team, verified seats, cycle); distillation is trajectory-aware (steps = the loop phases that actually ran); a replay gate refuses too-coarse triggers (failures → failure memory); the fast path binds the skill into the run's receipt and still pauses at the human gate; a failed live replay flags the skill permanently. Proven end-to-end against a real git repo and the real mission loop (`probe/vouch.test.ts` M4 suite). (4) **M5-lite — the mission timeline**: the Missions tab opens each dispatched mission into its unified chain (mission events + session/verdict bookends) as a phase timeline. Counts: **85 live suites / 84 offline bundles**. Gates on node v20.20.2: tsc 0, 85/85 live, 84/84 offline, vite clean. |
| **16.0.0** | **The Vouch Harbor merge — one product, one mission ID, one vouched chain.** The asymmetric merge of the two codebases: the execution engine (mission loop, crews, harnesses, receipts) + the control plane (the Teammate/Vouch face, routing, skills, failure memory) become ONE product with ONE receipt chain. `dispatch_mission` now drives the REAL mission loop under one mission ID minted at dispatch; every execution event is projected into the unified chain; the mission outcome lands in the unified mission ledger (one state — the ONE-STATE LAW). The face door is the control plane; the shell routes it under the "Vouch" label. Merge contract in `docs/STEP3-MERGE-SPEC.md`. Counts: **84 live suites / 83 offline bundles**. |
| **15.1.0** | **ROGUE becomes the runtime — the Vouch Cycle 2.0 (the accountable colleague, built fully).** The Teammate door's engine is upgraded from "a seat with tools" to the full Vouch Cycle: **ROUTE** (dual-process — fast path or slow path triaged by risk + steps + mode, shown in the trace), **RECALL** (facts, matched preferences, available skills — all vouched in the receipt), **SIMULATE** (risky actions are dry-run first: ROGUE signs a prediction — "what will exist when this is done" — the human gate shows it, and VOUCH checks prediction vs. reality in the receipt: `matched`/`diverged`), **LEARN** (successful runs distill **test-gated skills** — unknown tools and failing replays are rejected into **failure memory**, so self-learning cannot regress; user feedback (1–5 + note) binds to the receipt, classifies the failure mode, and flags bad skills for review), **threads** (dropped-thread continuity — "continue <thread>" resumes a dropped conversation with real context, honestly listed when none matches), **learned preferences** (visible, deletable, applied in RECALL), **live web evidence** (keyless Wikipedia/HN/GitHub providers via the MJ web-evidence layer — source kind stated, provider failures reported, never hidden), and an **inspectable Markdown-native memory export** (facts, preferences, skills, failure memory, workspace, threads — one `.md` you own). The receipt protocol gains `teammate.simulation` events and `route`/`prediction`/`skillId` verdict fields. The `teammate` probe grows 16 → 33 assertions pinned to all of it. Self-upgrade (ROGUE proposing changes to its own control logic) is the final gated feature — substrate in place (skills + failure memory + receipts), gated behind the evidence plane in a later release. Version bumped to 15.1.0 across all six pinned manifests; offline pack rebuilt. Counts: **82 live suites / 81 offline bundles**. |
| **15.0.0** | **The Teammate door — the Vouch Harbor face (major IA: five doors → six; the app opens on the face).** (1) New door **Teammate** leads the rail: a named, persistent seat (ROGUE) you message like a colleague, over the SAME engine — the human face, not a second product. (2) `mission/teammate.ts` — the teammate engine module (the page's ONLY import, probe-pinned): the labeled simulated brain (offline, rule-based — the `TeammateBrain` seam is where a real model plugs in 15.1), real tools (recursive-descent calculator — no eval; clock IST/UTC/local; offline knowledge base, honestly labeled; local memory the user can inspect and delete; a local workspace; system info), a **HUMAN GATE** that pauses the run for approval on risky actions (workspace writes, mission dispatch) and records the decision, and `dispatch_mission` — a chat sentence that drives the real `runMissionLoopCycle` with the host's real deps (no crew → honest refusal, never fake). (3) `receipts.buildChainedReceipt` — the generic event-list minter on the shared mj-proof-receipt/2 protocol (SHA-256 chain, HMAC seal, Ed25519 issuer signature or an honest `signatureNote`): **every finished teammate run is vouched** — tamper-evident, verifiable offline, JSONL-exportable from the rail. (4) IA moved together as one: PageKind + nav map + App route + page + probes (navAlign: six doors, engine group = teammate+loop+workflow, the app opens on the face); new probe `teammate` pins the engine rule, the gate (pause → approve executes, deny executes nothing, both recorded in the receipt), tamper detection, the receipt round-trip and the labeled brain. Version bumped to 15.0.0 across all manifests; offline pack rebuilt with the new suite. Counts: **82 live suites / 81 offline bundles**. |
| **14.1.1** | **The trust-anchor fix (the reviewer's forgery attack, closed) + mission-complete economics.** (1) Both standalone verifiers (`verify-mission-record.mjs`, `verify-receipt.mjs`) no longer accept a self-reported issuer key as authentic: a signed artifact without a pinned key verifies only as `VALID SIGNATURE — issuer UNVERIFIED` (exit 3, fingerprint printed); `--issuer-key <hex|@file>` pins the trusted Ed25519 key — match = `issuer AUTHENTICATED` (exit 0), mismatch = INVALID (exit 1). The claim is corrected everywhere: **zero MJ state required, issuer key pinned out-of-band** — never "zero trust". Pinned by new CLI cases in `missionRecord` and `verifierTool`. (2) Mission Record `economics` now spans EVERY cycle: `perCycle` chargeback rows (the exact CSV derivation) plus `totals` (measured spend, unmeasured/over-budget cycles, overrun, tokens-only and simulated seats, shared-cap `missionBudgetUsd`/`missionAdherence`) — the finance view is mission-complete, never last-cycle-only. Re-certified under standalone node v22.23.2. Counts unchanged: **81 live suites / 80 offline bundles**. |
| **14.1.0** | **The Mission Record + the ATELIER precision pass — one story, one artifact, one premium surface.** (1) **The Mission Record** (`mission/missionRecord.ts`): the crown artifact that unifies the evidence chain — ONE Ed25519-signed, hash-digested file per mission carrying authority (arena stamps, gate tiers), execution (cycle seats/outcomes), economics (the exact chargeback row the CSV exports), fleet assurance context (labeled as such), the canonical timeline and every vault receipt re-verified at seal AND at verify. Broken evidence is carried with its mark; an unsigned record must say so. Standalone zero-dependency verifier `tools/verify-mission-record.mjs` (digest + Ed25519 + embedded-receipt verification); Audit page ships the sealer (egress-gated download). Probe `missionRecord` pins seal/verify/tamper/CLI against the real stores. **The story: one mission, one record, zero MJ state required.** (2) **ATELIER** (`styles/atelier.css`): the precision refinement layer — Linear/Raycast-grade restraint over the frozen INK palettes: hairline borders + inset top highlights instead of heavy shadows, tighter display tracking (-0.022em), Inter cv01/ss03/zero features, tabular instrument numerals, 14px cards with calm hover lift (transform/opacity only; paint ≤200ms — meridian rule kept), machined buttons with quiet press and visible focus rings, inset inputs, near-invisible scrollbars, reduced-motion kill-switch. Counts: **81 live suites / 80 offline bundles**. |
| **14.0.1** | **The integration + certification release (the fundraising flagship).** The reviewer's verdict on 14.0.0 was exact — strong primitives, but only their probes called them. Closed: `mission/evidenceSurfaces.ts` reads the REAL mission-loop ledger (`mj.missionLoop.v1`, which now persists `tokensOnlySeats` and the signed `budgetUsd` cap; schema-tolerant for pre-14.0 records), the receipt vault and the feedback store, and the **Audit page** ships the three evidence products as surfaces: the Assurance Score card (live 0–100 with the named factor breakdown and the honest `unevaluated` refusal), the Agent FinOps card (live totals + an EGRESS-GATED, digest-stamped CSV chargeback export — the export itself passes the Egress Gate), and the Incident black box (mission picker → sealed, tamper-evident dossier download with the verification verdict shown). New probe `evidenceSurfaces` seeds the real stores through their real APIs and pins the derived math + refusal cases. All four gates re-certified under the standalone node v22.23.2 (tsc 0, 80/80 live incl. offlinePack freshness, 79/79 offline, vite clean). Counts unchanged: **80 live suites / 79 offline bundles**. |
| **14.0.0** | **The Assurance release — evidence as product.** (1) `tools/verify-receipt.mjs`: a standalone, zero-dependency receipt verifier (chain + seal + Ed25519; file or stdin) so the zero-MJ-state claim is a product, probe-pinned by `verifierTool`. (2) Agent FinOps (`mission/finOps.ts`): measured, receipt-backed chargeback rows with digest-stamped CSV export; tokens-only seats stay `unmeasured`, simulated seats are never charged. (3) Fleet Assurance Score (`mission/assuranceScore.ts`): a 0–100 evidence-derived rating with a named factor breakdown — unevaluated without measured runs, diluted by simulation. (4) Incident black box (`mission/incidentDossier.ts`): a one-file tamper-evident forensic dossier whose receipts are re-verified live at build AND verify time, with a SIEM JSONL projection. (5) **Product surfaces, not libraries**: the Audit page now ships the Assurance Score card (live factor breakdown), the Agent FinOps card with an egress-gated digest-stamped CSV chargeback export, and the Incident black box (mission picker, sealed tamper-evident dossier download) — all reading the real mission-loop ledger, receipt vault and feedback store via `mission/evidenceSurfaces.ts`; cycle records now persist `tokensOnlySeats` and the signed `budgetUsd` cap. Also folded in from this cycle: CI probe watchdog 120s→300s with `MJ_PROBE_TIMEOUT_MS`, always-on probe-log artifacts and runner git identity; the receipt-issuer Ed25519 key seals to the OS keychain on native (new typed `secret_get`); portable browser-service paths replace dev-machine leftovers; README header repaired. Counts: **80 live suites / 79 offline bundles**. |
| **13.5.1** | **Skill/implementation agreement (the 13.5.0 review's one finding, closed — then the design system freezes).** The 9.85/10 review (product-polish 10/10) flagged that `skills/ui-motion-language` said "Animate only transform and opacity" while the NATURA CSS also transitions paint properties (box-shadow, border-color, background, color, stroke) — a shipped skill stricter than the implementation it documents. 13.5.1 takes the reviewer's recommended option 1: the rule now reads **layout motion uses only transform/opacity** (never width/height/top/left/margin), while **visual-state transitions may also animate paint properties** (short, ≤200ms, state not entrance). No CSS changed — the code was the correct nuanced behavior; the documentation now says what the code does, so the skill and implementation agree. The fix is probe-pinned: meridian 45 → **46/46** (the motion skill must keep the nuanced rule and never regress to the absolute). **Design freeze** — with the skill aligned, the design system is frozen at the INK six + NATURA material/motion + the three shipped design skills; future UI work extends the frozen system through the skills. Highest-value next work: real users, real repeated missions, real evidence. Counts unchanged: **75 live suites / 74 offline bundles**. Gates under node v22.23.2: tsc 0, 75/75 live incl. offlinePack freshness + versionDrift 40/40, 74/74 offline, vite 0. |
| **13.5.0** | **The NATURA pass — built from three design skills that now SHIP in the repo.** MJ ships `skills/ui-premium-craft`, `skills/ui-motion-language` and `skills/node-graph-craft` — spec-form SKILL.md files (frontmatter name + description with what-and-when triggers) that encode how MJ's surface stays premium; any agent runtime or MJ's own Knowledge Forge (human-approved path) can load them. Then the pass applies them: **(1) Warmth & depth** — soft diffuse elevation instead of borders-everywhere, cards and nodes lift 1px with deeper shadow on hover, one-shot selection ring, slimmer scrollbars, warm selection ink, no gradients, no glow-stacks. **(2) Natural motion** — a unified deceleration curve (cubic-bezier 0.33,1,0.68,1); pages settle (fade + 10px rise + micro-scale) and cards drift in staggered (max 4 steps); toasts settle; connected ports ring once as confirmation; buttons get a quiet press; every effect is transform/opacity only with an explicit prefers-reduced-motion kill-list. **(3) No palette change** — the INK six (13.0.1) stay; this pass is frame, motion and craft. Probes: meridian 42 → **45/45** (+3 skill pins: presence, spec frontmatter, trigger sections). Counts unchanged: **75 live suites / 74 offline bundles**. Gates under node v22.23.2: tsc 0, 75/75 live incl. offlinePack freshness + versionDrift 40/40, 74/74 offline, vite 0. |
| **13.0.1** | **The INK retune — the owner-review pass on 13.0.0 (palette judged "common"; surface still instrument-cramped).** **(1) Palette on true black.** OBSIDIAN six → INK six: `ink` (black #000000 + smoked apricot #C98A62, default), `pitch` (OLED black + dusty rose), `slag` (graphite + whetstone steel), `fern` (black-green + seedpod olive), `ivory` (paper + copper) and `travertine` (stone + bronze-olive) lights. No gold, no brass, no violet, no cyan, no neon — every secondary stays color-mix-derived; old prefs (incl. the 13.0 names) migrate by alias. **(2) The premium frame (research-driven: whitespace and type hierarchy are the largest premium signals, not paint).** Body copy `.muted` moves from 9-11px mono to 12.5px Inter; cards 22×24 padding at 14px radius; buttons/inputs 8px radius with accent focus rings and a quiet press state; the nav rail reads in Inter 12.5 with an active accent tick; Settings theme options become real choices with live two-tone ground+signal previews, names and descriptors. **(3) Icons — the clean cut.** The FOUNDRY facet chips are gone; all 43 glyphs are a uniform 1.6-weight stroke, round caps/joins, no fills. **(4) Node anatomy v2 (the actual node redesign).** Cards carry live state on the frame (running/error/done tint border + icon tile; running agent spine pulses, killed by prefers-reduced-motion); ports now straddle the card edge so wires visibly land on their anchors, connected ports fill with the signal and a soft ring, valid targets pulse; purpose clamps to two lines; meta is one quiet dot-separated line. Probes: theme.test → **64/64** (reads THEME_IDS from store.ts as single source; ink hue pins, black-ground proof, no-gold ban, alias migration incl. OBSIDIAN-era names), meridian 42/42 with clean-cut icon pins. Engine untouched: missionLoop 80/80, knowledgeSkills 56/56, navAlign 42/42. Counts: **75 live suites / 74 offline bundles**. Gates under node v22.23.2: tsc 0, 75/75 live incl. offlinePack freshness + versionDrift 40/40, 74/74 offline, vite 0. |
| **13.0.0** | **The production-surface finish — the OBSIDIAN design system (major UI/UX upgrade on the frozen 12.x engine; engine logic untouched).** Built to the 2026 calm-tech direction after live design research: warm elevated neutrals as the ground, ONE rare mineral signal per palette, no AI-violet/cyan neon, no gradients. **(1) Brand-new theme set** — the fourteen INSCRIBED palettes retire to six finished palettes: obsidian (warm-jet + brass, new default), onyx (OLED + champagne), hematite (graphite + verdigris), peat (botanical + clay), ivory (paper + copper, the light flagship), travertine (warm stone + bronze-olive); every secondary derived from five base hues with color-mix (232 derivations); old saved themes migrate deliberately via THEME_ALIASES (wabi→obsidian, inscribed→onyx, cyanotype→hematite, daylight→ivory, …); wire/data-type colours retuned to the same mineral voice. **(2) A new node concept** — cards are precision plates: head icon on a tile, category spine on the left edge (brass for agents/controls, quiet ink otherwise), a breathing state lamp (running = brass pulse, reduced-motion = none), framed purpose/stream previews, instrument meta chips. **(3) Port↔wire connection clarity** — type-coloured port rings fill with the graph signal when connected, every anchor carries a reach stub across the card edge so wires visually join their port, valid drop targets pulse while linking, wires have stronger presence with hover glow. **(4) Motion** — 140–280ms purposeful ease-out, transform/opacity only, every animation dies under prefers-reduced-motion (probe-pinned). **(5) Assist removed (owner decision)** — the panel only ever mounted on the canvas page while its buttons were global, which read as "broken" everywhere else; nav item, Ctrl/Cmd+J, console button, panel and CSS are gone; the honest one-node drafting path (Home field + offline-labelled drafts) remains. Probes rewritten deliberately: theme.test 61/61 (OBSIDIAN contract: six-theme parse, exact hue pins, luminance proofs, retired-set ban, migration aliases, splash pins), meridian 42/42 (assist removal + tile/reach/pulse/lamp pins). Counts: **75 live suites / 74 offline bundles**. Gates under node v22.23.2: tsc 0, 75/75 live incl. offlinePack freshness + versionDrift 40/40, 74/74 offline, vite 0. |
| **12.2.0** | **Provider precision on the Knowledge Forge (the 12.1.1 review's one remaining item, closed).** The 9.85/10 review confirmed every 12.1.1 fix and froze the release with one open item: `dataHandling: "provider"` says content went to the harness's configured provider but not WHICH provider — Anthropic, OpenAI, a local model, or an enterprise proxy. Provider-bound proposals now record **`providerInfo`**: `vendor` (the harness's default when unconfigured — claude→Anthropic, codex→OpenAI, gemini→Google, grok→xAI, qwen→Alibaba Qwen, opencode→configurable; a labeled default, never a receipt), `endpointClass` **cloud-default / local-configured / unknown**, and `endpointBasis` **detected / user-declared / not-visible**, with the written reason on every record. Honest by construction and grounded in the actual Tauri surface (the renderer sees only PATH/searched/bins — MJ cannot read the harness's own endpoint override settings): a host-supplied `readEnv` probe classifies only what it can detect (no override visible → cloud-default; loopback override localhost/127.0.0.1/::1 → local-configured; any other override → unknown — MJ cannot tell where an enterprise proxy terminates; a throwing probe → not-visible); the user's own declaration on the forge card is honored as user-declared; otherwise the truthful default is **unknown + not-visible with its reason written** — never a guess. Mechanical paths keep `providerInfo: null` (nothing left the machine). The forge card gains an **ENDPOINT (your declaration)** select (unsure / default cloud / local model) and a provider chip per proposal (`provider: Anthropic · cloud-default` etc.; unknown classes are visually distinct and carry the reason on hover). Probes: knowledgeSkills 44 → **56/56** (+12: vendor-default table, loopback classification, the detected/declared/not-visible truth table incl. invalid-URL and throwing-probe rows, mechanical-null pin). Counts unchanged: **75 live suites / 74 offline bundles**. Gates under node v22.23.2: tsc 0, 75/75 live incl. offlinePack freshness + versionDrift 40/40, 74/74 offline, vite 0. |
| **12.1.1** | **Honesty fixes for the Knowledge Forge (the 12.1.0 review's two findings, closed).** The 9.6/10 review caught one real bug and one real overclaim. **(1) Renderer crash fixed** — `knowledgeSkills.ts` computed byte provenance with Node's `Buffer.byteLength`; the probe environment (Node) passed while the browser/Tauri renderer would throw `ReferenceError: Buffer is not defined` on every distill. Provenance now uses `new TextEncoder().encode(content).byteLength` — the codebase's own renderer-safe pattern — and the probe scans the module to forbid any Node-global `Buffer` from returning. **(2) Data-handling claim corrected** — 12.1.0 said the LLM pass meant "no content ever leaves the machine"; that is only true for the mechanical extractor and for harnesses pointed at a local model. Invoking Claude/Codex/OpenCode sends the document in the prompt to that harness's configured model provider (Anthropic, OpenAI, …) under the provider's own terms. Every proposal now records `dataHandling: "local" | "provider"` reflecting what ACTUALLY happened (mechanical / missing-binary = local; any invoked CLI = provider, even when the answer was garbage or crashed), the Loop page discloses the provider-send before a cloud pass and shows a `data: sent to provider` / `data: local` chip on every proposal. **(3) Wording narrowed** — "books → skills, the provable way" overstated: provenance and governance are provable, the extracted knowledge's correctness is not (a book can be wrong; approval is governance, not proof). The feature is positioned as *documents → structured knowledge proposals, with provenance and human approval*. Probes: knowledgeSkills 34 → **44/44** (renderer-safety scan, data-handling truth table incl. garbage/crash = provider, UI disclosure, honest wording). Counts unchanged: **75 live suites / 74 offline bundles**. Gates under node v22.23.2: tsc 0, 75/75 live incl. offlinePack freshness, 74/74 offline, vite 0. |
| **12.1.0** | **Knowledge Forge — books to skills, the provable way (the frozen 12.0 engine's first new capability).** Inspired by the 2026 OSS "book-to-skill" wave (virgiliojr94/Leutenegger distillers; the Agent-Skills format), built NATIVELY into MJ's own honesty rules: documents distill locally into structured knowledge PROPOSALS — a deterministic mechanical extractor (frameworks, decision rules, anti-patterns, code patterns; structure, not summaries) plus an OPTIONAL LLM pass that runs through MJ's OWN installed harness CLIs (claude/codex/opencode… local, no keys, no uploads). Six guardlines, each probe-pinned (probe/knowledgeSkills 34/34): (1) provenance is mandatory — real SHA-256 of the content, byte length, tool+version, distiller identity, time; (2) no structure → no proposal (an unstructured blob is refused in words); (3) nothing installs silently — one human decision per proposal, recorded with identity; (4) knowledge ≠ learning — proposals claim no measured effect, never touch lessons/autonomy/bandit, and ride briefings labelled [knowledge]; (5) the LLM pass is best-effort with an honest fallback (missing binary / garbage / crash → mechanical, reason written into the proposal, never credited to a model); (6) approval mirrors into the shared skill memory as an approved [knowledge] RECOURSE (governed human write) that rides EVERY future mission briefing through the same approvedSkillDefs path verified-mission skills use — the loop carries the book forward without pretending it measured it. The Loop page gains one Knowledge forge card (distill → approve/discard), engine-only imports preserved (navAlign 39 → **42/42**: forge is engine API; the page imports exactly one mission module). Counts: **75 live suites / 74 offline bundles**. Gates under node v22.23.2: tsc 0, 75/75 live incl. offlinePack freshness, 74/74 offline, vite 0. Next in the program: the Paperclip support connector (MJ delegates to an external agent-company when it needs the scale) — scoped, not yet built. |
| **12.0.3** | **Rating scope (the 12.0.2 review's one remaining item, closed — then frozen).** The 9.9/10 review's only open point: `submitHumanFeedback()` enforced rating validity, team binding and seat records but not that the cycle actually RAN. The engine now refuses an ABORTED cycle outright — `cycle … aborted before any seat ran — nothing to rate` — before any store is touched; a gate-FAIL/blocked cycle whose seats DID run stays ratable, by design: humans rate exactly the runs that went wrong (the ledger UI shows the panel on every cycle row, so this is a reachable invariant, not a pure API nicety). Probe (missionLoop 76 → **80/80**): an aborted cycle is produced through the engine's own exception path (no seat outcomes), the refusal is explicit, the store stays byte-identical, and a blocked cycle rates cleanly with its queue entry landing. Counts unchanged: **74 live suites / 73 offline bundles**. Gates under node v22.23.2: tsc 0, 74/74 live incl. offlinePack freshness, 73/73 offline, vite 0. With this, the 12.0 architecture is FROZEN: one canonical orchestrator, one loop ledger, one product API, five doors, human feedback bound + supersede-safe + rating-scoped. The next release is the real-world evidence program — repeated runs with real crews, measuring whether the loop's adaptations change verified behavior over time. |
| **12.0.2** | **Feedback integrity (the 12.0.1 review's two open items, closed — and then the architecture is frozen).** The 12.0.1 review verified the human-feedback-in-the-loop design and flagged two edge cases in `submitHumanFeedback()`. **(1) Team binding** — the API verified the cycle existed but never checked `args.teamId === cycle.teamId`, so a caller could file Team A's cycle feedback under Team B's id and write into Team B's seat records. It now refuses with `cycle … ran team X, not Y` BEFORE any store is touched; the probe proves the refusal leaves the evolution store byte-identical and the loop state untouched. **(2) Unconsumed re-ratings accumulated** — the UI keeps one current rating per cycle, but rating 4/5 → 2/5 → 5/5 before the next fold queued three entries, so the pipeline could fold criticism the human had already changed their mind about. Queued entries are now per-run (`runId` on each) and `applyTeamFeedback` supersedes any unconsumed entry for the same run — the human's last word wins until the fold consumes it; the verbatim history still records every submission (probe: 2 seats × 3 submissions = 6 rows), and the superseded 2/5 comment provably never reaches the evidence. Probes: teamEvolution 45 → **48/48** (unit: collapse + history + fold), missionLoop 67 → **76/76** (integration: refusal refs, reviewer's exact 4→2→5 scenario, one queued input, fold consumes one, no pretend criticism, suppression armed). navAlign stays 39/39. Counts unchanged: **74 live suites / 73 offline bundles**. Gates under node v22.23.2: tsc 0, 74/74 live incl. offlinePack freshness, 73/73 offline, vite 0. |
| **12.0.1** | **The Human-Feedback-in-the-Loop patch (the 12.0 review's open items, closed).** The 12.0.0 review confirmed the ONE-ENGINE fusion but found three honest gaps — this release closes all three. **(1) The explicit human feedback surface is back, inside the Loop.** Pre-12.0, TeamsPage carried the 1–5 rating + comment UI feeding `applyTeamFeedback()`; 12.0.0 retired the page but never migrated the interaction, so the engine's feedback was only measured outcomes + accept/reject. Now `missionLoop.submitHumanFeedback({cycleId, teamId, rating, comment})` is engine API and the Loop page renders a rating panel on every ledger cycle: a rating queues on every seat that ran, and the next ADAPT fold turns it into real evidence through the SAME pipeline the old page drove by hand — 1–2 becomes weight-2 human evidence (comment preserved verbatim), 4–5 arms praise suppression, 3 is neutral; queues are consumed exactly once (no double counting) and the loop state keeps one current rating per cycle. Probes prove it on real failing cycles: a 2/5 comment shows up as `Human: …` evidence at the next fold, a 4/5 arms suppression without inventing criticism, invalid ratings/unknown cycles are refused without touching any store. **(2) The Loop page now talks to the engine ONLY.** Crew persistence (loadCrews/persistCrew), the host-deps adapter (loopHostDeps) and the bus projection (loopBusFeed) moved behind missionLoop's API; the page imports exactly one mission module (navAlign's forbidden list now includes agentTeam/hostDeps/interAgentChannel). **(3) The claims are now precisely the architecture.** "ONE engine, ONE cycle, ONE store" overstated the fusion — missionLoop orchestrates the specialized engines of record (team evolution, autonomy/bandit, lessons, receipts, bus, crew persistence); it does not fuse them into one physical store. Headers, IA docs and this changelog now state the precise model, and navAlign pins the honest wording. Counts: missionLoop 56 → **67/67**; navAlign 32 → **39/39**; suites unchanged **74 live / 73 offline**. Gates under node v22.23.2: tsc 0, 74/74 live incl. offlinePack freshness, 73/73 offline, vite 0. |
| **12.0.0** | **ONE ENGINE — the Mission Loop (the real consolidation; 11.14.10 organized the shelves, 12.0 fuses the machinery).** The reviews said it best: the app's features were separate productions inside one app — TEAMS, team self-evolution, the feedback loop, adaptive learning and agent communication each with its own store, page and vocabulary. 12.0 merges them into ONE runtime: `src/mission/missionLoop.ts` owns the whole cycle — COMPOSE → DISPATCH → COMMUNICATE → EXECUTE → GATE → ADAPT — and folds every measured outcome through the SAME engines (seat signals → human-gated team-evolution candidates, bandit + elastic settlement, lesson reflection, one signed proof receipt per cycle) into ONE loop ledger (`mj.missionLoop.v1`). The product surface collapses to one engine screen + five doors: **Mission Loop · Workflows · Proof · Audit · System** (`src/app/nav.ts`, PageKind trimmed to the five; legacy feature pages stay in the tree only where probes exercise them and are no longer navigable — no orphan doors). The lifecycle MJ narrated since 11.14.10 now runs INSIDE the engine as its real phase rail. The Loop page (`src/pages/LoopPage.tsx`) is one working surface — crew compose, run deck, live bus feed, human gate on proposals, cycle ledger with receipts, adaptation strip — and imports the ENGINE only, never the fragment stores (navAlign pins that). Engine proof `probe/missionLoop.test.ts` (56 assertions) runs REAL gated team missions on real git worktrees: three measured failing cycles produce a seat candidate from evidence; a human REJECT changes nothing (the seat keeps failing — no pretend learning); a human ACCEPT applies a superset instruction edit; the next cycles complete verified; receipts per cycle verify end to end; bandit pulls accumulate; OFF mode records telemetry but never proposes. navAlign re-pinned 30 → 32 assertions (five doors, Engine/Verify/System, no orphan routes, page-imports-engine rule, phase rail == engine arc). Counts: **74 live suites / 73 offline bundles**. Gates under node v22.23.2: tsc 0, 74/74 live incl. offlinePack freshness, 73/73 offline, vite 0. |
| **11.14.11** | **The Provenance Cleanup + true race test (the 11.14.8 review's two open nits, closed).** **(1) Release-hygiene fixed and made mechanical** — the review found the release metadata said 11.14.8 while operational docs still named older artifacts: `BUILD-NATIVE.md` shipped `MJ_8.0.0` installer filenames, `DESKTOP-NATIVE.md`/`INSTALL-ON-LAPTOP.md` opened at "MJ 11.14.1" and named `MJ_11.0.0` installers, and `docs/PLATFORM-LIMITS.md` carried a stale title — all invisible because versionDrift only compared the `MJ X.Y` short form on title lines and never scanned bodies. All operational docs (BUILD-NATIVE, DESKTOP-NATIVE, INSTALL-ON-LAPTOP, DEPLOY-VERCEL, README, PLATFORM-LIMITS) now name exactly the current release, and `probe/versionDrift.test.ts` is extended (26 → 40 assertions): title checks are PATCH-aware (`MJ X.Y.Z` must equal the exact release, so 11.14.1 can never pass as 11.14.11) and every operational doc's BODY is scanned for any `MJ X.Y.Z` / `MJ_X.Y.Z` token naming a different release — history-file pointers are masked, everything else must be current. **(2) The budget-race scenario is now genuinely concurrent** — previously two sequential `reserve()` calls (which proved the atomic property but raced nothing); now 50 rounds of two async callers via `Promise.all`, each reaching `reserve()` after a real event-loop yield: exactly one admission per round, the cap never crossed. Counts unchanged: 73 live suites / 72 offline bundles. Gates under node v22.23.2: tsc 0, 73/73 live incl. offlinePack freshness, 72/72 offline, vite 0. |
| **11.14.10** | **The Consolidation Release — one product, one spine (11.14.9 intentionally skipped).** Kills the "fourteen mini-apps" feel: navigation was two drift-prone ad-hoc arrays in App.tsx with legacy per-release labels; now it is ONE source of truth (`src/app/nav.ts`) shaped as the product lifecycle — Overview → Build → Run → Verify → Learn → System — where every destination carries a stable key, a purpose-clear public label (Workflows, Mission Control, Connectors — legacy names retired), an icon and a one-line purpose shown as the sidebar tooltip. App.tsx renders ONLY from the map. New `probe/navAlign` (30 assertions) makes the consolidation mechanical: every PageKind has exactly one nav entry (no orphan pages), groups are the canonical lifecycle in order, labels match the public vocabulary, App cannot fork its own nav again, and the Home hero speaks the same pillar language (verification, learning from feedback, local data, signed evidence). Ships `docs/INFORMATION-ARCHITECTURE.md` — the model, the rename table, the 2026 IA research it applies (content-first IA, clear labeling, agent-UX accountability surfaces), and the rule for future releases (a feature belongs to a destination; a destination opens only when map + switch + probe move together). Navigation keys, page components and Ctrl+B collapse behaviour are unchanged — this is a product-alignment release, not a rewrite. Counts: 73 live suites / 72 offline bundles. Gates under node v22.23.2: tsc 0, 73/73 live incl. offlinePack freshness, 72/72 offline, vite 0. |
| **11.14.8** | **The Arena Gate WIRED — the battery now gates real missions (the 11.14.7 review's one gap, closed).** 11.14.7 proved the battery; 11.14.8 makes the headline literal: `executeTeam` now runs `runGovernanceArena()` BEFORE any seat is invoked — a REFUSED gate aborts the mission in words ("Nothing executed, nothing spent"), exactly like the custody, budget and action-packet admissions; the run report carries an `arena` stamp (gate + canonical sha256 digest over id+title+outcome + defended/total/breached); and the stamp rides the ACTUAL mission proof receipt as a new `arena.gate` event in the sealed SHA-256 chain (`AutonomyRunSummary.arenaGate` → `buildProofReceipt`; summaries without the stamp keep the pre-11.14.8 receipt shape byte-for-byte). Review nits also closed: the arena's exception classifier only accepts MJ's typed module-prefixed refusals (`custody:`/`ledger:`/`capability:`/`egress:` + refusal language), never stray wording; the canonical digest now covers id+title+outcome (notes stay human-readable in the report/event — raw notes embed a process-global sequence counter and would break receipt reproducibility, stated in code). New probe `arenaWired` (15 assertions): the mission path carries the PASS stamp with the same digest as the standalone battery at one clock, a REFUSED battery aborts before dispatch, the receipt chain carries the arena.gate event with the digest, and legacy summaries keep their old shape. Counts: 72 live suites / 71 offline bundles. Gates under node v22.23.2: tsc 0, 72/72 live incl. offlinePack freshness, 71/71 offline, vite 0. |
| **11.14.7** | **The Arena Gate — hostile scenarios become the gate to real runs (11.14.6 intentionally skipped).** Researched 2026 practice (Lakera, Promptfoo, PyRIT, Giskard; OWASP Agentic Top-10) says adversarial batteries must gate deployments — MJ's answer is not another LLM red-teaming SaaS but a battery that attacks MJ's OWN governance machine: `mission/arenaGate.ts` executes 11 hostile scenarios against the real custody / capability / egress / verifyGate / ledger modules (self-grading by the writer, an agent obtaining root authority, delegated scope widening, lapsed and revoked envelopes, hard budget caps and concurrent reservation races, egress outside scope, post-signing tamper, an agent writing DOCTRINE, off-policy datasets) and refuses the mission in words if ANY boundary falls. Where `guardrailAlign` asserts the source contains the right markers, `arenaGate` proves the boundaries hold at runtime — guardrails-as-code became guardrails-as-executable-battery. The report carries a canonical sha256 digest attachable to receipts; new probe `arenaGate` (22 assertions) pins the battery green and digest-stable. Counts: 71 live suites / 70 offline bundles. Gates under node v22.23.2: tsc 0, 71/71 live incl. offlinePack freshness, 70/70 offline, vite 0. |
| **11.14.5** | **The Focused Release — problem-first (11.14.4 intentionally skipped).** The 11.14.3 tree's engineering claims survived a hostile external audit (zero `as any`, zero `@ts-ignore`, zero TODO markers, zero Rust `unsafe`, zero npm vulnerabilities, 70/70 live + 69/69 offline re-verified), so 11.14.5 turns the remaining gaps into product: **(1) Problem-first focus** — `docs/PROBLEM-FOCUS.md` is the reverse-index every feature must answer (problem → feature → code → probe) against market evidence (88% of agent pilots never reach production; 92% of CISOs lack agent-identity visibility; EU AI Act high-risk enforcement wave began Aug 2026); the Home hero now names the problem MJ solves (prove what agents did, who approved it, that an independent agent checked it, that no data left the laptop) instead of describing internals. **(2) Bug fixed** — the packaged desktop CSP allowed no `https:` in `connect-src`, so the Researcher's renderer-fetched web-evidence providers (Wikipedia, HN Algolia, GitHub search, Brave) were silently blocked in the native build; the CSP now allow-lists exactly those four hosts plus the pre-existing local services, with no blanket `https:`. **(3) Hygiene floor made mechanical** — new `probe/codeHygiene.test.ts` (13 assertions): CI fails on any future type-escape hatch, TODO/FIXME marker, stray console output, second eval surface, CSP regression, Rust `unsafe`, or orphaned permission. **(4) Cleanup** — removed the orphaned `updater:default` grant (no endpoints/pubkey/artifacts/callers) and aligned `Cargo.lock`'s stale `mj-desktop 11.10.5` to 11.14.5, so every manifest agrees. Counts unchanged: 70 live suites / 69 offline bundles. Gates (external sandbox, node v20.20.2): tsc 0, 70/70 live incl. offlinePack freshness, 69/69 offline, vite 0; CI re-runs on Node v22.23.2. |
| **11.14.3** | **The Two-Machine Proof + durable privacy budget.** The 11.14.2 review's final judgment, executed: `mission/twoNode.ts` — A owns the data, B requests a capability, the relay (a dumb pipe that logs everything it sees) coordinates, A computes locally under the full Privacy Guard, only the bounded result crosses through the Egress Gate with a receipt. New probe twoNodeAlign (13) inspects the relay's own log: identity, request, authorization evidence, receipt — and ZERO raw record values; a doctored answer is caught at B by digest mismatch. Both technical weaknesses the same review named are closed: the privacy budget is now DURABLE — append-only, digest-chained, anchor-sealed ledger (dataset + requester + window), re-read on every request; restart resets nothing, doctoring refuses everything — and the minimum-cohort guard applies to FILTERED subsets (`where: { region: "APAC" }` refused at cohort 2). capabilityAlign 18 → 26; guardrailAlign 13 → 15 (durable budget; two-node relay visibility); thesis gains the proof's honest results. Counts: 69 live suites / 68 offline bundles. Gates on Node v22.23.2: tsc 0, 69/69 live, 68/68 offline, vite 0. |
| **11.14.2** | **The Privacy Guard — aggregate-only made privacy-honest.** The 11.14.1 review's decisive critique (aggregate-only ≠ privacy-safe: narrow or repeated queries can reconstruct rows) turned into mechanical guardrails: `DatasetPolicy` enforced BEFORE any computation — field policy (only declared fields aggregate), minimum cohort (small aggregates refused — they can expose individuals), hard per-window query budget (refusals count too — probing IS the attack), bounded precision (answers never carry more decimal grain than policy allows). capabilityAlign 11 → 18; guardrail manifest line 13; thesis gains competitor honesty (the moat is the combination — capability + policy + cryptographic receipts + agent execution — not "data stays local") and a section facing the hardest problem directly. Counts unchanged: 68 live suites / 67 offline bundles. Gates on Node v22.23.2: tsc 0, 68/68 live, 67/67 offline, vite 0. |
| **11.14.1** | **The Capability Channel — the enterprise thesis, working.** The upgrade from "share a file" to "expose a capability without exposing the data": `mission/capability.ts` — a remote colleague requests an APPROVED OPERATION (aggregate whitelist: count/sum/avg/max — structurally incapable of returning raw rows) on this machine's data; the computation runs WHERE THE DATA LIVES under a human-signed envelope scoped `capability:run`; only the digest-stamped answer may leave — through the Egress Gate, as a receipt of kind `capability-result`. No envelope / wrong scope / expired / revoked / non-human / off-whitelist / unknown dataset are each refused in words. Ships with `docs/ENTERPRISE-THESIS.md` — the validation artifact: cloud control plane, endpoint data plane, capability-not-data, the receipts moat, model-2 honesty, staged roadmap, and what must be validated before cloud infrastructure is built. Audit page gains the capability demo card; guardrail manifest reaches twelve lines. New probe: capabilityAlign (11); guardrailAlign 11 → 12. Counts: 68 live suites / 67 offline bundles. Gates on Node v22.23.2: tsc 0, 68/68 live, 67/67 offline, vite 0. |
| **11.14.0** | **The Egress Gate — the enterprise seed.** Company data stays on the laptop; the cloud is a switchboard, never a warehouse (WhatsApp, not a gallery upload): **(1)** `mission/egress.ts` — nothing leaves the machine without a fresh human-signed envelope scoped `egress:share`; missing/expired/revoked/wrong-scope/non-human envelopes are each refused in words; **(2)** the Egress Ledger — every authorized departure lands as a digest-chained receipt (item sha256, recipient, principal, envelope id); `verifyEgressLedger` names any edited record; **(3)** the Audit page routes the Proof Dossier export through the gate and shows the departure ledger with a one-click integrity check; guardrail manifest gains its eleventh line. This is the local primitive the multi-node enterprise roadmap (encrypted laptop-to-laptop channels, cloud relay that sees coordination but never payloads) is built on. New probe: egressAlign (8); guardrailAlign 10 → 11. Counts: 67 live suites / 66 offline bundles. Gates on Node v22.23.2: tsc 0, 67/67 live, 66/66 offline, vite 0. |
| **11.13.1** | **Hard-cap honesty + the commerce-blueprint patterns.** Fixes the 9.6/10 review exactly: **(1) Atomic budget admission** — the 11.13.0 cap was checked against a shared balance read before a concurrent wave, so parallel seats could all see "$0 spent" and overshoot; a `BudgetGate` now makes every seat RESERVE its share before dispatch (check-and-commit holds no await — the cap can never be crossed at dispatch time), admissions that fail are refused with the real reason, and settlement swaps each reservation for the REAL charge, measuring and naming any per-seat overrun. **(2) Token-only honesty** — harnesses that report tokens but no dollars are marked dollar-UNKNOWN in the run report and the budget accounting; MJ never invents a price (USD reported → enforceable; tokens only → spend in USD unknown). **(3) Dossier described correctly** — it is digest-stamped (tamper-evident from export), not independently issuer-signed; the Ed25519 signatures live on the learning receipts inside it. **(4) Market claim toned down** — MJ's defensible claim is the COMBINATION (agent-team authority + verification + learning evidence + proof export in one local system), not "no other product ships X". New capabilities, both adopted from public agent systems as PATTERNS (never code), with provenance on every entry: **the Pattern Registry** (`mission/patterns.ts` — sub-agent orchestration from OpenHands/SWE-agent, tool-calling registries from Goose/MCP, code-level guardrails + approval staging + skill-loading from Anthropic's Apache-2.0 Claude Commerce Agents blueprint, cost honesty from Codex/OpenCode) — any pattern can be proposed into the normal human-approval skill queue; and the **Guardrail Manifest** on the Audit page — the ten rules MJ physically cannot break, each pinned as a running code check by `probe/guardrailAlign.test.ts` (10). New probes: guardrailAlign (10), patternAlign (9); differentiatorAlign grows 16 → 22. Counts: 66 live suites / 65 offline bundles. Gates on Node v22.23.2: tsc 0, 66/66 live, 65/65 offline, vite 0. |
| **11.13.0** | **The Differentiators release.** MJ combines agent-team authority, verification, learning evidence and proof export into one local execution system. Market context (2026): coding agents compete on benchmarks and inline UX, while enterprises say copilots lack "reliability, auditability and policy control" — so MJ turned its governance primitives into enforced product features. MJ turns its governance primitives into enforced product features: **(1) Budget authority** — the root envelope now carries a hard USD cap (`budgetUsd`): signed into the canonical digest, attenuated like every other power (a child may tighten the cap, never loosen it), refused pre-flight when already exhausted, and checked by the executor against REAL charged spend after every wave — no further seat is invoked once the cap is crossed; the runner UI exposes the cap (blank = uncapped). **(2) Proof dossier** (`mission/dossier.ts`) — one click on the Audit page exports a single digest-stamped JSON: memories, beliefs, skills, the measured experiment, receipts and the live ledger, all derived from the stores at export time; any edit after export breaks the digest (`verifyProofDossier`). **(3) Proof-of-learning** rides inside the dossier: strategy versions, adopted score, measured-run count. Also fixed the 9.8/10 review's nit: Settings now says fourteen palettes. Numbering note: 11.12.4 was intentionally skipped — 11.12.5 shipped directly after 11.12.3; this feature release opens 11.13.x. New `probe/differentiatorAlign.test.ts` (16). Counts: 64 live suites / 63 offline bundles. Gates on Node v22.23.2: tsc 0, 64/64 live, 63/63 offline, vite 0. |
| **11.12.5** | **The Minimal Polish release.** (1) Debugging pass: every `JSON.parse` entry point audited (all guarded), zero stray console output verified, duplicate CSS affordance rules removed (`::selection`, scrollbars and the focus ring each existed twice with conflicting values — one canonical, palette-token version of each now). (2) Colors: a contrast pass over all fourteen palettes — muted text lifted toward legibility on dark themes, settled toward ink on light ones; pinned anchors (#000000 canvas, #1B1B1D surface, #D71921 signal) untouched, theme probe still green. (3) Nodes: hairline border at rest, hover *breaks the border* instead of lifting the card (the contradictory hover shadow is gone — INSCRIBED doctrine applied honestly), accent reserved for selection only. (4) Fonts: antialiased rendering, optical sizing, tabular numbers on instrument rows; the Inter/Space Mono/Doto stack unchanged. (5) Formatting: `.editorconfig` pins the tree's style contract (2-space, LF, final newline, trailing-whitespace hygiene). No architecture change; counts unchanged: 63 live suites / 62 offline bundles. Gates on Node v22.23.2: tsc 0, 63/63 live, 62/62 offline, vite 0. |
| **11.12.3** | **Governance becomes mechanical.** (1) The human-principal invariant is enforced, not documented: `issueRootEnvelope` refuses to sign for any principal that does not match `human:<id>`, attenuation refuses to delegate from a non-human root, and verification flags hand-rolled envelopes — pinned by probe. (2) The ledger's write-permission matrix is now a gate every governed write passes through: `saveLessons`, `saveSkills`, `saveBeliefs` and `saveImprovement` call `enforceWrite` before anything lands — agents can propose anything but mechanically cannot install approved skills, strategies, or house rules. (3) Funding/product collateral removed (FUNDING.md, pitch deck, product brief) — this tree is the build, not the pitch. governanceAlign 18 → 31 assertions. Gates on Node v22.23.2: tsc 0, 63/63 live, 62/62 offline, vite 0. |
| **11.12.2** | **External-review fixes — governance as real artifacts.** Authority Envelope (`mission/custody.ts`): signed envelopes with a human principal, scope, expiry and delegation chain; seats attenuate strict subsets, never outlive the parent; revocation instant; tampering fails verification; the executor refuses a mission its root envelope does not permit. Typed Ledger (`mission/ledger.ts`): SCAR / PRECEDENT / DOCTRINE / RECOURSE / STANCE with a write-permission matrix (agents never write DOCTRINE or install RECOURSE). One structured human approval queue (Evolution page: skills + beliefs). Plain-language Audit page (nav): receipts, experiment, five memories, beliefs — all live-derived. Step-repetition escalation wired through (SUGGEST-level only). New `probe/governanceAlign.test.ts` (18 assertions). Counts: 63 live suites / 62 offline bundles. Gates on Node v22.23.2: tsc 0, 63/63 live, 62/62 offline, vite 0. |
| **11.12.1** | **The execution boundary becomes fully cryptographic.** TeamExecutor verifies each action packet (SHA-256 digest + Ed25519 signature) FIRST and aborts on any mismatch before applying the mechanical permission rule. SCAR-first retrieval: failure lessons are queried before everything else. Write asymmetry: agent-inferred beliefs about the user start pending and brief only after human approval. Attenuated per-seat envelopes and step-repetition signals ride in the run report. Varkha extracts (docs/VARKHA-COMPAT.md) pinned by `probe/varkhaAlign.test.ts` (17). Gates on Node v22.23.2: tsc 0, 62/62 live, 61/61 offline, vite 0. |
| **11.12.0** | **MOSAIC alignment.** Belief store (`mission/belief.ts`) with epistemic status and approval asymmetry; proof-carrying action packets (`mission/actionPacket.ts`) signed at issue and checked at the boundary; causal edges in lesson memory (`retrieveCausal`); the strategy framework becomes a measured experiment arm; `probe/mosaicAlign.test.ts` (20 assertions) pins epistemics, packet integrity and source order. Gates on Node v22.23.2: tsc 0, 61/61 live, 60/60 offline, vite 0. |
| **11.11.1** | Binding review fixes for the 11.11.0 self-evolve loop: scar-prefix retrieval semantics, reflection determinism pins, and gate-policy hardening around simulated evidence. Gates re-certified on Node v22.23.2. |
| **11.11.0** | **SELF-EVOLVING — the AGI-grade autonomy release.** Measured runs now feed a closed improvement loop: **(1)** a lessons memory (`mission/lessons.ts`) reflects each run's measured facts into plain-language lessons that decay, reinforce and are retrieved into future team briefings (injected as `.mj-brief/ORG_LESSONS.md` for every seat); simulated runs produce environment lessons only — nothing about real execution is learned from them; **(2)** a strategy self-improvement loop (`mission/selfImprove.ts`) keeps an archive of team strategy versions, proposes one mutated candidate per generation and adopts only a strictly measured margin — all-simulated evidence leaves the candidate waiting with a written note; **(3)** skill evolution (`mission/skillEvolution.ts`) proposes reusable skills only from verified real missions (or three recurrences of one failure); skills join briefings only after human approval on the Evolve page; **(4)** learning receipts (`mission/learningReceipt.ts`) sign what the org learned — SHA-256 canonical digest plus Ed25519 issuer signature, verifiable with zero MJ state. New probe suite pins reflection determinism, memory dynamics, measured-only adoption, proposal honesty and receipt tamper-detection. Gates on Node v22.23.2: tsc 0, 60/60 live, 59/59 offline, vite 0. |
| **11.10.7** | **CLARITY — UI/UX redesign.** Icon rail replaced by a labelled, grouped sidebar (Workspace / Fleet / Proof / System) with Ctrl+B collapse to an icon rail; library drawer fixed (its entrance animation pinned it open, so the canvas could never use full width — now a slim LIBRARY handle); 14px base type with higher-contrast muted text; fourteenth palette **daylight** (paper ground, kiln-copper signal) and a theme-aware `color-scheme` meta so light themes render light form controls. Gates on Node v22.23.2: tsc 0, 59/59 live, 58/58 offline, vite 0. |
| **11.10.6** | Media asset release. Real 2× screenshots of the running product, the product brief and a 55-second demo recording ship under `media/`. No architecture change; manifests in lockstep; gates re-run. |
| **11.10.5** | **Verified AI Delivery V1.** Commit-bound provenance statements (`mission/provenance.ts`, in-toto-shaped, Ed25519-signed — issued only for merges that actually happened); AIBOM (`mission/aibom.ts`) built from receipts with approval status taken from the user's Role Board; deployer retention floor (Art. 26(6)-shaped, 6/12/24 months); Evidence Pack upgraded with all three plus SOX 404 / NIST 800-218A mappings. Suites #57–59 on a real git repository. |
| **11.10.1** | Merge engine. Ed25519 issuer signatures on receipts (`mj-proof-receipt/2`; a runtime without Ed25519 says so in writing via `signatureNote`), seat identity digests in-chain; **Merge Executor** (`mission/mergeExecutor.ts`) — `merge-tree` pre-flight, serialized merges, post-merge check, refuses blocked gates without a recorded override, reports `simulated` on git-less hosts, issues a signed merge attestation; **Evidence Pack** (`mission/evidencePack.ts`) re-verifies every receipt at export. Suites #54–56. |
| **11.10.0** | Enforcement. The adversarial gate is evaluated inside `executeTeam`, and its STRICT verdict blocks the merge path — the only way past is a recorded human override opened as a real approval request in Mission Control; every verifier's `reviewedSha` is checked against the snapshot sha so verdicts are bound to reviewed evidence; the runner emits real per-turn fleet heartbeats. Suite #53 on a real git repository. |
| **11.9.9** | Adversarial Verification Gate (`mission/verifyGate.ts` — verified only if a different harness than the writer checked the work); Receipt & Compliance Center (`mission/receiptVault.ts` — ledger, self-audit, SIEM export, compliance one-pager); Mission Control page over `mission/fleet.ts`; Role Board (`mission/roleBoard.ts`) — users declare which harnesses they own and which role each plays. Suites #49–52. |
| **11.9.8** | Layered (Sugiyama) auto-layout (`graph/layout.ts` — cycle breaking, longest-path layers, virtual nodes, deterministic barycenter crossing minimization, measured crossings) wired into `autoLayout`. Suite #48, 23 assertions. |
| **11.9.7** | Module headers state their full release lineage instead of a single version; historical markers kept as facts. Re-certified on Node 22. |
| **11.9.6** | Command-palette ranking made label-first (`paletteScore`); full re-certification under Node 22 LTS (v22.23.2). |
| **11.9.5** | Wire interaction fixed (drag-release port magnet, `connectRefusal` with the exact rule); Preflight lint (`graph/lint.ts`); command palette (`panels/CommandPalette.tsx` + `app/fuzzy.ts`); named checkpoints (`graph/checkpoints.ts`); Assist recut; **cyanotype** palette. |
| **11.9.4 (Redesign)** | Proof Receipts (`mission/receipts.ts` — SHA-256 hash-chained, sealed JSONL, verifiable with zero MJ state; suite #44); elastic seats inherit a harness the team already runs; structural redesign layer `styles/redesign.css` (tokens-only, inherited by every theme). |
| **11.9.4 (Commercial)** | Licensing engine (`mission/licensing.ts` — personal/trial/pro, offline HMAC-signed keys), first-run onboarding, License card in Settings. |
| **11.9.4 (Major+)** | The three autonomy engines wired into real execution: `prepareAutonomy()` picks bandit arms per run, `executeTeam` modulates briefings and wave plans, runs settle measured outcomes into `autonomyStore`; `verify/BUILD-INFO.txt` pinned by the offlinePack gate. |
| **11.9.4 (Major)** | Autonomy engines: Web Evidence search (`mission/webSearch.ts`), Elastic Seats (`mission/elasticSeats.ts`), Bandit-Routed Evolution (`mission/evolutionBandit.ts`). |
| **11.9.4 (Fixed)** | **wabi** becomes the default palette; transform/opacity-only motion system (boot splash, node entrance, wire draw-in, page transitions), all behind `prefers-reduced-motion`. |
| **11.9.4** | FOUNDRY visual identity: carbon ground, cream ink, brass accent, engraved icon grammar, serif display voice. |
| **11.9.3** | Theme cascade fix — `:root` fallback pinned to `:where(:root)` specificity so theme blocks always win. |
| **11.9.2** | Offline pack built and certified under Node 22 instead of EOL Node 20. |
| **11.9.1** | Release-packaging fix — the 11.9.0 archive had shipped an empty `verify/suites/` (a vacuous 0/0 gate) and stale provenance text; pack regenerated, BUILD-INFO rewritten, gates green. |
| **11.9** | De-vendored the external hermes agent (~12 MB); NTH theme; canvas integrity fixes (wires re-measured onto rendered port anchors, stacking contexts, inset ports). |
| **11.8.7** | De-vendored `vendor/hermes-agent` and its self-evolution agent; skill contract and evolution fitness engine rewritten as MJ TypeScript. |
| **11.8.5(i)** | Deep type pass — concrete persisted-state types, narrowed dispatch helpers. |
| **11.8.5** | CI runner currency (ubuntu-24.04 / macos-15 / Node 22); typed `MjCommands` registry; zero `as never` in `src/`. |
| **11.8.4** | Vendored runtime MCP servers + browser stubs for node builtins. |
| **11.8.1** | Environment hardening — EACCES/EPERM/ENOEXEC classified UNMEASURED; exit-code-first verdicts. |
| **11.8.0** | Turn-limit truth — `withTurnLimit()` capability-driven per harness. |
| **11.7.1** | Offline verification gate (`verify/run.mjs`) shipped as a release artifact; harness count 21→25. |
| **11.7.0** | Fixed the vacuous harness gate (`ok()` arguments reversed since 11.6); surfaced 5 latent defects. |
| **11.6** | Connector — 25-harness registry with researched install + argv per harness, custom harnesses. |
| **11.5** | Meridian — icon grammar, node contracts, Assist redesign. |
| **11.4** | Teams loop audit (praise-drain, arm suppression), palette and signal diet. |
| **11.3** | Inscription — design tokens, self-hosted fonts, mechanical motion. |
| **11.0** | Replay, evals, git layer, Proof page; CI compiles the real Tauri crate on three OSes. |
| **10.0** | Reviewer snapshots against writers' branches. |
| **9.0** | Teams that review what was written, not the base. |
| **8.0** | Version metadata consolidation. |
| **7.0** | Real verification + git layer, replay/evals, Proof page. |
| **6.0** | Agent runtime × identities × reusable teams. |
