# Vouch Harbor 18.5.0 "Apex" — upgrade notes

Base: 18.4.0 "Zenith". The product owner asked for VH to stand next to
xAI/SpaceXAI's **Grok Bot** (August 2026) and its open-source shapes
(**gawkbot**, OpenGrokBot, Rakazo and kin) without losing the governance
floor. This release absorbs what those products got right — goal mode,
standing approval rules, checkpointed always-resumable work — rebuilt on
VH's invariants: local-first, receipt-everything, human gate supreme, no
fake execution, no new framework. Protocol unchanged at v0.10.7.

## What Grok Bot is, and what of it is honest for VH

Grok Bot gives each "Bot" a persistent cloud computer that signs into your
tools and keeps working while you're away; teach-a-task turns one
demonstration into a routine; a layered Auto-Review system decides when a
human is consulted [1][2]. Its own docs admit the per-Bot computer is NOT a
security boundary and credentials sit on xAI/Cursor cloud [3]. gawkbot is the
local-first OSS shape: same roster/goal/routine mechanics on YOUR machine,
human approval gates on external actions, BYO model (Claude/Codex CLI or
local) [4]. VH takes the mechanics and refuses the trust model:

  • goal mode, but steps settle only via the real pipeline (executed /
    planned / gated / refused — never "completed" by fiat);
  • standing approval rules, but session-only, logged, revocable, and
    powerless over `critical` work — permanent autonomy still needs the 90%
    exam;
  • checkpointed resumable work, but on YOUR disk, no cloud VM, no shared
    credential surface.

## 1 · Assignments — goal mode with checkpoints

`src/vh19/goals.ts` + the door's Assignments panel. A goal is decomposed by
the SAME router that fields every message (≤5 specialist steps — a goal is
not a wishlist). "Run next step" pushes the step through `askVH19`: with a
provider key it executes; without one it lands as an honest PLAN; risky
steps pause at the human gate; a denied step is `refused`, a paused goal is
`paused` until an explicit Resume. Every settled step carries its
provenance digest; progress is checkpointed so a closed app resumes where it
stopped; settled steps cannot be re-settled (no outcome laundering).
`probe/goals` pins all of it (19 checks).

## 2 · Session Auto-Review rules at the gate

`src/vh19/gateRules.ts`: at a risky gate ask the modal offers "allow this
category for this session". Rules live in module memory — a restart forgets
everything; a rule answers ONLY when every category in the ask is ruled;
`critical` always pauses at a human; every rule answer is logged and shown
in the door with a one-click revoke. The engine still calls the gate; the
rule is a human-standing decision that answers it, not a bypass.

## 3 · Gemini on the stable line

Reviewer note absorbed: the Gemini native endpoint default moves to the
stable `v1` line (`generativelanguage.googleapis.com/v1`); the
OpenAI-compatible path remains `v1beta/openai/` per Google's docs. Env
override unchanged; `probe/vh19` now pins the stable default.

## 4 · Version identity & gates

18.4.0 → 18.5.0 ("Apex") across every manifest and current-facing doc.
Suites 112 → 113 live / 112 offline bundles (goals +19, door +2 pins);
offline pack, MCP and host engine bundles regenerated and re-pinned.

Gates on node v20.20.2: tsc 0 · protocol 171/171 · unit 20/20 · theme 10/10 ·
collabInvite 29/29 · goals 19/19 · selfEvolve 18/18 · teamEvolve 35/35 ·
vh19 79/79 · vh19Door 28/28 · full live fleet green · offline pack green ·
`sh VERIFY.sh` green on a bare, offline machine.

## References

[1] https://www.layer3labs.io/guides/what-is-grok-bot — Grok Bot explained
    (teach-a-task, always-on cloud computer, approvals).
[2] https://venturebeat.com/orchestration/spacexais-grok-bot-turns-agents-into-persistent-digital-coworkers-that-can-operate-your-apps-for-120-per-month
    — persistent digital coworkers, bot-to-bot delegation.
[3] https://www.digitalapplied.com/blog/grok-bot-ai-teammates-launch-cloud-computer-2026
    — "Do not use separate Bots as a security boundary"; shared cloud VM.
[4] https://gawk.bot/ and
    https://github.com/najmuzzaman-mohammad/gawkbot — gawkbot: local-first
    OSS Grok-Bot shape, approval gates on external actions, BYO model.
