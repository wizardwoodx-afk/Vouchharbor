Vouch Harbor 17.10.11 — what this archive can and cannot claim
===============================================================

ARCHIVE      VouchHarbor-17.10.11-WarrantTeams-VH19.zip
SOURCE     git tree, commit-under-construction 17.10.7 -> 17.10.11
BUILT      2026-09-14, Linux, Node v20.20.2 and v22.20.0, esbuild 0.25.12

VERIFIED IN THIS BUILD (each line was run, not asserted)
  npm test                       113 passed, 0 failed            (exit 0)
  node verify/run.mjs            112 passed, 0 failed            (in the dev tree, node_modules present)
  node verify/run.mjs            106 passed, 5 failed, 1 skipped  (BARE archive, zero deps —
                                 the 5 are pre-existing mcp*/metaLoop suites that need
                                 node_modules; see NOT VERIFIED. My 6 new suites pass there.)
  tsc --noEmit                   0 errors
  npm run build                  clean, dist bundle 478.65 kB
  cd protocol && npm test        171/171 UNIFIED-SENTINEL checks
  wcarena batteries              22/22 · 14/14 · 10/10 · 0-of-4 false-accepts
  unit                           20/20
  host engine byte pin           tools/vh-host-engine.mjs = 325211 B, matches .sha256
  tampered engine                exits 2, refuses to listen (reproduced)
  live host                      npm run host -> VH-A2A-READY, version 17.10.11, mounts
  honest refusal                 with no harness installed the harbor refuses in words and
                                 fabricates no completion (reproduced over HTTP)
  vendored catalog               158 specialists, every one with a description and a tool
                                 ceiling; body SHA-256 recomputed from disk, 158/158 match

NOT VERIFIED — read this before repeating any number above
  CI GREEN      Not verified. GitHub Actions on 17.10.7 reported failure on the
                windows Rust job. The JS gates pass on Linux at Node 20 and 22.
                The Rust stage (cargo check/test/clippy -D warnings) was NOT run
                here: no Rust toolchain in the build environment. 4,794 lines of
                src-tauri Rust are therefore unverified in this archive.
  RELEASE       There is no git tag and no GitHub Release for 17.10.7 or 17.10.11.
                Newest published release remains v17.10.5. Treat this as a build.
  WINDOWS BUILD No Windows build, no NSIS installer, no signed binary in this archive.
                Nothing here was installed on Windows 11.
  BARE-ARCHIVE RUN  Extracted copy, no node_modules: the six new suites (moeRouter,
                moeGate, moeMemory, moeSecurity, specialistRegistry, vh19Honesty) all
                PASS zero-dependency. 5 pre-existing suites fail there (mcpConformance,
                mcpRouter, mcpSdkClient, mcpSdkClientV2, metaLoop) because they import
                from node_modules. The pack's own summary line discloses this ("1
                skipped (need node_modules — esbuild)"), so the runner is honest, but
                the README's "zero install" framing does not hold for those 5 of 112.

  LLM CALLS     No provider request was made with a live API key. Model IDs and the
                USD/MTok table in src/moe/providers.ts are the vendors' published
                contracts plus an ESTIMATE for cost maths, labelled as such. Provider
                wiring is verified by construction and refusal paths only.
  ROUTING       The router is verified as a learning scoring function over a text
                catalog. It is not a neural model, is not "MoE" in the LLM sense, and
                is not validated against any task-accuracy benchmark.
  SCALE         158 specialists are vendored. 300-500 was the target and is NOT what
                this archive contains. Do not present 500.
  THE 90% GATE  Implemented as designed (agent-authored Q&A, user marks correct/wrong).
                In agent-authored mode it measures USER AGREEMENT, not competence; the
                sealed receipt states this. Grounded mode exists and was not exercised
                against a real model run in this build.

KNOWN GAPS WORTH FIXING NEXT
  · probe/*.ts is not typechecked (tsconfig include is ["src"]). Add probe to include.
  · No CI step installs protocol deps or runs protocol/test/selftest.js, so the 171
    checks are manual. One workflow line would close this.
  · state.brain-driven disclosure is pinned by probe/vh19Honesty, but the VH-19 router,
    gate and memory are not yet wired into the browser run path or the A2A mount.
